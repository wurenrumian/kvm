# 模块 07 · 设备直通与 I/O 虚拟化

> 本模块解决一个问题：**I/O 到底慢在哪，以及有哪些办法把它变快**。
> 全模拟、virtio、vhost、vhost-user、VFIO 直通，本质是"数据路径上少经过几个人"。
> 把这条路径画清楚，排错和选型就有了依据。

## 学习目标

- 讲清 I/O 虚拟化的四层数据面：全模拟、virtio、vhost/vhost-user、VFIO 直通，各自"数据经过谁"；
- 吃透 virtio split virtqueue 的三段结构（desc / avail / used）、通知与中断抑制、feature bits 协商；
- 理解 vhost / vhost-user / vDPA 的边界，知道什么场景该用哪个；
- 掌握 IOMMU DMA remapping、IOMMU group/container、`vfio-pci` 绑定全流程与 ACS 分组；
- 会做 GPU 直通与 vGPU / GVT-g / SR-IOV 选型，能处理 vBIOS、vendor reset bug 等经典坑；
- 会直通 USB / SCSI / 串口，会用 virtiofs 做高性能文件共享；
- 建立"直通问题怎么定位"的排错直觉，能读懂 `dmesg`、`lspci -vvv` 和 `/sys/kernel/iommu_groups`。

---

## 7.1 I/O 虚拟化的演进

### 为什么 I/O 比 CPU/内存难虚拟化

CPU 和内存有 VT-x / EPT 这类硬件兜底，开销可以压到接近零；
I/O 不行——设备是**有状态的、异步的、面向寄存器的**，一次磁盘写可能涉及
几十次寄存器读写加一次 DMA。虚拟化必须回答两个问题：

1. **谁来处理这次访问**：硬件（直通）、内核（vhost）、还是用户态（QEMU）？
2. **数据搬几次、跨几次边界**：每次跨用户态/内核态、每次内存拷贝都是成本。

### 四层模型

| 层次 | 机制 | 数据经过 QEMU | 处理者 | 性能 | 典型设备 |
| --- | --- | --- | --- | --- | --- |
| 全模拟 | e1000 / IDE / rtl8139 | 是（逐寄存器模拟） | QEMU 用户态 | 低 | 兼容老系统 |
| virtio | 共享环 + 半虚拟化驱动 | 是 | QEMU 用户态 | 高 | blk / scsi / net |
| vhost | 数据面下沉内核 | 否 | 内核 `vhost` 线程 | 更高 | vhost-net / vhost-scsi |
| vhost-user | 数据面到独立进程 | 否 | DPDK / OVS 进程 | 极高 | OVS-DPDK |
| VFIO 直通 | 整块设备给 guest | 否 | 硬件直接访问 | 接近原生 | GPU / NVMe / 网卡 |

> 关键结论：**越往右，性能和隔离性越好，但灵活性、可迁移性、资源利用率越差**。
> 直通设备通常无法热迁移，也不能被多台 VM 共享。

### 三条数据路径对比

```text
① 全模拟（e1000）：guest 驱动写寄存器 → VM-Exit → QEMU 逐位模拟 → 宿主协议栈
   guest 每次 I/O 都陷出，最慢

② virtio / vhost：guest 驱动只操作共享内存 virtqueue
   guest 写 desc + avail → kick → [QEMU 线程 | vhost 内核线程] → 宿主设备
   只有"通知"这一次会陷出（ioeventfd 可让内核直接接管）

③ VFIO 直通：guest 驱动直接读写物理设备 BAR、直接发 DMA
   IOMMU 把设备 DMA 地址（IOVA=GPA）翻译到宿主物理地址（HPA）
   路径最短，但设备被独占
```

---

## 7.2 virtio 深入：virtqueue 与 feature 协商

### 核心思想

virtio 不做硬件模拟，而是**定义一套"半虚拟化"协议**：
guest 装专用驱动，驱动和 hypervisor 约定一块共享内存（virtqueue），
用生产者-消费者环交换描述符。这样 guest 只需"把请求放进环里 + 敲一下门"，
不必模拟真实硬件的寄存器行为。

规范由 OASIS 维护：**virtio 0.9.5（legacy）** 与 **virtio 1.x（modern）**。
Linux 内核里 guest 侧实现在 `drivers/virtio/`，QEMU 侧在 `hw/virtio/`。

### split virtqueue 的三段结构

一个 virtqueue 由三块**共享内存**组成（guest 分配，双方按 GPA 访问）：

```text
        ┌──────────────── Descriptor Table（描述符表）────────────────┐
        │ desc[0] { addr, len, flags, next }                          │
        │ desc[1] { addr, len, flags, next }   ← 描述一段缓冲区         │
        │ ...                                        flags 可串成链    │
        └─────────────────────────────────────────────────────────────┘
                     ▲ 链头索引写入 ▼
        ┌────────────── Available Ring（avail，驱动写、设备读）─────────┐
        │ flags | idx | ring[0..num-1]                                │
        │   ↑中断抑制        ↑驱动每放一个缓冲就 +1，最后写（发布）      │
        └─────────────────────────────────────────────────────────────┘
                     ▲ 设备回填 ▼
        ┌────────────── Used Ring（used，设备写、驱动读）───────────────┐
        │ flags | idx | ring[0..num-1] { id, len }                    │
        │   ↑通知抑制        ↑设备处理完把链头 id + 实际长度写回         │
        └─────────────────────────────────────────────────────────────┘
```

关键点：

- **desc table**：每个表项 16 字节，`addr` 是 GPA，`len` 是长度，`flags` 有
  `NEXT`（还有下一段）、`WRITE`（设备可写，即接收缓冲）、`INDIRECT`（间接描述符）；
  `next` 指向链中下一个表项。
- **avail ring**：驱动把"要交给设备的链头索引"写进来，`idx` 自增表示有新请求。
  这是**驱动 → 设备**方向。
- **used ring**：设备处理完，把链头 `id` 和实际写入长度写回，`idx` 自增。
  这是**设备 → 驱动**方向。
- **内存序**：`idx` 必须最后写，且要用写屏障（内核里是 `virtio_wmb` /
  `smp_wmb`）保证描述符先于索引对对方可见。这是最容易被忽略的并发细节。

内核符号：`include/uapi/linux/virtio_ring.h` 定义 `vring_desc` / `vring_avail` /
`vring_used`；guest 侧实现在 `drivers/virtio/virtio_ring.c`；QEMU 侧在
`hw/virtio/virtio.c`（`VirtQueue`、`virtqueue_pop`、`virtqueue_push`）。

### 通知机制：kick 与 call

```text
驱动侧（kick）：
  放好请求 → 若未抑制通知 → 写 virtio PCI 的 queue notify 寄存器
  legacy：写 I/O 端口；modern：写 MMIO BAR 中 notify 地址
  → 触发 VM-Exit（QEMU 路径）或命中 ioeventfd（vhost 路径，不陷出）

设备侧（call）：
  处理完 → 若未抑制中断 → 触发中断（MSI-X）
  → 通过 irqfd 直接注入 guest，避免 QEMU 用户态参与
```

**抑制标志**是性能关键，用于批处理、减少 VM-Exit 和中断风暴：

| 标志 | 位置 | 作用 |
| --- | --- | --- |
| `VRING_AVAIL_F_NO_INTERRUPT` | avail.flags | 驱动告诉设备：先别打断我 |
| `VRING_USED_F_NO_NOTIFY` | used.flags | 设备告诉驱动：先别 kick 我 |
| Event Index（`EVENT_IDX`） | 动态阈值 | 用"事件索引"精确控制何时通知，比标志更细 |

`ioeventfd` / `irqfd` 是 KVM 的两个加速原语（`virt/kvm/eventfd.c`）：
把"写 notify 寄存器"绑定到一个 eventfd，内核直接唤醒 vhost 线程；
把"设备中断"绑定到另一个 eventfd，内核直接注入 guest。两者配合，
virtio 数据面可以**完全不经过 QEMU 用户态**。

### feature bits 协商

设备启动时双方交换 64 位（modern）特性位，取交集。驱动只启用双方都支持的位。

通用位（节选）：

| 特性 | 含义 |
| --- | --- |
| `VIRTIO_F_VERSION_1` | modern 设备标志（1.0） |
| `VIRTIO_RING_F_INDIRECT_DESC` | 支持间接描述符，省 desc 表空间 |
| `VIRTIO_RING_F_EVENT_IDX` | 事件索引，精细化通知抑制 |
| `VIRTIO_F_ACCESS_PLATFORM` | 设备受 IOMMU/vIOMMU 约束（原 `IOMMU_PLATFORM`） |
| `VIRTIO_F_RING_PACKED` | packed ring（1.1），更省内存、更易硬件实现 |
| `VIRTIO_F_IN_ORDER` | 保证按序完成，简化驱动 |
| `VIRTIO_F_SR_IOV` | 设备支持 SR-IOV |

设备专属位（节选）：

| 设备 | 特性 | 含义 |
| --- | --- | --- |
| virtio-blk | `BLK_F_MQ` | 多队列 |
| virtio-blk | `BLK_F_FLUSH` / `F_CONFIG_WCE` | 写缓存控制 |
| virtio-blk | `BLK_F_DISCARD` / `F_WRITE_ZEROES` | TRIM / 写零 |
| virtio-net | `NET_F_MQ` | 多队列 |
| virtio-net | `NET_F_CSUM` / `GUEST_CSUM` | 校验和卸载 |
| virtio-net | `NET_F_HOST_TSO4/6` | TSO/LRO 卸载 |
| virtio-net | `NET_F_CTRL_VQ` | 控制队列（MAC、VLAN） |

> 排错启示：guest 里 `ethtool -k` 看到校验和/TSO 全 off，往往是 feature 没协商上，
> 而不是网卡坏了。

### legacy vs modern（PCI 传输层）

| 维度 | legacy（0.9.5） | modern（1.0+） |
| --- | --- | --- |
| 配置访问 | BAR0 I/O 端口，固定偏移 | PCI capability 链 |
| 通知 | I/O 端口写 | MMIO BAR 中 notify 地址 |
| 中断 | 共享 INTx 或 MSI-X | 强制 MSI-X |
| 布局 | 固定 | 由 capability 描述（common/notify/ISR/device） |
| 兼容 | 老系统 | 现代默认 |

Linux 的 `virtio_pci` 同时支持两者；同时实现两套的称 **transitional device**。

### 常见 virtio 设备横向对比

| 设备 | 用途 | 关键特性 | 适用 |
| --- | --- | --- | --- |
| virtio-blk | 块设备，简单高效 | MQ、flush、discard、write_zeroes | 少量盘、追求低延迟 |
| virtio-scsi | SCSI 语义 | 多队列、热插拔、TRIM、透传 SCSI | 多盘、需 `smartctl`/持久预留 |
| virtio-net | 网卡 | MQ、csum/TSO 卸载、RSS | 通用网络 |
| virtio-gpu | 图形 | 2D / virgl / venus | 轻量桌面、3D 共享 |
| virtio-fs | 文件共享 | FUSE over virtio、DAX | 宿主机目录共享 |
| virtio-vsock | 主机- guest socket | 无网络栈 | agent、日志采集 |

**virtio-blk vs virtio-scsi 怎么选**：

- 单盘/少盘、追求极致延迟 → **virtio-blk**，路径更短；
- 多盘、需要热插拔/多队列/TRIM/SCSI 透传 → **virtio-scsi**；
- 两者都走 blk-mq / scsi-mq，现代版本性能差距已经不大；
- 注意：virtio-blk 的多队列是"每队列绑定一个 vCPU"，队列数不宜超过 vCPU 数。

```xml
<!-- virtio-blk：单盘低延迟 -->
<disk type='file' device='disk'>
  <driver name='qemu' type='qcow2' cache='none' io='io_uring' queues='4'/>
  <source file='/var/lib/libvirt/images/db.qcow2'/>
  <target dev='vda' bus='virtio'/>
</disk>

<!-- virtio-scsi：多盘 + 热插拔 -->
<controller type='scsi' index='0' model='virtio-scsi'>
  <driver queues='8'/>
</controller>
```

---

## 7.3 vhost / vhost-user / vDPA：数据面下沉的三个阶段

### vhost：数据面进内核

virtio 的数据面本来在 QEMU 用户态，每次 kick 都要 VM-Exit 到 QEMU。
**vhost** 把处理环的代码移进内核线程，QEMU 只负责控制面（建环、配 feature）：

```text
virtio（QEMU 用户态）                vhost（内核）
guest kick ──VM-Exit──► QEMU         guest kick ──ioeventfd──► vhost worker
QEMU 读环 → aio 提交                 vhost 读环 → 直接收发（tun）
完成 → QEMU 注入中断                  完成 → irqfd 注入中断
（两次用户态参与）                    （零次用户态参与）
```

`vhost` 是通用框架（`drivers/vhost/vhost.c`），各设备后端有
`vhost/net.c`、`vhost/scsi.c`、`vhost/vsock.c`。内核模块：`vhost`、`vhost_net`、
`vhost_scsi`。

```bash
lsmod | grep -E 'vhost'
# vhost_net        # 网卡数据面内核加速
# vhost            # 通用框架
```

```xml
<interface type='network'>
  <source network='default'/>
  <model type='virtio'/>
  <driver name='vhost' queues='4'/>   <!-- 内核 vhost -->
</interface>
```

### vhost-user：数据面到独立进程

当数据面需要在**用户态做更多事**（OVS-DPDK 的流表、用户态协议栈），
vhost 就把环交给一个独立进程，双方用 **Unix socket** 跑 vhost-user 协议
（QEMU 文档 `docs/interop/vhost-user.rst`）。

控制消息（节选）：`GET/SET_FEATURES`、`SET_MEM_TABLE`（共享内存区域）、
`SET_VRING_NUM/ADDR/BASE`、`SET_VRING_KICK/CALL`、`SET_LOG_BASE`（脏页日志，
迁移用）、`IOTLB_MSG`（配合 vIOMMU）。

libvirt 里 vhost-user 网卡用**接口类型** `vhostuser` 指定，而不是
`<driver>` 的 name（后者常见的 `vhost` 指内核 vhost）：

```xml
<interface type='vhostuser'>
  <source type='unix' path='/var/run/ovs/vhost0.sock' mode='server'/>
  <model type='virtio'/>
</interface>
```

```text
guest ──ioeventfd──► [vhost-user 后端进程，如 ovs-vswitchd]
        共享内存（vring）  │  用户态收发 / DPDK 轮询
                          ▼
                      物理网卡（DPDK PMD 直接接管）
```

### vDPA：数据面进硬件

**vDPA（vhost Data Path Acceleration）** 更进一步：让**硬件直接实现 virtio
数据面**，控制面仍走 vhost 协议。网卡表现为一个"virtio 设备"，但环的收发由
硬件完成。内核侧 `drivers/vhost/vdpa.c` + `drivers/vdpa/`，用户态用
`vdpa` netlink 或 `vhost-vdpa` 驱动接入。

```bash
# 查看/创建 vDPA 设备（示意，需硬件支持）
vdpa dev show
vdpa dev add name vdpa0 mgmtdev pci/0000:03:00.2
```

### 三者对比

| 维度 | vhost | vhost-user | vDPA |
| --- | --- | --- | --- |
| 数据面位置 | 内核线程 | 用户态进程 | 硬件 |
| 谁读环 | `vhost_*` 内核模块 | DPDK/OVS 进程 | 网卡硬件 |
| 用户态参与 | 无 | 后端进程 | 无 |
| 灵活度 | 中 | 高（可编程） | 低（硬件固定） |
| 典型场景 | 通用虚拟化 | SDN、NFV | 智能网卡卸载 |
| 迁移 | 支持 | 支持（可配 log） | 视实现 |

> 选型口诀：**普通业务用 vhost；需要 DPDK/OVS 流表用 vhost-user；
> 有 SmartNIC 且追求极限用 vDPA**。

---

## 7.4 VFIO 与 IOMMU：直通的正确姿势

### 直通要解决的两个问题

把物理设备直接给 guest，会带来两个必须由硬件回答的问题：

1. **DMA 安全**：设备发起 DMA 用的是"guest 物理地址"，如何保证它只能访问
   这台 VM 的内存，而不是别的 VM 或内核内存？
2. **中断隔离**：设备中断如何只投递给这台 VM？

答案是 **IOMMU**（Intel VT-d / AMD-Vi）：

- **DMA remapping**：IOMMU 为每个设备域维护页表，把设备看到的地址（IOVA）
  翻译成宿主物理地址（HPA）。直通时，VFIO 把 guest 的内存区域
  （GPA）映射进这个域，于是**设备眼中的 IOVA 就是 GPA**，DMA 精确落在
  guest 内存里。
- **Interrupt remapping**：把 MSI/MSI-X 中断按来源重映射到正确的 guest
  中断，防止设备伪造中断。

```text
        设备发起 DMA: IOVA(=GPA) ──► [IOMMU 页表] ──► HPA（guest 的物理页）
                                          ▲
                          VFIO_IOMMU_MAP_DMA 建立 GPA→HPA 映射

        无 IOMMU 时设备 DMA 直通宿主物理地址 → 可越界访问，绝不能直通
```

> `iommu=pt`（pass-through）让**不参与直通**的设备走恒等映射，
> 避免所有设备都吃地址翻译的开销；直通设备仍用完整页表。

### 前置条件

1. CPU 支持 **VT-d / AMD-Vi**（IOMMU）；
2. 内核开启 IOMMU：`intel_iommu=on iommu=pt`（加到内核 cmdline）；
3. 设备能被 IOMMU **分组隔离**；
4. 设备绑定到 **vfio-pci** 驱动。

```bash
# 开启 IOMMU（Fedora，GRUB）
sudo grubby --update-kernel=ALL --args="intel_iommu=on iommu=pt"
# 确认
cat /proc/cmdline
dmesg | grep -i -e DMAR -e IOMMU
# 应为 "DMAR: IOMMU enabled"
```

### IOMMU group 与 container

- **IOMMU group**：IOMMU 能区分/隔离的**最小设备集合**。同一 group 内的设备
  共享一个 IOMMU 域，必须整组分配给同一个使用者（同一台 VM 或宿主机）。
  为什么？因为 IOMMU 只能按 group 设置地址翻译，无法区分 group 内设备。
- **container**：VFIO 里"共享同一地址空间的一组 group"。一个 VM 通常一个
  container。内核侧 `vfio_iommu_type1` 实现地址空间管理。

```text
/dev/vfio/vfio            ← container（地址空间）
/dev/vfio/<group_id>      ← group
/dev/vfio/<group_id>      ← group（同一 container 可含多组）
   │
   └── VFIO_GROUP_GET_DEVICE_FD → /dev/vfio/<group>/<device> 的 fd
```

QEMU 打开设备的典型 ioctl 序列（`hw/vfio/`）：

```text
VFIO_GET_API_VERSION
VFIO_CHECK_EXTENSION(TYPE1_IOMMU)
open /dev/vfio/<group> → VFIO_GROUP_GET_STATUS
VFIO_GROUP_SET_CONTAINER(container_fd)
VFIO_SET_IOMMU(TYPE1v2_IOMMU)
对每段 guest RAM：VFIO_IOMMU_MAP_DMA
VFIO_GROUP_GET_DEVICE_FD → VFIO_DEVICE_GET_INFO / GET_REGION_INFO
VFIO_DEVICE_SET_IRQS 绑定中断（eventfd）
VFIO_DEVICE_RESET
```

内核源码：`drivers/vfio/vfio.c`（框架）、`drivers/vfio/vfio_iommu_type1.c`
（type1 域）、`drivers/vfio/pci/vfio_pci.c`（PCI 设备）、
`drivers/iommu/intel/iommu.c`（Intel VT-d）。

### 绑定全流程

```bash
# 1. 确认 IOMMU 开启（见上）
dmesg | grep -i -e DMAR -e IOMMU

# 2. 找到目标设备（记下 vendor:device id）
lspci -nn | grep -i nvidia        # 例：01:00.0 VGA ... [10de:xxxx]

# 3. 查看 IOMMU 分组（组内有哪些设备）
for d in /sys/kernel/iommu_groups/*/devices/*; do
  g=$(basename "$(dirname "$(dirname "$d")")")
  echo "Group $g: $(basename "$d")"
done | sort -V

# 4. 绑定到 vfio-pci
# 方式 A（推荐，开机即绑）：内核参数
#   vfio-pci.ids=10de:xxxx,10de:yyyy
sudo grubby --update-kernel=ALL --args="vfio-pci.ids=10de:xxxx,10de:yyyy"

# 方式 B：运行时手动解绑 + driver_override
echo 0000:01:00.0 | sudo tee /sys/bus/pci/devices/0000:01:00.0/driver/unbind
echo vfio-pci    | sudo tee /sys/bus/pci/devices/0000:01:00.0/driver_override
echo 0000:01:00.0 | sudo tee /sys/bus/pci/drivers_probe
# 验证
lspci -k -s 01:00.0 | grep -A2 'Kernel driver'
```

XML：

```xml
<hostdev mode='subsystem' type='pci' managed='yes'>
  <driver name='vfio'/>
  <source>
    <address domain='0x0000' bus='0x01' slot='0x00' function='0x0'/>
  </source>
</hostdev>
```

> `managed='yes'` 让 libvirt 在 VM 启动时自动解绑宿主驱动并绑 `vfio-pci`，
> 关机时还原。`managed='no'` 则要求你提前绑好。
> 同一 IOMMU 组内的所有设备需一起直通，否则隔离不成立。

### ACS 与分组过大

**ACS（Access Control Services）** 是 PCIe 交换机/根端口的一项能力，
控制不同下游端口之间能否做 peer-to-peer 访问。没有 ACS，同一交换机下的
多个设备可能无法互相隔离，于是被塞进同一个 IOMMU group。

```bash
# 查看设备/桥的 ACS 能力
lspci -vvv -s 01:00.0 | grep -i acs
# 输出示例：
#   ACSCap: SrcValid+ TransBlk+ ReqRedir+ CmpltRedir+ UpstreamFwd+ EgressCtrl- DirectTrans-
#   ACSCtl: SrcValid- TransBlk- ReqRedir- ...
# 注意：有 ACSCap 不代表开着（ACSCtl 才是当前状态）
```

处理策略：

| 情况 | 做法 | 安全性 |
| --- | --- | --- |
| 设备在独立根端口下 | 天然独立分组 | 好 |
| 同组有多个设备 | 全部一起直通 | 好（但浪费） |
| 换 PCIe 插槽可拆组 | 换插槽/用带 ACS 的交换机 | 好 |
| 缺 ACS 且无法换 | `pcie_acs_override` 强制分组 | **差**，破坏隔离，仅实验 |

> `pcie_acs_override` 不是上游特性，需要打补丁的内核，且会**牺牲隔离安全性**，
> 生产环境不推荐。

### vIOMMU 与嵌套翻译

给 guest 也配一个虚拟 IOMMU（`<iommu model='intel'/>`），guest 内部就能用
IOMMU（如运行自己的 VM，或做设备直通的 DMA 隔离）。此时是**两级翻译**：
guest IOVA → GPA 由 vIOMMU 负责，GPA → HPA 由宿主 IOMMU 负责。
这要求硬件支持嵌套翻译（Intel 的 scalable mode 等），并配合 vhost-user 的
`IOTLB_MSG` 让后端感知映射变化。

---

## 7.5 GPU 直通、vGPU 与 GPU 半虚拟化

### 三种路线

| 方式 | 原理 | 一卡多机 | 迁移 | 适用 |
| --- | --- | --- | --- | --- |
| **整卡直通（VFIO）** | 整块 GPU 给一台 guest | 否 | 否 | AI 训练、单机高性能 |
| **vGPU / GVT-g / SR-IOV** | 硬件切分显存/时间片给多台 guest | 是 | 部分 | VDI、多租户图形 |
| **GPU 半虚拟化（virtio-gpu / venus）** | 共享渲染，命令转发到宿主 GPU | 是 | 是 | 轻量桌面、3D 共享 |

### 整卡直通

基本流程同 7.4，但有 GPU 特有的坑：

**1. vBIOS（显存 ROM）**

某些卡（尤其消费级、部分 NVIDIA）不给 ROM，guest 初始化时拿不到
显示参数，需要从宿主机 dump 后喂给 QEMU。

```bash
# dump vBIOS（设备未被占用时）
echo 1 | sudo tee /sys/bus/pci/devices/0000:01:00.0/rom
sudo cat /sys/bus/pci/devices/0000:01:00.0/rom > /var/lib/libvirt/vbios/01.rom
echo 0 | sudo tee /sys/bus/pci/devices/0000:01:00.0/rom
```

```xml
<hostdev mode='subsystem' type='pci' managed='yes'>
  <driver name='vfio'/>
  <source><address domain='0x0000' bus='0x01' slot='0x00' function='0x0'/></source>
  <rom bar='on' file='/var/lib/libvirt/vbios/01.rom'/>
</hostdev>
```

**2. vendor reset bug**

消费级 AMD（Polaris / Vega / Navi 部分型号）在 guest 重启后无法正确复位，
表现为黑屏或 FLR 失败。可用社区模块 `vendor-reset`（DKMS）打补丁，
或干脆避免重启 guest、用冷启动恢复。

```bash
dmesg | grep -i -E 'reset|flr' | tail
# 出现 "Failed to reset ..." / "FLR failed" 即命中
```

**3. 音频 function**

显卡的 HDMI 音频通常是同一 PCI 设备的 **function 0x1**，往往与显卡同组。
只直通 0x0、不直通 0x1 可能导致 guest 音频异常或分组报错——**一起直通**。

```xml
<!-- 显卡 + 音频一起直通 -->
<hostdev mode='subsystem' type='pci' managed='yes'>
  <source><address domain='0x0000' bus='0x01' slot='0x00' function='0x0'/></source>
</hostdev>
<hostdev mode='subsystem' type='pci' managed='yes'>
  <source><address domain='0x0000' bus='0x01' slot='0x00' function='0x1'/></source>
</hostdev>
```

**4. 宿主机不能再占用该 GPU**

直通要求宿主驱动（nouveau / amdgpu / nvidia / i915）不绑定该卡。
若宿主机控制台正用这块卡，会看到黑屏或绑定失败。做法：

- 用集成显卡（iGPU）给宿主机输出；
- 内核参数关闭 framebuffer：`video=efifb:off`、`initcall_blacklist=sysfb_init`；
- 把 GPU 的 vendor:device id 加进 `vfio-pci.ids=`，开机就绑 vfio；
- 屏蔽宿主驱动：`modprobe.blacklist=nouveau`。

**5. NVIDIA 消费卡的历史限制**

早期 GeForce 驱动检测到虚拟机会报 "Error 43" 拒绝工作，需要隐藏 KVM：
libvirt 侧 `<features><kvm><hidden state='on'/></kvm></features>`。
较新驱动已放开，但隐藏 KVM 对某些专业软件仍有必要。

### vGPU / GVT-g / SR-IOV

- **NVIDIA vGPU**：需要 vGPU 许可的卡 + 宿主 vGPU Manager 驱动 + guest
  vGPU 驱动（均需 license）。通过 **mdev（mediated device）** 框架呈现为
  虚拟 PCI 设备，用 `mdevctl` 管理。
- **Intel GVT-g**：基于 i915 的 mdev 实现，`i915.enable_gvt=1`，
  mdev 类型形如 `i915-GVTg_V5_4`；在新平台已被 Xe 的 SR-IOV 取代。
- **SR-IOV GPU**：新一代 Intel Xe / AMD / NVIDIA 支持把 GPU 切成多个
  VF（真实 PCI function），VF 走标准 VFIO 直通，隔离性和兼容性最好。

```bash
# 查看可用的 mdev 类型
ls /sys/bus/pci/devices/0000:01:00.0/mdev_supported_types/
cat /sys/bus/pci/devices/0000:01:00.0/mdev_supported_types/nvidia-256/available_instances

# 创建 mdev（得到 UUID）
UUID=$(uuidgen)
echo "$UUID" | sudo tee /sys/bus/pci/devices/0000:01:00.0/mdev_supported_types/nvidia-256/create
mdevctl list
```

```xml
<hostdev mode='subsystem' type='mdev' model='vfio-pci' managed='yes'>
  <source><address uuid='9a1b...'/></source>
</hostdev>
```

### GPU 半虚拟化：virtio-gpu / virgl / venus

`virtio-gpu` 支持三种能力层次：

| 层次 | 名称 | 提供的能力 | 后端 |
| --- | --- | --- | --- |
| 2D | virtio-gpu | 显示、光标、基础加速 | QEMU |
| 3D | virgl | OpenGL ES / OpenGL | virglrenderer |
| Vulkan | venus | Vulkan | virglrenderer + venus |

优点：可共享、可迁移、无需独占；缺点：**不支持 CUDA/ROCm**，
3D 性能与兼容性不如直通。适合 Linux 桌面、CI 里的图形测试。

> 结论：**要 CUDA 训练 → 整卡直通或 vGPU/SR-IOV；
> 要多租户图形桌面 → vGPU；要轻量共享图形 → virtio-gpu + venus**。

---

## 7.6 其他直通：USB / SCSI / 串口 / virtiofs

### USB 直通

```xml
<!-- 按 vendor:product 直通单个 USB 设备 -->
<hostdev mode='subsystem' type='usb' managed='yes'>
  <source>
    <vendor id='0x1234'/><product id='0x5678'/>
  </source>
</hostdev>

<!-- 按物理端口直通（更稳定，避免同型号混淆） -->
<hostdev mode='subsystem' type='usb' managed='yes'>
  <source><address bus='1' port='4'/></source>
</hostdev>
```

```bash
lsusb                       # 找 vendor:product
lsusb -t                    # 看拓扑与端口
virsh attach-device web01 usb.xml --live   # 热插拔
```

- **整个 USB 控制器直通**：把控制器的 PCI 设备当 hostdev 直通，并在 guest
  配 `<controller type='usb' model='qemu-xhci'/>`（或先禁用模拟控制器），
  这样 guest 能独占该控制器下所有 USB 口。
- **远程重定向**：SPICE 场景可用 `<redirdev bus='usb' type='spicevmc'/>`
  把客户端本地 USB 设备重定向进 guest。

### SCSI / 块设备透传

```xml
<!-- 透传宿主机 scsi_host 上的一个 LUN（SAN/磁带常用） -->
<hostdev mode='subsystem' type='scsi' managed='yes'>
  <source>
    <adapter name='scsi_host5'/>
    <address bus='0' target='0' unit='0'/>
  </source>
</hostdev>
```

适用场景：SAN LUN、磁带库、需要 SCSI 持久预留（persistent reservation）
的集群文件系统、需要 `smartctl`/`sg_io` 透传的盘。
QEMU 侧对应 `scsi-generic` / `scsi-block`，libvirt 通常自动选择。

### 串口直通

```xml
<!-- 默认：pty，virsh console 可连 -->
<serial type='pty'><target port='0'/></serial>

<!-- 直通宿主机物理串口 -->
<serial type='dev'>
  <source path='/dev/ttyS1'/>
  <target port='0'/>
</serial>

<!-- 走 TCP，便于远程调试 -->
<serial type='tcp'>
  <source mode='bind' host='0.0.0.0' service='4444'/>
  <protocol type='telnet'/>
  <target port='0'/>
</serial>
```

### virtiofs：高性能目录共享

virtiofs 把宿主机目录以 FUSE-over-virtio 方式共享给 guest，比传统的
9p/virtio-9p 快得多，且支持 DAX（让 guest 直接 mmap 宿主机页缓存，
减少拷贝）。由用户态 `virtiofsd` 提供服务。

```bash
# 手动启动 virtiofsd（libvirt 可代管）
sudo /usr/libexec/virtiofsd \
  --socket-path=/run/virtiofsd/web01.sock \
  --shared-dir=/srv/share \
  --cache=auto --sandbox=namespace
```

```xml
<memoryBacking>
  <source type='memfd'/>
  <access mode='shared'/>      <!-- virtiofs 需要共享内存 -->
</memoryBacking>

<filesystem type='mount' accessmode='passthrough'>
  <driver type='virtiofs'/>
  <source dir='/srv/share'/>
  <target dir='/mnt/share'/>
</filesystem>
```

guest 内：

```bash
mount -t virtiofs myfs /mnt/share
```

> 对比：9p 实现简单但每次读写多次拷贝、延迟高；virtiofs 走共享内存 +
> 多队列，适合大文件与低延迟场景。若 guest 未加载 `virtiofs` 模块或
> virtiofsd socket 权限不对，挂载会失败。

---

## 7.7 直通选型决策树

```text
需要把物理设备给 VM？
│
├─ 设备能被 IOMMU 独立分组？ ── 否 ──► 换插槽 / 一起直通 / 放弃直通
│                                    （ACS override 仅实验）
└─ 是
   ├─ 设备需要被多台 VM 共享？
   │   ├─ 是 → SR-IOV（VF 直通）或 mdev/vGPU，或 virtio 半虚拟化
   │   └─ 否 → 整卡/整设备 VFIO 直通
   │
   ├─ 需要热迁移？
   │   ├─ 是 → 只能 virtio（virtio-net / virtio-blk / virtiofs）
   │   └─ 否 → 直通可接受
   │
   └─ 追求极限性能且能接受独占？
       ├─ 是 → VFIO 直通 / SR-IOV VF
       └─ 否 → virtio + vhost（默认）
```

## 7.8 直通对性能与迁移的影响

- **性能**：直通省掉了 QEMU/内核数据面，延迟和吞吐接近原生；但 CPU 侧
  仍可能因 MSI-X/MMIO 的 VM-Exit 有少量开销。
- **迁移**：绝大多数直通设备**不可热迁移**（设备状态无法完整保存/恢复）。
  带直通设备的 VM 迁移需要先热拔设备，或用 SR-IOV 的迁移方案。
- **资源利用率**：直通是"独占"，超分不了；SR-IOV/vGPU 才能一卡多机。
- **快照**：直通设备状态不在快照里，快照恢复后设备可能不一致。
- **安全边界**：直通设备被 guest 完全控制，务必确认 IOMMU 隔离成立，
  否则恶意 guest 可发起 DMA 攻击（模块 10）。

---

## 动手实验

**实验 7-1：对比 virtio-blk 与 IDE 性能**

同一 guest 分别用 `bus='ide'` 和 `bus='virtio'`，`fio` 对比 IOPS 与延迟：

```bash
fio --name=t --ioengine=libaio --direct=1 --rw=randread --bs=4k \
    --iodepth=32 --numjobs=4 --size=1G --runtime=30 --group_reporting
```

**实验 7-2：查看 IOMMU 分组**

```bash
ls /sys/kernel/iommu_groups/ | wc -l
for d in /sys/kernel/iommu_groups/*/devices/*; do
  echo "Group $(basename $(dirname $(dirname $d))): $(basename $d)"
done | sort -V | head
```

记录哪些组包含多个设备，思考为什么。

**实验 7-3：直通一个 USB 设备**

用 `lsusb` 找设备，按 7.6 的 XML 直通，进 guest 用 `lsusb` 验证；
再用 `virsh detach-device` 热拔，观察 guest 行为。

**实验 7-4：确认 vhost 生效**

```bash
lsmod | grep vhost_net
# guest 跑 iperf3 时，用 top/ps 观察 vhost 线程 CPU 占用
ps -eLo pid,tid,comm | grep -E 'vhost|qemu'
```

**实验 7-5：对比 virtio 与 vhost 的 VM-Exit**

```bash
sudo perf kvm stat live
# 分别在 virtio（无 vhost）和 vhost 下压网络，观察 IO 相关 VM-Exit 数量差异
```

**实验 7-6：virtiofs 共享目录**

按 7.6 配置 virtiofs，guest 内挂载，用 `dd`/`fio` 对比 9p 的吞吐，
并观察 `--cache` 模式对结果的影响。

**实验 7-7：dump vBIOS（选做，需闲置 GPU）**

按 7.5 的步骤 dump ROM，用 `rom bar='on' file=...` 配置，验证 guest 是否
能正常初始化显示。

**实验 7-8：观察 IOMMU 翻译与页错误（进阶）**

```bash
# 直通设备运行中，查看 DMAR 相关日志
sudo dmesg -w | grep -i -E 'DMAR|IOMMU|vfio'
```

---

## 排错指南

| 现象 | 根因 | 定位命令 | 处理 |
| --- | --- | --- | --- |
| 直通后 guest 黑屏 | 宿主机 framebuffer/驱动仍占用 GPU | `lspci -k -s 01:00.0`、`dmesg \| grep -i efifb` | 用 iGPU 输出，加 `video=efifb:off`、`initcall_blacklist=sysfb_init` |
| `vfio-pci` 绑定失败/device busy | 设备被宿主驱动占用 | `readlink /sys/bus/pci/devices/0000:01:00.0/driver` | 先 `unbind`，用 `driver_override` 或 `vfio-pci.ids=` |
| IOMMU 分组过大，无法单卡直通 | 缺 ACS 或拓扑限制 | `lspci -vvv -s ... \| grep -i acs`、`/sys/kernel/iommu_groups` | 换 PCIe 插槽；`pcie_acs_override` 仅实验 |
| `DMAR: IOMMU disabled` | 内核未开 IOMMU | `cat /proc/cmdline`、`dmesg \| grep -i DMAR` | 加 `intel_iommu=on iommu=pt` 并重启 |
| `vfio: DMA map failed` / not permitted | IOMMU 未启用或映射错误 | `dmesg \| grep -i vfio` | 确认 IOMMU 开启、guest RAM 已映射 |
| 直通后中断丢失/风暴 | 中断重映射未开 | `dmesg`、`/proc/interrupts` | 开启 IRQ remapping；勿用 `allow_unsafe_interrupts` |
| guest 重启后 GPU 失效 | vendor reset bug | `dmesg \| grep -i -E 'reset\|flr'` | 装 `vendor-reset`，或避免 guest 重启 |
| GPU 直通后无 HDMI 音频 | 未直通 function 0x1 | `lspci -nn` | 显卡与音频一起直通 |
| 直通网卡后宿主机失联 | 把管理网口直通了 | `ip a`、`lspci -k` | 用另一块网卡做管理 |
| 直通设备 guest 不识别 | 缺驱动/vBIOS | guest 内 `lspci -k`、`dmesg` | 装驱动；提供 vBIOS |
| virtio 设备 guest 不识别 | 缺 virtio 驱动 | guest 内 `lsmod`、`lspci` | 装 `virtio-win` / 内核模块 |
| 多队列未生效 | queues > vCPU 或未协商 | `ethtool -l`、`ls /sys/block/vda/mq` | 调整 `queues`，确认 feature 协商 |
| virtiofs 挂载失败 | virtiofsd 未跑/权限/缺模块 | `ps aux \| grep virtiofsd`、`dmesg` | 启动 daemon、修 socket 权限、装模块 |
| USB 直通设备不出现 | 未从宿主解绑或端口变化 | `lsusb`、`virsh dumpxml` | 用 `managed='yes'`，按端口而非产品直通 |
| 迁移报直通设备不支持 | 直通设备不可迁移 | `virsh migrate` 报错 | 迁移前热拔设备，或换 SR-IOV 方案 |

### 案例复盘 A：消费级 AMD 卡，guest 一重启就黑屏

**现象**：显卡直通后首次开机正常，`reboot` 之后 guest 黑屏，
`dmesg` 里出现 `Failed to reset ...`，必须冷启动宿主机才能恢复。

**定位**：`dmesg | grep -i -E 'reset|flr'` 命中；`lspci -vv` 看设备
Reset 能力；查型号发现是 Polaris/Navi 消费卡。

**根因**：这些卡缺少正确的 function-level reset 支持，guest 重启时
hypervisor 无法把 GPU 恢复到干净状态。

**处理**：安装 `vendor-reset` DKMS 模块（提供设备特定的复位钩子），
或在流程上避免 guest 重启（用 `virsh destroy` + `start` 冷启）。
换专业卡/服务器卡可根治。

### 案例复盘 B：绑 vfio 后整块 NVMe 消失

**现象**：为直通一块 NVMe 执行了 unbind，随后宿主机上该盘符消失，
`/` 所在盘也报错，系统近乎不可用。

**定位**：`lsblk` 发现消失的不止目标盘；`for d in /sys/kernel/iommu_groups/*/devices/*` 显示多个 NVMe 在同一 group。

**根因**：NVMe 控制器（甚至同一 PCIe 交换下的多块盘）可能共处一个
IOMMU group，unbind 一个等于整组脱离宿主驱动。

**处理**：确认目标盘的 group 只含它自己再操作；若同组，需把整组一起
直通（包括系统盘），这通常不可接受——正确做法是换插槽或换支持 ACS 的
平台。**教训：动手前先看 `/sys/kernel/iommu_groups`**。

### 案例复盘 C：virtiofs 共享目录延迟比本地高一个量级

**现象**：guest 挂载 virtiofs 后，小文件随机读写延迟远高于预期，
`dd` 大文件吞吐尚可。

**定位**：对比 `--cache=auto` 与 `--cache=always`；`perf` 观察
FUSE 请求路径；发现每个小文件操作都走完整 FUSE 往返。

**根因**：小文件场景下，FUSE 协议往返和宿主 `virtiofsd` 的同步策略
成为瓶颈；DAX 未启用导致还要拷贝。

**处理**：对大文件/读多写少场景启用 DAX；调整 `virtiofsd --cache`；
小文件密集场景考虑改用块设备或 9p 的合适参数。**结论：virtiofs 不是
万能加速，要按负载选 cache/DAX 策略**。

---

## 练习

1. 画图对比 virtio-net、vhost-net、vDPA、SR-IOV VF 四种网络的数据路径，
   标出每次 I/O 经过哪些组件。
2. 解释 split virtqueue 中 `avail.idx` 与 `used.idx` 的内存序要求，
   为什么 `idx` 必须最后写？
3. 为什么 GPU 直通后宿主机通常不能再用这块卡？列出至少三种让宿主机
   "让出" GPU 的手段。
4. 说明 IOMMU group 对直通安全性的意义。同组设备为什么必须一起分配？
5. 给定一块支持 SR-IOV 的网卡，设计"一卡多机 + 可迁移"的方案，
   说明每台 VM 的取舍。
6. 你的服务器上某块盘直通后宿主失联，写出你的排查顺序（命令级）。

## 延伸阅读

- 内核文档：`Documentation/driver-api/vfio.rst`
- VFIO 与 IOMMU：<https://www.kernel.org/doc/html/latest/driver-api/vfio.html>
- QEMU vhost-user 协议：`docs/interop/vhost-user.rst`
- QEMU virtio 实现：`hw/virtio/`、`hw/vfio/`、`backends/vhost-user.c`
- 内核 virtio 驱动：`drivers/virtio/`、`drivers/vhost/`、`drivers/vdpa/`
- NVIDIA vGPU 文档：<https://docs.nvidia.com/grid/>
- QEMU virtio 规范（OASIS）：<https://docs.oasis-open.org/virtio/virtio/>
- Linux IOMMU 文档：`Documentation/arch/x86/iommu.rst`、`Documentation/driver-api/iommu.rst`
- vendor-reset 项目：<https://github.com/gnif/vendor-reset>
