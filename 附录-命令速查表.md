# 附录 · 命令速查表

> 一页纸备查（已扩充为全量速查）。所有命令在 Fedora 44 + libvirt 环境下验证过语义。
> 风格：命令块 + 表格 + 极简说明；正文出现 XML 一律用反引号包裹。

## 一、环境与检查

```bash
# 硬件虚拟化能力
grep -m1 -oE 'vmx|svm' /proc/cpuinfo
lscpu | grep -iE 'Virtualization|Hypervisor|Model name'
lsmod | grep -E '^kvm'
ls -l /dev/kvm
systemd-detect-virt
dmesg | grep -iE 'kvm|vmx|svm|iommu'

# KVM 模块参数总览
ls /sys/module/kvm*/parameters/
cat /sys/module/kvm_intel/parameters/*     # Intel
cat /sys/module/kvm_amd/parameters/*       # AMD

# 安装
sudo dnf install -y qemu-kvm libvirt-daemon-kvm virt-install libguestfs-tools
sudo systemctl enable --now libvirtd

# 版本
qemu-system-x86_64 --version
virsh version
qemu-system-x86_64 -accel help
qemu-system-x86_64 -M help | head

# 能力检查
virt-host-validate
```

| 检查项 | 期望 | 说明 |
| --- | --- | --- |
| `grep -oE 'vmx\|svm'` | 有输出 | CPU 支持硬件虚拟化 |
| `ls /dev/kvm` | 存在 | KVM 模块已加载 |
| `systemd-detect-virt` | `none` | 裸机（嵌套时为 kvm） |
| `virt-host-validate` | 全 PASS | 综合检查（含 IOMMU、SELinux） |

## 二、QEMU 裸命令

```bash
# 最简启动
qemu-system-x86_64 -enable-kvm -cpu host -smp 2 -m 2048 \
  -drive file=disk.qcow2,if=virtio,format=qcow2 -nographic

# 挂 ISO 安装
qemu-system-x86_64 -enable-kvm -cpu host -smp 2 -m 2048 \
  -cdrom install.iso -boot d -nographic

# 常用参数
#   -enable-kvm        KVM 加速（等价 -accel kvm）
#   -accel kvm        显式加速器
#   -cpu host          CPU 透传
#   -smp N             vCPU 数（-smp 4,cores=2,threads=2 可指定拓扑）
#   -m MB              内存（-m 2G 亦可）
#   -drive file=...,if=virtio,format=qcow2
#   -blockdev ...      更现代的块设备写法
#   -netdev user,id=n0 -device virtio-net-pci,netdev=n0
#   -nographic         串口直连
#   -daemonize         后台运行
#   -pidfile /run/x.pid
#   -name vm1 -uuid <uuid>
#   -monitor stdio     QEMU monitor
#   -qmp unix:/run/qmp.sock,server,nowait
#   -nodefaults        不创建默认设备
#   -S                 启动即暂停
#   -snapshot          临时盘（不落盘）
#   -no-reboot         退出而非重启
#   Ctrl-a x           退出
```

```bash
# 内核直启（常用于 microVM / 实验）
qemu-system-x86_64 -enable-kvm -cpu host -m 512 -smp 1 \
  -kernel vmlinuz -initrd initrd.img -append "console=ttyS0 root=/dev/vda" \
  -drive file=rootfs.img,if=virtio,format=raw -nographic

# PCI 直通
qemu-system-x86_64 -enable-kvm -cpu host -m 4096 \
  -device vfio-pci,host=0000:01:00.0 -nographic

# QMP 控制
qemu-system-x86_64 ... -qmp unix:/run/qmp.sock,server,nowait
# 另开终端：socat - UNIX-CONNECT:/run/qmp.sock
```

常用 QEMU monitor 命令：

```text
info kvm           # 是否 KVM 加速
info status        # 运行/暂停
info cpus          # vCPU 状态与线程
info registers     # 寄存器
info block         # 块设备
info network       # 网卡
info pci           # PCI 拓扑
info mtree         # 内存地址空间
xp /8xb 0x100000   # 读物理内存
```

## 三、qemu-img

```bash
qemu-img create -f qcow2 disk.qcow2 20G
qemu-img create -f raw disk.raw 20G
qemu-img info disk.qcow2
qemu-img info --backing-chain disk.qcow2
qemu-img convert -f qcow2 -O raw disk.qcow2 disk.raw
qemu-img convert -O qcow2 -c disk.raw disk.qcow2      # -c 压缩
qemu-img resize disk.qcow2 +10G
qemu-img resize --shrink disk.qcow2 10G
qemu-img commit overlay.qcow2
qemu-img check disk.qcow2
qemu-img check -r all disk.qcow2                      # 自动修复
qemu-img create -f qcow2 -b base.qcow2 -F qcow2 overlay.qcow2
qemu-img map disk.qcow2                               # 分配了哪些扇区
qemu-img snapshot -c snap1 disk.qcow2                 # 内部快照
qemu-img snapshot -l disk.qcow2
qemu-img snapshot -a snap1 disk.qcow2
qemu-img snapshot -d snap1 disk.qcow2
qemu-img compare a.qcow2 b.qcow2
qemu-img dd if=disk.qcow2 of=out.raw bs=1M
qemu-img bench -f qcow2 disk.qcow2
qemu-img amend -f qcow2 -o lazy_refcounts=on disk.qcow2
```

| 格式 | 说明 | 适用 |
| --- | --- | --- |
| `raw` | 无元数据，性能最好 | 固定大小、直通盘 |
| `qcow2` | 稀疏、快照、后备链、压缩 | 通用、需要快照 |
| `vmdk`/`vhd`/`vhdx` | 兼容 VMware/Hyper-V | 迁移 |
| `qcow`/`qed` | 历史格式 | 不推荐新用 |

## 四、virt-install

```bash
sudo virt-install \
  --name vm1 --memory 2048 --vcpus 2 \
  --cpu host-passthrough \
  --disk path=/var/lib/libvirt/images/vm1.qcow2,size=10,bus=virtio \
  --cdrom /path/install.iso \
  --network network=default,model=virtio \
  --graphics none --console pty,target_type=serial

# 从 cloud 镜像导入
sudo virt-install --name c1 --memory 2048 --vcpus 2 --import \
  --disk path=/var/lib/libvirt/images/c1.qcow2,bus=virtio \
  --cloud-init root-password-generate=on \
  --network network=default,model=virtio \
  --graphics none --console pty,target_type=serial

# 只打印 XML / 预演
virt-install ... --print-xml
virt-install ... --dry-run

# 常用开关
#   --machine q35            新机型（推荐）
#   --os-variant fedora41    优化默认设备
#   --boot uefi              固件类型
#   --boot hd,cdrom,menu=on  启动顺序
#   --vcpus 4,maxvcpus=8     支持热插拔上限
#   --memorybacking hugepages=yes
#   --numatune 0,mode=strict --cpu pinning
#   --hostdev 0000:01:00.0   直通
#   --tpm backend.type=emulator,backend.version=2.0
#   --rng /dev/urandom
#   --watchdog i6300esb
#   --controller type=scsi,model=virtio-scsi
#   --network bridge=br0,model=virtio
#   --graphics vnc,listen=0.0.0.0
#   --noautoconsole
#   --autostart
```

## 五、virsh 生命周期

```bash
virsh list --all
virsh start vm1
virsh shutdown vm1          # 优雅（ACPI）
virsh destroy vm1           # 强制（拔电）
virsh reboot vm1
virsh reset vm1
virsh suspend vm1 / resume vm1
virsh autostart vm1 / autostart --disable vm1
virsh domstate vm1 / dominfo vm1
virsh console vm1           # 退出 Ctrl+]
virsh undefine vm1 --remove-all-storage
virsh undefine vm1 --nvram --remove-all-storage   # UEFI 变量也删
virsh send-key vm1 KEY_LEFTCTRL KEY_ALT KEY_DELETE
virsh domdisplay vm1        # VNC 地址
```

## 六、virsh 配置与信息

```bash
# 定义/编辑
virsh dumpxml vm1
virsh edit vm1
virsh define vm1.xml
virsh dumpxml vm1 > vm1.xml.bak

# 热插拔 / 调整
virsh attach-disk vm1 /data/d2.qcow2 vdb --persistent
virsh detach-disk vm1 vdb --persistent
virsh attach-interface vm1 network default --model virtio --persistent
virsh detach-interface vm1 network --mac <mac> --persistent
virsh attach-device vm1 dev.xml --live --config
virsh detach-device vm1 dev.xml --live --config
virsh update-device vm1 dev.xml --live
virsh change-media vm1 sda /path/new.iso --insert --config
virsh blockresize vm1 vda 30G
virsh setvcpus vm1 4 --live --config
virsh setmem vm1 3G --live --config
virsh setmaxmem vm1 8G --config

# 查询
virsh domblklist vm1
virsh domiflist vm1
virsh vcpuinfo vm1
virsh cpu-stats vm1
virsh domstats vm1
virsh dominfo vm1
virsh desc vm1
virsh hostname vm1
virsh domcontrol vm1

# 调度与限流
virsh schedinfo vm1
virsh schedinfo vm1 --set vcpu_quota=50000
virsh blkdeviotune vm1 vda --total-iops-sec 1000
virsh numatune vm1 --mode strict --nodeset 0
```

## 七、快照 / 克隆 / 备份

```bash
# 内部快照（qcow2，元数据简单，性能略降）
virsh snapshot-create-as vm1 snap1 "desc"
virsh snapshot-list vm1
virsh snapshot-info vm1 snap1
virsh snapshot-revert vm1 snap1
virsh snapshot-delete vm1 snap1
virsh snapshot-current vm1

# 外部快照（推荐，磁盘可共享/可回滚）
virsh snapshot-create-as vm1 ext1 --disk-only --atomic

# 后备链操作
virsh blockcommit vm1 vda --active --pivot     # 合并到基础盘
virsh blockpull vm1 vda --wait --verbose       # 拉平到当前盘
virsh blockcopy vm1 vda /shared/vm1.qcow2 --wait --verbose

# 克隆
virt-clone --original vm1 --name vm2 --file /var/lib/libvirt/images/vm2.qcow2
virt-clone --original vm1 --name vm2 --auto-clone

# 备份 API
virsh backup-begin vm1 --backupxml backup.xml
virsh backup-dumpxml vm1
virsh checkpoint-create-as vm1 ck1
virsh checkpoint-list vm1

# 离线镜像处理
virt-customize -a vm1.qcow2 --hostname vm2 --root-password password:xxx
virt-sysprep -a vm1.qcow2
virt-sparsify --compress vm1.qcow2 vm1-slim.qcow2
virt-inspector -a vm1.qcow2 | head
virt-filesystems -a vm1.qcow2 --all --long
guestfish --ro -a vm1.qcow2 -i ls /
```

| 类型 | 命令 | 优点 | 缺点 |
| --- | --- | --- | --- |
| 内部快照 | `snapshot-create-as` | 单文件、管理简单 | 依赖 qcow2、性能略降 |
| 外部快照 | `--disk-only` | 可回滚/可共享、性能好 | 文件多、需管理后备链 |
| 块提交 | `blockcommit --pivot` | 合并外部快照 | 耗时、占 IO |
| 块拉平 | `blockpull` | 合并后备链 | 中断依赖 |
| 备份 API | `backup-begin` | 增量、一致性 | 需 XML 定义 |

## 八、网络

```bash
virsh net-list --all
virsh net-dumpxml default
virsh net-start default / net-autostart default
virsh net-define net.xml
virsh net-undefine default
virsh net-destroy default
virsh net-update default add ip-dhcp-host \
  "<host mac='52:54:00:aa:bb:cc' name='vm1' ip='192.168.122.10'/>" --live --config

ip -d link show virbr0
bridge link show
brctl show virbr0
virsh domiflist vm1
virsh domifaddr vm1 --source arp
virsh net-dhcp-leases default
virsh domifstat vm1 vnet0

# OVS（如使用）
sudo ovs-vsctl show
sudo ovs-vsctl add-br br0
sudo ovs-vsctl add-port br0 eth0
```

| 网络类型 | 说明 | 场景 |
| --- | --- | --- |
| `network` (NAT) | libvirt 默认，virbr0 转发 | 开发、单机 |
| `bridge` | 直接桥接到物理网卡 | 生产、需外部可达 |
| `direct` | macvtap | 高性能、无桥 |
| `hostdev` | SR-IOV VF 直通 | 极致性能 |
| `user` | QEMU 用户态 NAT | 免 root 实验 |

## 九、存储池

```bash
virsh pool-list --all
virsh pool-dumpxml p
virsh pool-define-as p dir --target /data/vms
virsh pool-build p && virsh pool-start p && virsh pool-autostart p
virsh pool-refresh p
virsh pool-delete p && virsh pool-undefine p

virsh vol-list p
virsh vol-create-as p d1.qcow2 20G --format qcow2
virsh vol-clone --pool p d1.qcow2 d2.qcow2
virsh vol-resize --pool p d1.qcow2 30G
virsh vol-delete --pool p d1.qcow2
virsh vol-wipe --pool p d1.qcow2
virsh vol-upload --pool p d1.qcow2 local.img
virsh vol-download --pool p d1.qcow2 local.img
virsh vol-path --pool p d1.qcow2
virsh vol-name --pool p d1.qcow2
```

```bash
# iSCSI 池
virsh pool-define-as iscsi1 iscsi --source-host 10.0.0.5 \
  --source-dev iqn.2026-01.com.example:storage --target /dev/disk/by-path
# RBD 池
virsh pool-define-as rbd1 rbd --source-host ceph1 --source-name vms \
  --source-auth-type ceph --source-secret <uuid>
# netfs 池
virsh pool-define-as nfs1 netfs --source-host 10.0.0.6 \
  --source-path /export/vms --target /mnt/vms
```

## 十、迁移

```bash
# 基础 live migration（共享存储）
virsh migrate --live vm1 qemu+ssh://host2/system

# 存储不共享：全量/增量复制
virsh migrate --live --copy-storage-all vm1 qemu+ssh://host2/system
virsh migrate --live --copy-storage-inc vm1 qemu+ssh://host2/system

# 后拷贝（内存大、停机敏感）
virsh migrate --live --postcopy vm1 qemu+ssh://host2/system

# 自动收敛（脏页跟不上时）
virsh migrate --live --auto-converge vm1 qemu+ssh://host2/system

# 对等/隧道/并行
virsh migrate --live --p2p vm1 qemu+ssh://host2/system
virsh migrate --live --tunneled vm1 qemu+ssh://host2/system
virsh migrate --live --parallel --parallel-connections 4 vm1 qemu+ssh://host2/system

# 只迁部分磁盘 / 压缩
virsh migrate --live --migrate-disks vdb vm1 qemu+ssh://host2/system
virsh migrate --live --comp-methods xbzrle vm1 qemu+ssh://host2/system

# 观察与控制
virsh domjobinfo vm1
virsh domjobabort vm1
virsh migrate-setmaxdowntime vm1 500
virsh migrate-setspeed vm1 1000
virsh migrate-compcache vm1 --size 100

# 存储迁移
virsh blockcopy vm1 vda /shared/vm1.qcow2 --wait --verbose
```

| URI | 传输 | 说明 |
| --- | --- | --- |
| `qemu+ssh://host2/system` | SSH | 常用，走 ssh 隧道 |
| `qemu+tcp://host2/system` | TCP | 需配置认证 |
| `qemu+unix:///system` | 本地 socket | 本机管理 |
| `tcp://host2` | 裸迁移流 | 配合 `--migrateuri` |

## 十一、监控排障

```bash
virsh domstats vm1 --cpu-total --balloon --block --interface
virsh domstats vm1 --state --vcpu --perf
virsh domblkstat vm1 vda
virsh domifstat vm1 vnet0
virsh dommemstat vm1
virsh cpu-stats vm1 --total
virsh vcpuinfo vm1
sudo kvm_stat --once
sudo kvm_stat --event exits
sudo perf kvm stat live
sudo perf kvm stat record -a sleep 5 && sudo perf kvm stat report
sudo perf kvm --host --guest record -a sleep 5
sudo trace-cmd record -e kvm:* sleep 5 && sudo trace-cmd report
sudo ls /sys/kernel/debug/tracing/events/kvm/
sudo bpftrace -l 'tracepoint:kvm:*'

# 日志
sudo tail -f /var/log/libvirt/qemu/vm1.log
journalctl -u virtqemud -n 50
journalctl -u libvirtd -n 50
sudo ausearch -m avc -ts recent
dmesg -T | tail -50

# QMP/HMP 直查
virsh qemu-monitor-command vm1 --hmp 'info block'
virsh qemu-monitor-command vm1 --hmp 'info network'
virsh qemu-monitor-command vm1 --pretty '{"execute":"query-kvm"}'
virsh qemu-monitor-command vm1 --pretty '{"execute":"query-block"}'
virsh qemu-monitor-command vm1 --pretty '{"execute":"query-cpus-fast"}'
```

## 十二、性能调优速查

```bash
# CPU
virsh vcpupin vm1 0 2
virsh vcpupin vm1 --live --config
virsh emulatorpin vm1 0-1
virsh iothreadpin vm1 1 4
virsh schedinfo vm1 --set vcpu_quota=50000
numactl --hardware
taskset -pc <pid>
chrt -f -p 90 <vcpu-tid>

# 内存
grep HugePages /proc/meminfo
echo 4 | sudo tee /sys/kernel/mm/hugepages/hugepages-1048576kB/nr_hugepages
echo 0 | sudo tee /sys/kernel/mm/ksm/run
virsh setmem vm1 3G --live
virsh dommemstat vm1

# 磁盘/网络压测
fio --name=t --ioengine=libaio --rw=randread --bs=4k --iodepth=32 \
  --numjobs=4 --size=1G --runtime=30 --group_reporting
iperf3 -c <ip> -P 8
stress-ng --cpu 4 --timeout 60s
cyclictest -m -p 90 -i 200 -l 100000 -h 400

# 宿主实时性调优（内核参数）
#   isolcpus=2-5 nohz_full=2-5 rcu_nocbs=2-5
#   intel_pstate=disable  (按需)
sudo tuned-adm profile realtime-virtual-host
```

| 调优项 | 参数 | 效果 |
| --- | --- | --- |
| vCPU 绑核 | `vcpupin` | 减少调度迁移 |
| 模拟器绑核 | `emulatorpin` | I/O 线程不抢 vCPU |
| I/O 线程 | `iothreadpin` | 隔离磁盘处理 |
| 大页 | `memoryBacking/hugepages` | 降低 TLB miss |
| 锁内存 | `memoryBacking/locked` | 消除缺页抖动 |
| 关 KSM | `ksm/run=0` | 去侧信道与扫描开销 |
| 多队列 | `queues=N` | 提升 virtio 吞吐 |

## 十三、直通与安全

```bash
# IOMMU / VFIO
dmesg | grep -i -e DMAR -e IOMMU
for d in /sys/kernel/iommu_groups/*/devices/*; do \
  echo "Group $(basename $(dirname $(dirname $d))): $(basename $d)"; done
lspci -nnk
virsh nodedev-list --tree
virsh nodedev-dumpxml pci_0000_01_00_0
virsh nodedev-detach pci_0000_01_00_0
virsh nodedev-reattach pci_0000_01_00_0
echo 8086 1234 | sudo tee /sys/bus/pci/drivers/vfio-pci/new_id
sudo modprobe vfio-pci

# sVirt / SELinux
getenforce
ps -eZ | grep qemu
ls -Z /var/lib/libvirt/images/*.qcow2
sudo semanage fcontext -a -t svirt_image_t "/data/vms(/.*)?"
sudo restorecon -Rv /data/vms
sudo setsebool -P virt_use_nfs on

# AppArmor（Debian 系）
sudo aa-status

# 嵌套虚拟化
cat /sys/module/kvm_intel/parameters/nested
sudo modprobe kvm_intel nested=1
echo 'options kvm_intel nested=1' | sudo tee /etc/modprobe.d/kvm.conf

# 机密计算
cat /sys/module/kvm_amd/parameters/sev /sys/module/kvm_amd/parameters/sev_snp
cat /sys/module/kvm_intel/parameters/tdx
ls -l /dev/sev /dev/sev-guest
```

## 十四、嵌套与机密计算

```bash
# 嵌套
cat /sys/module/kvm_intel/parameters/nested      # Intel
cat /sys/module/kvm_amd/parameters/nested        # AMD
grep -oE 'vmx|svm' /proc/cpuinfo                 # guest 内
ls -l /dev/kvm                                   # guest 内

# SEV 能力（QEMU monitor）
#   { "execute": "query-sev" }
#   { "execute": "query-sev-capabilities" }

# TDX
dmesg | grep -i tdx
cat /sys/module/kvm_intel/parameters/tdx
```

| 参数 | 位置 | 说明 |
| --- | --- | --- |
| `nested` | kvm_intel/kvm_amd | 嵌套开关（新内核默认开） |
| `enable_shadow_vmcs` | kvm_intel | 影子 VMCS，嵌套性能关键 |
| `ept` / `npt` | kvm_intel/kvm_amd | 二级页表 |
| `sev` / `sev_es` / `sev_snp` | kvm_amd | SEV 各档支持 |
| `tdx` | kvm_intel | TDX 支持 |

## 十五、常用 XML 片段

```xml
<!-- 磁盘：高性能 -->
<disk type='file' device='disk'>
  <driver name='qemu' type='qcow2' cache='none' io='io_uring' discard='unmap' queues='4'/>
  <source file='/var/lib/libvirt/images/vm1.qcow2'/>
  <target dev='vda' bus='virtio'/>
</disk>

<!-- 网卡：多队列 + vhost -->
<interface type='network'>
  <source network='default'/>
  <model type='virtio'/>
  <driver name='vhost' queues='4'/>
</interface>

<!-- CPU：绑核 -->
<vcpu placement='static'>4</vcpu>
<cputune>
  <vcpupin vcpu='0' cpuset='2'/>
  <vcpupin vcpu='1' cpuset='3'/>
  <emulatorpin cpuset='0-1'/>
</cputune>

<!-- CPU：模型 + 拓扑 -->
<cpu mode='host-passthrough' check='none'>
  <topology sockets='1' dies='1' cores='4' threads='2'/>
  <feature policy='require' name='vmx'/>
</cpu>

<!-- 内存：巨页 + 锁内存 -->
<memoryBacking>
  <hugepages>
    <page size='1048576' unit='KiB'/>
  </hugepages>
  <locked/>
</memoryBacking>

<!-- 直通 -->
<hostdev mode='subsystem' type='pci' managed='yes'>
  <source><address domain='0x0000' bus='0x01' slot='0x00' function='0x0'/></source>
</hostdev>

<!-- vTPM -->
<tpm model='tpm-crb'><backend type='emulator' version='2.0'/></tpm>

<!-- UEFI + Secure Boot -->
<os firmware='efi'>
  <type arch='x86_64' machine='q35'>hvm</type>
  <loader secure='yes'/>
  <nvram template='/usr/share/OVMF/OVMF_VARS.fd'/>
</os>
<features><smm state='on'/></features>

<!-- iothread + virtiofs -->
<iothreads>1</iothreads>
<disk type='file' device='disk'>
  <driver name='qemu' type='qcow2' iothread='1' cache='none' io='native'/>
  <source file='/data/vm1.qcow2'/>
  <target dev='vda' bus='virtio'/>
</disk>
<filesystem type='mount' accessmode='passthrough'>
  <driver type='virtiofs' queue='1024'/>
  <source dir='/host/share'/>
  <target dir='share'/>
</filesystem>

<!-- watchdog / rng -->
<watchdog model='i6300esb' action='reset'/>
<rng model='virtio'>
  <backend model='random'>/dev/urandom</backend>
</rng>

<!-- 图形：VNC 无密码（仅内网） -->
<graphics type='vnc' port='-1' autoport='yes' listen='127.0.0.1'/>
```

| 片段 | 关键属性 | 备注 |
| --- | --- | --- |
| 磁盘 driver | `cache`/`io`/`discard`/`queues` | `io_uring` 现代默认 |
| 网卡 driver | `name='vhost'`/`queues` | 多队列需 guest 配合 |
| cputune | `vcpupin`/`emulatorpin` | 与隔离核配合 |
| memoryBacking | `hugepages`/`locked` | 实时与高性能必备 |
| loader | `secure='yes'` | 需 q35 + SMM |

## 十六、常用路径与日志

| 内容 | 路径 / 命令 |
| --- | --- |
| VM 磁盘默认目录 | `/var/lib/libvirt/images/` |
| VM XML | `virsh dumpxml vm1` |
| QEMU 日志 | `/var/log/libvirt/qemu/vm1.log` |
| libvirt 日志 | `journalctl -u virtqemud`、`journalctl -u libvirtd` |
| 网络定义 | `/etc/libvirt/qemu/networks/` |
| 存储池定义 | `/etc/libvirt/storage/` |
| 内核日志 | `dmesg`、`journalctl -k` |
| SELinux 审计 | `/var/log/audit/audit.log`、`ausearch -m avc` |
| KVM 事件 | `/sys/kernel/debug/tracing/events/kvm/` |
| OVMF 固件 | `/usr/share/OVMF/`、`/usr/share/edk2/ovmf/` |
| QEMU 二进制 | `/usr/bin/qemu-system-x86_64` |

## 十七、退出 / 快捷键

| 场景 | 按键 |
| --- | --- |
| QEMU `-nographic` 退出 | `Ctrl-a` 然后 `x` |
| QEMU 切 monitor | `Ctrl-a` 然后 `c` |
| QEMU 帮助 | `Ctrl-a` 然后 `h` |
| virsh console 退出 | `Ctrl-]` |
| VNC 释放鼠标 | `Ctrl-Alt` |
| QEMU monitor 退出 | `quit` 或 `Ctrl-a x` |

## 十八、字段解读小注

| 来源 | 字段 | 含义 |
| --- | --- | --- |
| `virsh dominfo` | State / CPU time / Max memory | 状态、累计 CPU、上限内存 |
| `virsh domstats` | `cpu.time`、`vcpu.N.time` | 累计 CPU 纳秒 |
| `virsh domstats` | `balloon.current/available` | 当前/可用内存（balloon） |
| `virsh domstats` | `block.N.rd.reqs`、`net.N.rx.bytes` | 块请求、网络字节 |
| `virsh domblkstat` | `rd_req`、`rd_bytes`、`wr_*`、`flush_reqs` | 块 I/O 计数 |
| `virsh domifstat` | `rx_bytes`、`rx_errs`、`rx_drop`、`tx_*` | 网卡收发与丢包 |
| `qemu-img info` | virtual size / disk size | 逻辑大小 / 实际占用 |
| `qemu-img info` | backing file | 后备链父盘 |
| `lscpu` | Virtualization / Hypervisor vendor | 虚拟化能力 / 是否在 VM 中 |
| `top` | `%st`（steal） | 被 hypervisor 抢走的 CPU |
| `kvm_stat` | `exit_*` | 各类 VM-Exit 次数 |
| `schedinfo` | `vcpu_quota`/`vcpu_period` | vCPU 带宽限流 |
