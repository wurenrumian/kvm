# 模块 02 · 环境搭建与第一台虚拟机

## 学习目标

- 在 Fedora 44 上装好 QEMU/KVM + libvirt 工具链，说清每个包各自提供什么；
- 理解 Fedora 的 modular daemons 架构与 systemd socket 激活；
- 用 **两种方式** 跑通第一台虚拟机：裸 QEMU 命令行、libvirt/virt-install；
- 读懂 QEMU 关键参数（`-accel`/`-machine`/`-device`/`-netdev`/`-blockdev`/`-nographic`）；
- 掌握 virt-install 常用参数，理解 XML 方式；
- 理解 libvirt 默认网络，能进 guest 控制台；
- 掌握 cloud-init 原理，并用 `--cloud-init` 免安装起 VM。

---

## 2.1 安装工具链

```bash
# 核心组件
sudo dnf install -y qemu-kvm libvirt-daemon-kvm virt-install \
    libguestfs-tools guestfs-tools virt-top bridge-utils

# 让 libvirt 管理网络（默认 NAT 网络 virbr0）
sudo systemctl enable --now libvirtd
sudo systemctl enable --now virtnetworkd    # Fedora 上 libvirt 拆分了 modular daemons

# 加入用户组，避免每次 sudo（重新登录后生效）
sudo usermod -aG kvm,libvirt "$USER"

# 检查
virsh version
virsh net-list --all
```

> **Fedora 注意**：新版把 libvirtd 拆成 `virtqemud`/`virtnetworkd`/`virtstoraged` 等
> modular daemons。`systemctl enable --now libvirtd` 若提示不存在，改用对应 daemon，
> 或统一 `systemctl enable --now virtqemud.socket virtnetworkd.socket`。
> **modular 与单体二选一，不要同时开**，否则会抢 socket。

### 每个包到底装了什么

| 包 | 提供 | 作用 |
| --- | --- | --- |
| `qemu-kvm` | `qemu-system-x86_64`、`qemu-img` | QEMU 系统模拟器，KVM 加速的入口；`qemu-img` 管理镜像 |
| `libvirt-daemon-kvm` | `virtqemud` + QEMU/KVM 驱动 | 拉齐 libvirt 核心 daemon 与 KVM 支持 |
| `libvirt-daemon` | `libvirtd`（单体型） | 传统单体型 daemon（与 modular 二选一） |
| `libvirt-client` | `virsh` | 命令行客户端 |
| `virt-install` | `virt-install`/`virt-clone`/`virt-xml` | 基于 libvirt 的建机与批量改配置工具（Python virtinst） |
| `libguestfs-tools` | `guestfish`、libguestfs 库 | 离线查看/编辑 guest 磁盘的底层工具 |
| `guestfs-tools` | `virt-customize`/`virt-df`/`virt-resize`/`virt-inspector` | 基于 libguestfs 的高层工具 |
| `virt-top` | `virt-top` | 类似 `top` 的虚拟机监控 |
| `bridge-utils` | `brctl` | 传统网桥工具（现多用 `ip link`/`bridge`，但脚本仍常见） |
| `libvirt-daemon-config-network` | `default` 网络定义 | 提供 virbr0 的默认 NAT 网络 XML |
| `qemu-guest-agent` | guest 内 agent | 宿主机与 guest 交互（优雅关机、取 IP、fstrim），需装进 guest |
| `virt-manager`（可选） | GUI | 图形化管理 |

> 注意：`libguestfs-tools` 与 `guestfs-tools` 的分工随 Fedora 版本变化，
> 老版本里 `virt-*` 都归 `libguestfs-tools`。不确定就两个都装（如上）。

### Fedora modular daemons 架构

老版本 libvirt 只有一个巨大的 `libvirtd`，什么都管；新版本按"驱动"拆成多个
按需启动的小 daemon，全部通过 **systemd socket 激活**（有人连 socket 才拉起进程）。

```text
                         +------------------------------+
   virsh / virt-install  |  /run/libvirt/libvirt-sock    |
        |                +--------------+---------------+
        | 连接                          | socket 激活
        v                               v
   +--------------+            +------------------+
   | virtqemud    |  QEMU/KVM  | virtnetworkd     |  网络（virbr0/dnsmasq）
   | (域/生命周期)|            | virtstoraged     |  存储池/卷
   +------+-------+            | virtnodedevd     |  主机设备（nodedev）
          |                    | virtnwfilterd    |  网络过滤
          | 调用               | virtsecretd      |  密钥/secret
          v                    | virtinterfaced   |  主机网卡
   +--------------+            | virtproxyd       |  远程代理
   | virtlogd     |  日志      +------------------+
   | (按需拉起)   |
   +------+-------+
          |
          v
   qemu-system-x86_64 进程
```

| daemon | 管什么 | 对应 socket |
| --- | --- | --- |
| `virtqemud` | QEMU/KVM 域 | `virtqemud.socket`（另有 `-ro`/`-admin`） |
| `virtnetworkd` | 虚拟网络 | `virtnetworkd.socket` |
| `virtstoraged` | 存储池/卷 | `virtstoraged.socket` |
| `virtnodedevd` | 主机设备 | `virtnodedevd.socket` |
| `virtnwfilterd` | 网络过滤规则 | `virtnwfilterd.socket` |
| `virtsecretd` | secrets | `virtsecretd.socket` |
| `virtinterfaced` | 主机接口 | `virtinterfaced.socket` |
| `virtproxyd` | 远程/统一入口 | `virtproxyd.socket` |
| `virtlogd` | 每域日志 | `virtlogd.socket` |

启用方式（二选一）：

```bash
# 方式 A：modular（推荐，新 Fedora 默认）
sudo systemctl enable --now virtqemud.socket virtnetworkd.socket \
    virtstoraged.socket virtnodedevd.socket virtnwfilterd.socket \
    virtsecretd.socket virtinterfaced.socket virtproxyd.socket virtlogd.socket

# 方式 B：单体（老习惯）
sudo systemctl enable --now libvirtd.socket
```

查看与排错：

```bash
systemctl list-unit-files 'virt*.socket' | grep enabled
systemctl status virtqemud.socket
sudo systemctl restart virtqemud
journalctl -u virtqemud -u virtnetworkd --since '10 min ago'
```

---

## 2.2 准备一个系统镜像

```bash
mkdir -p ~/kvm/images ~/kvm/iso && cd ~/kvm

# 方案 A：下载 Fedora Cloud 镜像（约 600MB，自带 cloud-init）
wget -P images https://download.fedoraproject.org/pub/fedora/linux/releases/44/Cloud/x86_64/images/Fedora-Cloud-Base-44-1.1.x86_64.qcow2

# 方案 B：下载 Alpine virt ISO（极小，适合练手）
# wget -P iso https://dl-cdn.alpinelinux.org/alpine/v3.20/releases/x86_64/alpine-virt-3.20.0-x86_64.iso
```

镜像类型速览：

| 类型 | 说明 | 用途 |
| --- | --- | --- |
| ISO 安装镜像 | 需走安装流程 | 首次装系统 |
| Cloud 镜像（qcow2） | 预装好、带 cloud-init | `--import` 免安装 |
| 容器镜像（tar） | 与 VM 无关 | 容器用 |

```bash
# 看看镜像格式、虚拟大小、后备链
qemu-img info images/Fedora-Cloud-Base-44-1.1.x86_64.qcow2
file iso/alpine-virt-3.20.0-x86_64.iso
```

> 下载 404 多半是版本号/路径变了，用 `curl -I URL` 确认实际文件名，
> 或到 Fedora/Alpine 的 mirror 列表里找当前版本。

---

## 2.3 方式一：裸 QEMU 命令行（理解本质）

QEMU 的参数就是虚拟机的"零件清单"。先跑一台最简单的（无图形，串口输出）：

```bash
# 用 Alpine ISO 演示：-nographic 把串口接到终端
qemu-system-x86_64 \
  -accel kvm \
  -cpu host \
  -smp 2 \
  -m 1024 \
  -cdrom ~/kvm/iso/alpine-virt-3.20.0-x86_64.iso \
  -boot d \
  -nographic
```

参数解读（这些都会在模块 03 展开）：

| 参数 | 含义 |
| --- | --- |
| `-accel kvm` | 用 KVM 加速（等价旧写法 `-enable-kvm`；不加就是纯软件 TCG，慢 10~50 倍） |
| `-cpu host` | vCPU 型号透传宿主机特性 |
| `-smp 2` | 2 个 vCPU |
| `-m 1024` | 1GB 内存 |
| `-cdrom` | 挂载 ISO |
| `-nographic` | 无图形，串口直连终端 |

**用磁盘镜像跑持久化系统**：

```bash
# 创建 10G qcow2 磁盘
qemu-img create -f qcow2 ~/kvm/images/disk1.qcow2 10G

qemu-system-x86_64 \
  -accel kvm -cpu host -smp 2 -m 2048 \
  -drive file=$HOME/kvm/images/disk1.qcow2,if=virtio,format=qcow2 \
  -cdrom ~/kvm/iso/alpine-virt-3.20.0-x86_64.iso \
  -boot d -nographic
```

> 退出 QEMU：`Ctrl-a` 松开再按 `x`（在 `-nographic` 下）。

### QEMU 命令行更细

**1）加速器与机器类型**

```bash
-accel kvm                        # 现代写法
-accel kvm,kernel_irqchip=on      # 内核内中断控制器（默认）
-accel tcg                        # 纯软件模拟（对照实验用）
-machine q35,accel=kvm            # q35 芯片组（PCIe，推荐）
-machine pc,accel=kvm             # i440fx（老，兼容性最好）
```

- `-accel kvm` 与 `-enable-kvm` 等价；`-machine accel=kvm` 也行。
- **q35 vs i440fx**：q35 是较新的芯片组，原生 PCIe、无 IDE、更适合
  virtio/VFIO；老 guest 镜像或某些驱动只认 i440fx 时再用 `pc`。

**2）前端与后端分离：`-device` / `-netdev` / `-blockdev`**

QEMU 把"设备（前端）"和"后端"分开描述，这是理解 QEMU 命令行的关键。

```bash
# 网络：后端 -netdev（tap），前端 -device（virtio-net-pci）
qemu-system-x86_64 -accel kvm -cpu host -m 1024 \
  -netdev tap,id=n0,ifname=tap0,script=no,downscript=no \
  -device virtio-net-pci,netdev=n0,mac=52:54:00:12:34:56 \
  -nographic

# 磁盘：后端 -blockdev（节点图），前端 -device
qemu-system-x86_64 -accel kvm -cpu host -m 1024 \
  -blockdev driver=file,node-name=file0,filename=$HOME/kvm/images/disk1.qcow2 \
  -blockdev driver=qcow2,node-name=disk0,file=file0 \
  -device virtio-blk-pci,drive=disk0 \
  -nographic
```

| 写法 | 层级 | 特点 |
| --- | --- | --- |
| `-drive file=...,if=virtio,format=qcow2` | 后端+前端二合一 | 简洁，老写法，隐式创建节点 |
| `-blockdev` + `-device` | 后端与前端分开 | 显式节点图，支持快照/过滤/多后端，libvirt 新版本用 |
| `-netdev` + `-device` | 后端与前端分开 | 网络标准写法 |

`-drive` 的常见选项：`if=virtio`（总线）、`cache=none|writeback`、
`io=native|threads`、`format=qcow2|raw`、`discard=unmap`、`aio=io_uring`。

**3）控制台与图形**

```bash
-nographic                        # 无图形 + 串口/监控接到 stdio
-serial mon:stdio                 # 串口与 monitor 复用 stdio
-serial pty                       # 串口挂到伪终端（libvirt 常用）
-display none                     # 只要无图形
-vnc :1                           # VNC 监听 5901
-monitor stdio                    # QEMU monitor 接到终端
-monitor unix:/tmp/qemu-mon,server,nowait
-qmp unix:/tmp/qemu-qmp,server,nowait   # QMP（libvirt 用这个）
```

- `-nographic` 下 `Ctrl-a c` 在串口与 monitor 间切换，`Ctrl-a x` 退出。
- **QMP** 是 QEMU 的机器可读控制协议，libvirt 主要通过 QMP 管理 QEMU。

**4）后台运行与调试**

```bash
-daemonize                        # 转后台（要求无 stdio 交互）
-pidfile /tmp/vm.pid
-D /tmp/qemu.log                  # 日志文件
-d int,cpu_reset,guest_errors     # 调试开关：中断/复位/guest 错误
-trace events=/tmp/events         # 跟踪事件
```

**5）CPU 与拓扑**

```bash
-smp 4,sockets=1,cores=2,threads=2   # 显式拓扑，影响 guest 看到的 SMT
-cpu host,migratable=on              # 透传但屏蔽影响迁移的特性
-cpu qemu64,+ssse3,+sse4.1           # 自定义特性
-numa node,nodeid=0,cpus=0-1,memdev=mem0
```

---

## 2.4 方式二：libvirt（生产用法）

libvirt 用 XML 描述虚拟机，命令更规范、可管理。两种子方式：

### A. 快速用 virt-install

```bash
sudo virt-install \
  --name demo01 \
  --memory 2048 \
  --vcpus 2 \
  --cpu host-passthrough \
  --disk path=/var/lib/libvirt/images/demo01.qcow2,size=10,format=qcow2,bus=virtio \
  --cdrom ~/kvm/iso/alpine-virt-3.20.0-x86_64.iso \
  --network network=default,model=virtio \
  --graphics none \
  --console pty,target_type=serial \
  --os-variant detect=on
```

装完系统后（或对 cloud 镜像），日常操作用 virsh：

```bash
virsh list --all
virsh start demo01
virsh console demo01        # 串口登录，退出按 Ctrl+]
virsh shutdown demo01       # 优雅关机
virsh destroy demo01        # 强制断电（相当于拔电源）
virsh undefine demo01 --remove-all-storage
```

### virt-install 常用参数逐一说明

| 参数 | 作用 | 备注 |
| --- | --- | --- |
| `--name` | 域名称（唯一） | 也是默认磁盘/日志名来源 |
| `--memory` | 内存 MB，或 `maxmemory=...` | 支持热插拔需设 `maxmemory` |
| `--vcpus` | vCPU 数，可 `4,maxvcpus=8` | `--vcpus 4,sockets=1,cores=2,threads=2` 设拓扑 |
| `--cpu` | `host-passthrough`/`host-model`/`custom` | 迁移与性能的关键 |
| `--machine` | 芯片组，如 `q35` | 默认随 `--os-variant` |
| `--disk` | 磁盘，逗号分隔属性 | `path,size,format,bus,cache,io,sparse` |
| `--cdrom` / `--location` | ISO / 安装树 URL | `--location` 可用网络安装源 |
| `--import` | 直接引导已有磁盘，不走安装 | 配合 cloud 镜像 |
| `--network` | 网络，`network=`/`bridge=` | `model=virtio,mac=...,virtualport=` |
| `--graphics` | `none`/`spice`/`vnc` | 无头服务器用 `none` |
| `--console` | 串口控制台 | `pty,target_type=serial` |
| `--os-variant` | 优化 guest 类型 | `detect=on` 或 `fedora-unknown` |
| `--cloud-init` | 注入 cloud-init 数据 | 见 2.6 |
| `--boot` | 启动顺序/固件 | `hd,cdrom` 或 `uefi` |
| `--features` | ACPI/APIC/Hyper-V 等 | `--features hyperv.relaxed=on` |
| `--rng` / `--tpm` | 随机数/TPM 设备 | TPM 用于 Windows 11/BitLocker |
| `--hostdev` | 设备直通 | 模块 07 |
| `--filesystem` | 共享目录（virtiofs/9p） | 需 guest 驱动 |
| `--memorybacking` | 大页/共享内存 | 模块 08 |
| `--cputune` / `--numatune` | 绑核/NUMA | 模块 08 |
| `--noautoconsole` | 不自动打开控制台 | 脚本化常用 |
| `--autostart` | 随宿主启动 | 等价 `virsh autostart` |
| `--dry-run` / `--print-xml` | 不真正创建 / 打印 XML | 学习 XML 的好帮手 |

> 学习技巧：先用 `--print-xml` 看 libvirt 生成的 XML，再对照
> `ps -ef | grep qemu` 看最终 QEMU 命令行，三层（参数 → XML → QEMU）就打通了。

### B. 直接写 XML 定义（推荐进阶后使用）

```bash
virsh dumpxml demo01 > ~/kvm/demo01.xml   # 导出当前定义
virsh edit demo01                          # 编辑（保存即重定义）
virsh define ~/kvm/demo01.xml              # 从文件定义

# 校验 XML 合法性
virt-xml-validate ~/kvm/demo01.xml domain
```

最小可用 XML 骨架：

```xml
<domain type='kvm'>
  <name>demo02</name>
  <memory unit='MiB'>2048</memory>
  <vcpu>2</vcpu>
  <os>
    <type arch='x86_64' machine='q35'>hvm</type>
    <boot dev='hd'/>
  </os>
  <devices>
    <disk type='file' device='disk'>
      <driver name='qemu' type='qcow2'/>
      <source file='/var/lib/libvirt/images/demo02.qcow2'/>
      <target dev='vda' bus='virtio'/>
    </disk>
    <interface type='network'>
      <source network='default'/>
      <model type='virtio'/>
    </interface>
    <console type='pty'><target type='serial'/></console>
  </devices>
</domain>
```

---

## 2.5 libvirt 默认网络

`virsh net-list` 会看到一个 `default` 网络：**NAT 模式**，网段通常是 `192.168.122.0/24`。

- 宿主机上的 `virbr0` 是它的网关；
- guest 通过 DHCP 拿到 `192.168.122.x`；
- guest 能访问外网，外网默认**不能**主动访问 guest（NAT 特性）。

```bash
virsh net-list --all
virsh net-dumpxml default
ip addr show virbr0
```

数据通路：

```text
 guest eth0 (192.168.122.10)
      | virtio-net
      v
 宿主机 tap 设备 --桥接--> virbr0 (192.168.122.1)
                              |  dnsmasq（DHCP/DNS）
                              |  NAT（iptables/nftables MASQUERADE）
                              v
                          物理网卡 --> 外网
```

排障要点：

```bash
# 网桥与 tap 是否存在
ip -br link show type bridge
ip -br link show type tun

# dnsmasq 是否在监听
ps -ef | grep '[d]nsmasq'
ss -lunp | grep :53

# NAT 规则
sudo nft list ruleset | grep -i masquerade   # 或 sudo iptables -t nat -L -n
```

---

## 2.6 用 cloud 镜像 + cloud-init 免安装

Cloud 镜像自带 cloud-init，可以用 `virt-install --cloud-init` 一步到位：

```bash
# 从镜像导入前，先把下载的 qcow2 放到目标位置
sudo cp ~/kvm/images/Fedora-Cloud-Base-44-1.1.x86_64.qcow2 /var/lib/libvirt/images/cloud01.qcow2

sudo virt-install \
  --name cloud01 \
  --memory 2048 --vcpus 2 --cpu host-passthrough \
  --disk path=/var/lib/libvirt/images/cloud01.qcow2,size=10,format=qcow2,bus=virtio \
  --import \
  --cloud-init root-password-generate=on,user-name=fedora \
  --network network=default,model=virtio \
  --graphics none --console pty,target_type=serial \
  --os-variant fedora-unknown
```

### cloud-init 原理

cloud-init 是 guest 里的一个初始化服务，启动时从"数据源（datasource）"读取配置，
按阶段执行。libvirt 的 `--cloud-init` 走的是 **NoCloud** 数据源：
生成一个小 ISO（卷标 `cidata`），里面放三个文件，挂给 guest 当光驱。

```text
 libvirt / virt-install
   |  生成 seed ISO（卷标 cidata）
   |   +- user-data      用户/密码/SSH key/软件包/runcmd
   |   +- meta-data      instance-id、hostname
   |   +- network-config 网络配置（可选）
   v
 seed ISO --作为 CD-ROM 挂给 guest-->
        guest 内 cloud-init
           | 1. 探测数据源（NoCloud：卷标 cidata）
           | 2. local 阶段：网络、磁盘
           | 3. init 阶段：user-data（用户、密钥、包、runcmd）
           | 4. config/final 阶段：收尾、上报
           v
        配置完成，可直接 ssh
```

| `--cloud-init` 选项 | 作用 |
| --- | --- |
| `root-password-generate=on` | 随机生成 root 密码并打印 |
| `root-password=...` | 指定 root 密码（不推荐明文） |
| `user-name=...` | 创建用户 |
| `ssh-key=...` | 注入 SSH 公钥（生产首选） |
| `user-data=/path/user-data.yaml` | 自定义 user-data |
| `meta-data=...` | 自定义 meta-data |
| `disable=on` | 关闭默认行为 |

自定义 user-data 示例（按 YAML 语法）：

```yaml
#cloud-config
hostname: cloud01
users:
  - name: fedora
    sudo: ALL=(ALL) NOPASSWD:ALL
    ssh_authorized_keys:
      - ssh-ed25519 AAAA... you@laptop
package_update: true
packages:
  - qemu-guest-agent
runcmd:
  - systemctl enable --now qemu-guest-agent
```

```bash
# 用自定义 user-data 起机
sudo virt-install --name cloud02 --memory 2048 --vcpus 2 \
  --disk path=/var/lib/libvirt/images/cloud02.qcow2,size=10,format=qcow2,bus=virtio \
  --import --network network=default,model=virtio \
  --graphics none --console pty,target_type=serial \
  --cloud-init user-data=./user-data.yaml
```

> 排障：进不去 guest 时，在 guest 里看 `cloud-init status --long`、
> `/var/log/cloud-init.log`、`/run/cloud-init/`；宿主机侧确认 seed ISO 已挂载
> （`virsh domblklist cloud01`）。

---

## 动手实验

**实验 2-1：跑通第一台 VM**
按 2.3 用 QEMU 起一台，再按 2.4 用 virt-install 起一台，对比体验。

**实验 2-2：观察真实 QEMU 命令行**

```bash
ps -ef | grep '[q]emu-system' | tr ' ' '\n' | grep -v '^$'
```
对照 `virsh dumpxml demo01`，找出 XML 与 QEMU 参数的对应关系。

**实验 2-3：guest 与宿主机连通性**

```bash
virsh domifaddr demo01              # 查 guest IP
ping <guest-ip>
ssh <user>@<guest-ip>
```

**实验 2-4：用 `--print-xml` 学习 XML**

```bash
virt-install --name dry01 --memory 1024 --vcpus 1 \
  --disk size=5 --import --print-xml | tee /tmp/dry01.xml
# 观察 libvirt 如何把命令行翻译成 XML，再对照 --dry-run
```

**实验 2-5：对比 modular 与单体 daemon 的连接**

```bash
systemctl is-active virtqemud.socket libvirtd.socket
virsh -c qemu:///system uri
virsh -c qemu:///session uri
```

---

## 排错指南

| 现象 | 根因 | 定位命令 | 处理 |
| --- | --- | --- | --- |
| `-enable-kvm: KVM not available` | 无 `/dev/kvm` 权限或模块没加载 | `ls -l /dev/kvm; lsmod \| grep kvm; id` | 加入 `kvm` 组重登；`modprobe kvm_intel` |
| `virsh` 连不上 | daemon 没起或 socket 未激活 | `systemctl status virtqemud.socket` | `systemctl enable --now virtqemud.socket` |
| 两套 daemon 冲突 | `libvirtd` 与 `virtqemud` 抢 socket | `systemctl --failed; journalctl -u virtqemud` | 二选一，停用另一套 |
| 访问 `/var/lib/libvirt/images` 被拒 | 目录权限或 SELinux 标签不对 | `ls -ldZ /var/lib/libvirt/images; ausearch -m avc -ts recent` | `restorecon -Rv` 或 `semanage fcontext` |
| 自定义目录放镜像起不来 | 目录不在 `virt_image_t` 上下文 | `ls -Z /data/vms` | `semanage fcontext -a -t virt_image_t '/data/vms(/.*)?'` 后 `restorecon -Rv /data/vms` |
| guest 拿不到 IP | virbr0 没起 / DHCP 未响应 | `virsh net-list; ip -br a show virbr0; ps -ef \| grep dnsmasq` | `virsh net-start default; virsh net-autostart default` |
| `virsh console` 卡住 | guest 未配串口控制台 | `virsh dumpxml demo01 \| grep -A2 console` | XML 加 pty 串口 + guest 内核 `console=ttyS0,115200` |
| cloud 镜像起不来 / 无账号 | cloud-init 数据源未识别 | `virsh domblklist cloud01`；进 guest 看日志 | 确认 seed ISO 卷标为 `cidata`；检查 user-data 语法 |
| `virt-install` 报 unknown os-variant | libosinfo 数据缺失 | `osinfo-query os \| grep fedora` | `dnf install libosinfo`；改用 `fedora-unknown` |
| `Could not open '/dev/kvm'` | 权限/嵌套/容器缺设备 | `systemd-detect-virt; ls -l /dev/kvm` | 见模块 01 案例 1；容器加 `--device /dev/kvm` |
| ISO 下载 404 | 版本号/路径变了 | `curl -I URL` | 去 mirror 查实际文件名，或换 Alpine |
| 磁盘格式探测警告 | 未显式指定 `format` | `qemu-img info disk1.qcow2` | 命令/XML 显式写 `format=qcow2` |
| 启动后黑屏无显示 | `--graphics none` 但无串口 | `virsh domdisplay demo01` | 加 `--console pty,target_type=serial` |
| 防火墙挡住 guest 出网 | firewalld 未放行 virbr0 | `firewall-cmd --zone=libvirt --list-all` | `firewall-cmd --zone=libvirt --add-service=dhcp` 或重载 |
| `qemu-system-x86_64: command not found` | 没装 `qemu-kvm` | `rpm -q qemu-kvm` | `dnf install qemu-kvm` |

### 案例复盘

**案例 1：`Permission denied` 其实不是权限问题。**
某工程师把镜像放到 `/data/vms`，`chmod 777` 了还是起不来。
根因是 SELinux 上下文——目录不是 `virt_image_t`，`virtqemud` 被 AVC 拒绝。
定位：`ausearch -m avc -ts recent | grep virt`，或 `ls -Z /data/vms`。
处理：`semanage fcontext -a -t virt_image_t '/data/vms(/.*)?'` 再 `restorecon -Rv`。
教训：**libvirt 报权限错误时，先分清是 Unix DAC 还是 SELinux。**

**案例 2：`virsh console` 一直空白。**
guest 起来了、能 ping 通，就是 `virsh console` 卡住。根因是 guest 内核
没有把控制台输出到 ttyS0。定位：`virsh dumpxml` 确认有 pty 串口，
进 guest 看 `/proc/cmdline` 有没有 `console=ttyS0`。
处理：用 `virt-customize -a disk.qcow2 --append-line` 改 grub 参数，
或 cloud-init 里配，然后更新 grub。
教训：**pty 设备只是宿主机侧；guest 内核还得往它写。**

**案例 3：装完 libvirt 却一个 daemon 都没起。**
`dnf install` 只装文件，不会自动 enable。Fedora 上又分 modular/单体两套。
定位：`systemctl list-unit-files 'virt*.socket'`，`systemctl status virtqemud`。
处理：按 2.1 启用 modular sockets；注意别和 `libvirtd.socket` 同开。
教训：**"装好了"和"跑起来了"是两回事。**

**案例 4：cloud 镜像导入后 root 登不进。**
`--import` 起机，控制台提示登录但不知道密码。根因是没传 cloud-init 数据，
或用了默认但没注意 `root-password-generate=on` 打印的随机密码。
定位：重跑时看 virt-install 输出；进 guest 看 `cloud-init status --long`。
处理：改用 `--cloud-init root-password=...,user-name=fedora` 或注入 SSH key。
教训：**cloud 镜像默认无密码，必须有数据源。**

---

## 练习

1. 用纯 QEMU 命令起一台带 **两张** virtio 网卡 + **两块**磁盘的 VM。
2. 导出 demo01 的 XML，改成 4 vCPU / 4GB 内存后 `define`，验证生效。
3. 解释 `virsh shutdown` 与 `virsh destroy` 的本质区别。
4. 用 `-blockdev` 重写 2.3 的磁盘部分，说明与 `-drive` 的差异。
5. 写一份 user-data，创建用户、注入 SSH key、安装并启用 `qemu-guest-agent`。

## 延伸阅读

- `man qemu-system-x86_64`
- `man virt-install`、`man virsh`
- QEMU 参数：<https://www.qemu.org/docs/master/system/invocation.html>
- libvirt XML 格式参考：<https://libvirt.org/formatdomain.html>
- cloud-init 文档：<https://cloudinit.readthedocs.io/>
