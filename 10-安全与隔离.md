# 模块 10 · 安全与隔离

> 虚拟化的安全命题只有一句话：**guest 是不可信的，QEMU 是攻击面，宿主内核是可信基**。
> 这一模块把"边界画在哪、用什么机制守边界、怎么验证和排错"讲透。

## 学习目标

- 讲清 KVM 的安全模型与攻击面：为什么"guest 逃逸 = 攻破 QEMU 进程"，边界如何划分；
- 掌握 sVirt/SELinux 的标签机制（`svirt_t`/`svirt_image_t`、MCS category）与 AppArmor 的差异；
- 理解 cgroup/namespace/能力（capability）/seccomp 如何把 QEMU 关进笼子；
- 掌握镜像加密（LUKS、qcow2 LUKS、libvirt secret）、UEFI Secure Boot、vTPM/swtpm 的原理与配置；
- 认识侧信道（Spectre/Meltdown/L1TF/MDS/Retbleed/MMIO）与 KSM 的风险，并会做缓解取舍；
- 会读 SELinux 审计日志与 libvirt/QEMU 日志，按现象定位安全类故障；
- 知道机密计算（SEV/TDX）的定位，能写出一份可落地的加固与审计清单。

---

## 10.1 安全模型与攻击面

KVM 的安全边界本质上是 **QEMU 进程边界**：guest 逃逸 = 攻破 QEMU 进程。
原因在于设备模型：

- 为了兼容，QEMU 在用户态**用 C 代码模拟真实硬件**（`hw/` 目录下的网卡、磁盘控制器、
  显卡、USB、串口、软驱等）。guest 可以通过 MMIO/PIO/DMA 直接驱动这些模拟设备，
  于是 guest 完全控制了一条进入 QEMU 地址空间的输入通道。
- 模拟代码解析 guest 提供的结构体、长度、描述符。一旦校验不严，就是内存破坏漏洞。
  历史上大量 "guest escape" CVE 都出在这里：软驱 VENOM（CVE-2015-3456）、
  SLiRP 网络栈、USB 与 UAS、以及各类 SCSI/显卡模拟。
- 结论：**攻击面大小 ≈ guest 能触达的模拟设备数量**。这也是后面所有"减少设备、
  优先 virtio"建议的根本理由。

反过来说，哪些东西**不在**这个边界内：

- KVM 内核模块本身（`arch/x86/kvm/`、`virt/kvm/`）运行在内核态、可信基内，不经过它做模拟；
- `vhost`/`vfio` 把数据面下沉到内核或硬件，**绕过了 QEMU 用户态解析**，相对更小；
- 但 `vhost` 在内核态处理 guest 内存，一旦有漏洞影响面更大 —— 所以它属于内核可信基，
  要跟内核一起打补丁。

```text
        ┌─────────────────────── guest（不可信）───────────────────────┐
        │  应用 / guest 内核                                            │
        └───────────────┬──────────────────────────────────────────────┘
                        │ 指令、MMIO、PIO、DMA、virtqueue
        ┌───────────────▼──────────────────────────────────────────────┐
        │  QEMU 用户态进程  ← 主要攻击面                                │
        │  ├─ 设备模型：网卡/磁盘/显卡/USB/串口（C 代码，历史漏洞多）   │
        │  ├─ 迁移、QMP/HMP 监视器、图形后端                            │
        │  └─ 主循环 + vCPU 线程                                        │
        └───────────────┬──────────────────────────────────────────────┘
                        │ ioctl(/dev/kvm)、系统调用、文件
        ┌───────────────▼──────────────────────────────────────────────┐
        │  KVM 内核模块 + 宿主内核（可信基）                            │
        │  └─ vhost / vfio / iommu / sVirt 策略 / cgroup / seccomp      │
        └───────────────┬──────────────────────────────────────────────┘
                        ▼
                 宿主硬件 / 其他 VM
```

因此防护是**分层纵深**的，任何一层都不能单独兜底：

1. **进程隔离**：用非 root 用户、cgroup、namespace 限制 QEMU；
2. **强制访问控制（MAC）**：sVirt（SELinux/AppArmor）给每个 VM 打唯一标签，
   禁止跨 VM 访问——即使 QEMU 被攻破也无法横向移动；
3. **系统调用与能力收敛**：seccomp 过滤 syscall，capability 白名单；
4. **硬件/固件**：IOMMU 隔离、Secure Boot、vTPM、内存加密（SEV/TDX）；
5. **运维层**：及时打补丁、最小化设备、网络分段、加密静态数据。

> 心智模型：安全不是"能不能逃逸"的单一开关，而是"逃逸之后还能走多远"。
> sVirt 保证逃逸的 QEMU 只能碰到自己那台 VM 的资源；cgroup 保证它耗尽不了宿主；
> IOMMU 保证它 DMA 不到别的内存。

## 10.2 sVirt 与 SELinux

sVirt 把每个 QEMU 进程和它的磁盘文件打上**唯一标签**，用 SELinux 类型强制（TE）
+ MCS 类别实现隔离。核心是两类标签：

| 标签 | 打给谁 | 作用 |
| --- | --- | --- |
| `svirt_t` | KVM 加速的 QEMU 进程 | 受限域，只能访问被标记的资源 |
| `svirt_tcg_t` | 纯软件模拟（TCG）的 QEMU | 同上，TCG 专用域 |
| `svirt_image_t` | VM 磁盘镜像文件 | 只有持同类标签的 QEMU 能读写 |
| `svirt_tmp_t` | QEMU 临时文件 | 隔离的临时区 |

**MCS（Multi-Category Security）** 是关键：SELinux 有 c0–c1023 共 1024 个类别，
libvirt 的 SELinux 安全驱动（`src/security/security_selinux.c`，核心函数类似
`virSecuritySELinuxGenSecurityLabel`）给每台 VM 随机分配**一对**类别，例如
`c123,c456`。进程和它的所有镜像共享同一对类别；类别不重合的两个域，
即使类型都是 `svirt_t`，也互相访问不了。这就是"逃逸后无法横向攻击其他 VM"的实现方式。

标签长这样，三段含义是 `user:role:type:level`：

```bash
# 查看 SELinux 状态
getenforce
sestatus

# 查看 VM 进程标签
ps -eZ | grep qemu
# system_u:system_r:svirt_t:s0:c123,c456 ...

# 查看镜像文件标签
ls -Z /var/lib/libvirt/images/*.qcow2
# system_u:object_r:svirt_image_t:s0:c123,c456
```

**为什么自定义目录容易出事**：SELinux 按路径（`file_contexts`）分配初始标签。
`/var/lib/libvirt/images` 被预定义为镜像目录，放这里的文件 `restorecon` 后
自动是 `svirt_image_t`。但你自建的 `/data/vms` 没有被定义，文件会是默认的
`default_t` 或 `usr_t`，与 `svirt_t` 不匹配 → AVC 拒绝 → VM 起不来。

```bash
# 给自定义目录打标签（否则 VM 起不来）
sudo semanage fcontext -a -t svirt_image_t "/data/vms(/.*)?"
sudo restorecon -Rv /data/vms
```

相关布尔开关（按需打开，不要全开）：

```bash
# 让 VM 访问 NFS/Samba 上的镜像
sudo setsebool -P virt_use_nfs on
sudo setsebool -P virt_use_samba on

# 查看所有 virt_* 布尔
sudo semanage boolean -l | grep virt_
```

> 常见故障：把镜像放到 `/home` 或自建目录，SELinux 拦截导致 VM 无法启动。
> 解决：正确打标签，或（不推荐）`setenforce 0`。
> 调试阶段可临时用 `setenforce 0` 确认是 SELinux 问题，**确认后必须恢复 `setenforce 1`**。

安全驱动是 libvirt 域启动流程的一环：`virtqemud` 在启动 QEMU 前依次调用
DAC（文件属主）、SELinux/AppArmor（标签）、cgroup 三个安全驱动，创建资源时生成标签，
销毁时回收。理解这条链，排错时就知道"标签是 libvirt 生成并 `setfilecon` 上去的"，
而不是你手动改的。

## 10.3 AppArmor（Debian/Ubuntu）

Fedora 用 SELinux，Debian/Ubuntu 用 AppArmor。AppArmor 是**基于路径**的 MAC，
比 SELinux 简单，但没有 MCS 那样的动态类别隔离。

libvirt 会为每个 VM 生成 `/etc/apparmor.d/libvirt/libvirt-<uuid>.files`，
限制 QEMU 可访问的路径；基础策略在 `/etc/apparmor.d/abstractions/libvirt-qemu`
和 `/etc/apparmor.d/usr.sbin.libvirtd`。

```bash
sudo aa-status | grep libvirt
sudo cat /etc/apparmor.d/libvirt/libvirt-*.files
```

```text
# libvirt-<uuid>.files 大致内容（示意）
  /var/lib/libvirt/images/disk.qcow2 rw,
  /var/lib/libvirt/qemu/domain-1-web01/* rw,
  /dev/kvm rw,
```

| 维度 | SELinux（RHEL/Fedora） | AppArmor（Debian/Ubuntu） |
| --- | --- | --- |
| 匹配方式 | 标签（inode 属性） | 路径 |
| 隔离粒度 | 类型 + MCS 类别，天然多租户 | 路径白名单，无类别 |
| 改路径是否要重标 | 要，`semanage`+`restorecon` | 相对宽松，profile 里允许即可 |
| 调试命令 | `ausearch -m avc` / `audit2allow` | `dmesg` / `journalctl -k` / `aa-status` |

## 10.4 cgroup、namespace 与沙箱

MAC 管"能碰哪些对象"，cgroup/namespace 管"能用多少资源、能看到什么视图"。

**cgroup（资源与设备白名单）**：libvirt 把每台 VM 放进一个 systemd scope，
位于 `machine.slice` 下，名字形如 `machine-qemu\x2d1\x2dweb01.scope`：

```bash
systemctl status 'machine-qemu\x2d1\x2dweb01.scope'
cat /sys/fs/cgroup/machine.slice/machine-qemu\\x2d1\\x2dweb01.scope/memory.max
```

cgroup v2 下常用控制项：`cpu.weight`/`cpu.max`、`memory.max`/`memory.high`、
`io.max`、`pids.max`。libvirt 还有 **devices 白名单**：QEMU 在私有 `/dev` 视图里
只能看到被允许的设备（`/dev/kvm`、`/dev/net/tun`、指定的 VFIO 设备等）。
相关配置在 `/etc/libvirt/qemu.conf` 的 `cgroup_controllers`、`cgroup_device_acl`。

**namespace（视图隔离）**：libvirt 为 QEMU 建立私有 mount namespace，
挂载受限的 `/dev`、隐藏宿主的敏感路径；结合净 namespace 让 VM 的网络栈独立。

**seccomp 沙箱**：QEMU 支持 `-sandbox`，libvirt 默认开启 seccomp 过滤，
只放行设备模型需要的一小撮 syscall。配置项是 `qemu.conf` 的 `seccomp_sandbox`。

```bash
# 看 QEMU 当前实际收到的沙箱参数（libvirt 翻译后的真实命令行）
ps -ef | grep '[q]emu-system' | tr ' ' '\n' | grep -i sandbox
```

## 10.5 进程权限与能力收敛

即使有 MAC，也应让 QEMU 以**最小权限**运行：

- Fedora 上 libvirt 的 QEMU 默认以 `qemu` 用户（组 `qemu`）运行，非 root；
- libvirt 启动 QEMU 前用 libcap-ng **清空并仅保留**必要 capability；
  相关开关是 `qemu.conf` 的 `clear_emulator_capabilities`（默认 1，别关）；
- `dynamic_ownership`（默认 1）让 libvirt 按需调整镜像文件属主，避免给 QEMU
  一个"能读所有文件"的 wide-open 权限。

```bash
# 确认 QEMU 跑在非特权用户、cap 已收敛
ps -o user,pid,comm -C qemu-system-x86_64
grep -E 'Cap(Inh|Prm|Eff|Bnd)' /proc/$(pgrep -f qemu-system-x86_64 | head -1)/status
# CapEff 应为很小的集合，而不是 000001ffffffffff（全能力）
```

> 会话模式（`qemu:///session`）用当前用户跑 VM，没有 `virtqemud` 的 MAC 与能力管理，
> 隔离更弱，但适合个人开发。生产用 `qemu:///system`。

## 10.6 磁盘加密

静态数据加密解决"磁盘被拿走/宿主机运维能看到明文"的问题。两种粒度：

| 方案 | 加密层 | 宿主能看到明文吗 | 特点 |
| --- | --- | --- | --- |
| LUKS 加密块设备（dm-crypt） | 宿主机块层 | 映射期间能看到 | 对 QEMU 透明、任何镜像格式都能用 |
| qcow2 内置 LUKS | QEMU 块层 | 宿主看到密文 | 文件级、便于搬迁副本；旧 AES 格式已废弃 |

**方案 A：LUKS 加密块设备**

```bash
sudo cryptsetup luksFormat /dev/vg0/vm-secret
sudo cryptsetup open /dev/vg0/vm-secret vm-secret
# 把 /dev/mapper/vm-secret 作为 guest 磁盘
```

**方案 B：qcow2 内置加密（LUKS 格式）**

```bash
qemu-img create -f qcow2 --object secret,id=sec0,data=passphrase \
  -o encrypt.format=luks,encrypt.key-secret=sec0 disk-enc.qcow2 20G
```

> 事实纠正/提醒：qcow2 曾有一个 `encrypt.format=aes`（AES-CBC + 自己派的密钥）
> 的老格式，密钥派生弱、已被弃用。**一律用 `encrypt.format=luks`**（AES-256-XTS）。

libvirt 通过 **secret 对象**管理口令，而不是把口令写进 XML：

```xml
<disk type='file' device='disk'>
  <driver name='qemu' type='qcow2'/>
  <source file='/data/disk-enc.qcow2'/>
  <encryption format='luks'>
    <secret type='passphrase' uuid='...'/>
  </encryption>
</disk>
```

```bash
# 定义并设置 secret（口令走 secret driver，不落在域 XML 里）
cat > secret.xml <<'EOF'
<secret ephemeral='no' private='yes'>
  <description>disk-enc passphrase</description>
</secret>
EOF
virsh secret-define secret.xml
virsh secret-set-value --interactive <uuid>
virsh secret-list
```

`virsh secret-set-value` 的值由 libvirt 的 secret driver（`virtsecretd`）保存，
默认在 `/etc/libvirt/secrets/` 下，权限 0600、仅 root 可读。**排错提示**：
迁移或换宿主机后如果没预置同一个 secret，加密盘会打不开——这是常见事故点。

## 10.7 UEFI Secure Boot

Secure Boot 防的是"guest 的 bootloader/内核被植入"：UEFI 固件在启动链每一跳
校验签名，未签名或签名不在 `db` 里的组件直接拒绝执行。虚拟机里由 OVMF 提供固件。

签名链与 UEFI 变量：`PK`（Platform Key）→ `KEK`（Key Exchange Key）→ `db`（允许）/ `dbx`（吊销）。

```xml
<os firmware='efi'>
  <type arch='x86_64' machine='q35'>hvm</type>
  <loader secure='yes'/>
  <nvram template='/usr/share/OVMF/OVMF_VARS.secboot.fd'/>
</os>
```

```bash
# Fedora 上需要 OVMF
sudo dnf install -y edk2-ovmf
# 固件路径（不同版本可能是 /usr/share/edk2/ovmf/）
ls /usr/share/OVMF/ | grep -i secboot
# guest 内验证
mokutil --sb-state
```

说明：

- `<os firmware='efi'>` 让 libvirt 自动选择固件（firmware autoselection），
  `<loader secure='yes'/>` 表示要求启用 Secure Boot 的固件；
- `OVMF_VARS.secboot.fd` 是**变量模板**，每台 VM 会复制成独立的 NVRAM，
  变量（含你自己的 `db`/`MOK` 密钥）存在这份副本里；
- guest 若要加载自签名模块，用 `mokutil --import <pubkey.der>`，重启进 MOK 管理器登记。

| 场景 | 现象 | 说明 |
| --- | --- | --- |
| 用 raw Code 而未用 secboot 版 | `mokutil --sb-state` 显示 disabled | 固件根本没开校验 |
| 用了自编译内核/驱动 | 启动被拒绝 | 需签名或 `mokutil --import` |
| Windows 11 | 强制要求 | Secure Boot + vTPM 2.0 都得开 |

## 10.8 vTPM 与 swtpm

vTPM 给 guest 一个 TPM 2.0，用于 BitLocker、密钥密封（sealing）、measured boot 和远程证明。
QEMU 用 **swtpm** 这个用户态进程模拟 TPM；libvirt 通过 `<tpm>` 设备接入。

```xml
<devices>
  <tpm model='tpm-crb'>
    <backend type='emulator' version='2.0' persistent_state='yes'/>
  </tpm>
</devices>
```

```bash
sudo dnf install -y swtpm swtpm-tools
# guest 内
dmesg | grep -i tpm
tpm2_getrandom 8
tpm2_pcrread
```

原理要点：

- `tpm-crb`（Command Response Buffer）是 TPM 2.0 推荐的接口；老的 `tpm-tis` 也用，
  但新平台优先 CRB；
- 状态持久化在 `/var/lib/libvirt/swtpm/<uuid>/`，`persistent_state='yes'` 才能在
  重启后保留密钥/NVRAM（否则每次重置，BitLocker 会要求恢复密钥）；
- swtpm 进程以 `swtpm` 用户运行，SELinux 域是 `swtpm_t`，libvirt 负责启动与授权；
- **measured boot**：PCR 0–7 记录固件/bootloader 的度量值，远程方可读取并验证
  guest 是否被篡改——这是机密计算里"证明"的基础。

## 10.9 侧信道与缓解

CPU 侧信道是**跨信任域**的信息泄漏：同一物理核上的两个上下文（可能分属两个租户）
通过共享的微架构结构互相窥探。虚拟化放大了风险，因为 SMT 让不同 VM 共享物理核。

| 攻击 | CVE | 原理 | 缓解 |
| --- | --- | --- | --- |
| Spectre V1/V2 | 2017-5753 / 2017-5715 | 推测执行越界读取、分支目标注入 | 微码 + 内核缓解、`spectre_v2` |
| Meltdown | 2017-5754 | 乱序执行绕过权限读内核映射 | KPTI（`pti=on`） |
| L1TF | 2018-3646 | L1 数据缓存 + 页表项侧信道 | `l1tf=full,force` + 关 SMT |
| MDS | 2018-12126/27/30、2019-11091 | 微架构填充缓冲泄漏 | 微码 + `mds=full,nosmt` |
| TAA | 2019-11135 | TSX 异步中止泄漏 | `tsx_async_abort=full` |
| SRBDS | 2020-0543 | 特殊寄存器缓冲采样 | 微码 + `srbds=` |
| Retbleed | 2022-29900 / 2022-29901 | 返回指令预测被滥用 | `retbleed=auto` + IBRS/IBPB |
| BHI | 2022-0001 / 2022-0002 | 分支历史注入 | 内核缓解 + 微码 |
| MMIO 陈旧数据 | 2022-21123/25/66 | VM entry/exit 未清 MMIO 缓冲 | `mmio_stale_data=full` |
| Downfall | 2022-40982 | Gather 指令泄露向量寄存器 | `gds=mitigate` |

```bash
# 查看当前漏洞与缓解状态（宿主与 guest 都要看）
grep . /sys/devices/system/cpu/vulnerabilities/*

# 关闭 KSM（敏感场景）
echo 0 | sudo tee /sys/kernel/mm/ksm/run

# 核心调度缓解 SMT 侧信道：内核参数 sched_core=1
# 也可直接关闭 SMT（简单粗暴但会损失算力）
cat /sys/devices/system/cpu/smt/control
```

**虚拟化相关的额外细节**：

- **VM-Exit 是"冲刷点"也不总是**：L1TF 的虚拟化缓解要求 VM entry 时清 L1，
  并让 guest 无法通过 `L1D_FLUSH` 猜测宿主；内核参数 `l1tf=full,force` 中的
  `force` 表示即使有 SMT 也强制上缓解（性能代价大）。
- **MMIO 类**：因为 hypervisor 会在 VM entry/exit 时复用微架构缓冲，
  同一物理核上跨 VM 可能读到残留，默认 `mitigations=auto` 会处理。
- **核心调度（core scheduling）**：让同一物理核上 SMT 的两个超线程**只跑同一信任组**
  的 vCPU，从而在保留 SMT 的同时阻断跨租户侧信道。内核参数 `sched_core=1`，
  进程侧用 `prctl(PR_SCHED_CORE_CREATE)` 打上 cookie，同 cookie 才允许同核。
- **取舍**：缓解几乎都降性能。单租户/受信负载可以适度放宽（`mitigations=off`），
  多租户云必须默认全开，并把"关 SMT"作为兜底。

## 10.10 KSM 风险与内存侧信道

KSM（Kernel Same-page Merging）扫描并合并内容相同的物理页，省内存，但代价是
**合并本身就是一个信息通道**：攻击者通过测量自己页的写入延迟，能判断某段内存
是否与其他 VM 共享，进而推断对方内容（Dedup Est Machina 类攻击）。

```bash
# 观察 KSM 状态
cat /sys/kernel/mm/ksm/run             # 1=开
grep -E 'pages_shared|pages_sharing|pages_volatile|full_scans' \
  /sys/kernel/mm/ksm/*
```

实践建议：

- 多租户或高安全场景：`run=0`，并禁用 `ksmtuned`；也可设 `merge_across_nodes=0`
  避免跨 NUMA 节点合并带来的额外延迟与信息泄漏面；
- 单租户、内存超分吃紧：可开 KSM，但要接受侧信道风险与 CPU 开销；
- 与内存相关的高性能场景，用 HugePages/low-latency 反而会**阻止** KSM 合并（大页不合并）。

## 10.11 IOMMU 与设备隔离

直通（VFIO）设备必须经 IOMMU 才能安全交给 guest：没有 IOMMU，设备 DMA 不受限，
一块被直通的网卡可以 DMA 到宿主或其他 VM 的任意内存——这是比软件漏洞更彻底的越界。

```bash
# 确认 IOMMU 已启用
dmesg | grep -i -E 'DMAR|IOMMU'
ls /sys/kernel/iommu_groups/
```

要点：

- Intel 平台内核参数 `intel_iommu=on iommu=pt`（`pt` = passthrough，减少非直通设备开销）；
- 设备按 **IOMMU group** 分组，同组设备只能整体直通；ACS（Access Control Services）
  决定分组是否干净，需要主板/固件支持；
- 没有 IOMMU 或分组不干净时，VFIO 直通要么失败，要么不安全。详见模块 07。

## 10.12 机密计算（预告）

前面所有机制都假设"宿主机可信、只防 guest 攻击宿主"。机密计算反过来：
**连 hypervisor 也不可信**，目标是把 guest 内存对宿主也加密。

| 技术 | 厂商 | 核心机制 |
| --- | --- | --- |
| AMD SEV | AMD | 每 VM 独立内存加密密钥 |
| AMD SEV-ES | AMD | 加密 guest 寄存器状态 |
| AMD SEV-SNP | AMD | 增加完整性（RMP 反向映射表），防宿主篡改 |
| Intel TDX | Intel | Trust Domain，TDX Module 管理硬件隔离的 VM |

- 密钥由硬件安全处理器管理（AMD 的 PSP / Intel 的 TDX Module），宿主拿不到；
- 与 vTPM 配合可做**远程证明**：证明"这台 VM 跑在真正的机密硬件上且未被篡改"；
- 适用：多租户云、合规要求"运行中数据对云厂商也不可见"的场景；
- 代价：功能受限（部分设备/迁移能力受限）、性能有开销、生态仍在演进。
- 具体命令与 XML 见模块 12。

## 10.13 其他加固清单

按"减少攻击面 + 缩小权限 + 及时更新"三条主线组织：

**减少攻击面**

- 禁用不必要的设备（USB、串口、图形、软驱、声卡），能删就删；
- 磁盘/网卡优先 `virtio`（半虚拟化面积小），少用 `e1000`/`IDE` 等全模拟设备；
- 最小化 guest 镜像（少装服务、少开端口）；
- 关闭不需要的 QEMU 监视器入口，QMP socket 权限收紧。

**缩小权限**

- 用 `qemu:///system`（受 `virtqemud` 管理），非 root 用户跑 QEMU；
- 保持 `clear_emulator_capabilities=1`、`seccomp_sandbox` 开启；
- 限制 QEMU 的 cgroup 资源（CPU/内存/IO），防止 DoS；
- 只给直通设备是必要的，且确认 IOMMU 分组干净。

**周边与流程**

- 定期更新 QEMU/libvirt/内核/微码（漏洞多在这些组件）；
- 网络层做隔离（VLAN、安全组、私有 bridge/OVS）；
- 镜像与备份静态加密（LUKS）；
- 生产镜像启用 Secure Boot + vTPM；
- 建立补丁与变更流程，安全事件有审计可追溯。

## 10.14 审计与合规

安全机制只有在"能证明它生效"时才有意义，所以要会看审计日志、做合规扫描。

```bash
# SELinux 拒绝审计
sudo ausearch -m AVC,USER_AVC -ts recent
sudo aureport -a | head

# 给 libvirt 配置目录加审计规则
sudo auditctl -w /etc/libvirt -p wa -k libvirt_conf
sudo auditctl -w /var/lib/libvirt/images -p wa -k libvirt_images

# 合规基线扫描（OpenSCAP + SCAP Security Guide）
sudo dnf install -y openscap-scanner scap-security-guide
oscap xccdf eval --profile xccdf_org.ssgproject.content_profile_cis \
  --results /tmp/kvm-cis.xml \
  /usr/share/xml/scap/ssg/content/ssg-fedora-ds.xml
```

合规要点：

- **等保 2.0 / PCI-DSS / CIS**：都要求访问控制、审计留痕、最小权限、数据加密——
  本模块的 sVirt/cgroup/LUKS/审计正好逐条对应；
- **审计日志**统一进 `auditd`，`/var/log/audit/audit.log` 要防篡改、可上报；
- **完整性**：只读基线可用 `dm-verity`，文件级可用 IMA/EVM；measured boot 的
  PCR 值可作证明输入；
- 定期用 `virsh domcapabilities`、`lscpu`、内核漏洞状态页做资产与风险盘点。

## 10.15 案例复盘

**案例 A：换了镜像目录，VM 直接起不来**

- 现象：把磁盘从 `/var/lib/libvirt/images` 挪到自建 `/data/vms`，改完 XML 后
  `virsh start` 报 permission denied，QEMU 日志里也是打不开磁盘。
- 定位：`ausearch -m avc -ts recent` 看到 `svirt_t` 访问 `default_t` 被拒；
  `ls -Z /data/vms/*.qcow2` 显示标签不是 `svirt_image_t`。
- 根因：SELinux 按路径（`file_contexts`）给文件初始打标，`/data/vms` 未定义，
  文件是 `default_t`，与 QEMU 进程的 `svirt_t` 不匹配。
- 处理：`semanage fcontext -a -t svirt_image_t "/data/vms(/.*)?"` +
  `restorecon -Rv /data/vms`，无需重启即可恢复。
- 教训：**换路径先想 SELinux**。用 `setenforce 0` 只是把问题藏起来，
  等于拆掉一整层隔离。

**案例 B：加密盘迁移到新宿主后打不开**

- 现象：qcow2 LUKS 加密盘拷到另一台宿主机（或热迁移后）guest 起不来，
  日志报 decrypt / secret 相关失败。
- 定位：`virsh secret-list` 在源/目标对比，`/var/log/libvirt/qemu/<name>.log` 看具体报错。
- 根因：加密盘的密钥来自 libvirt secret（或 key file），**secret 不会跟着磁盘走**，
  目标宿主没有同名 secret。
- 处理：在目标端 `virsh secret-define` + `virsh secret-set-value`，或统一改用
  外部 key file 并预置。
- 教训：加密盘的"配置"包含 secret，迁移/备份清单里必须带上它，并单独保管密钥。

**案例 C：vTPM 每次重启都重置**

- 现象：guest 内用 vTPM 密封的密钥、BitLocker 等，一重启就失效。
- 定位：`virsh dumpxml <name> | grep -A3 tpm`，看 backend 是否带 `persistent_state`。
- 根因：未开 `persistent_state='yes'`，swtpm 用临时状态目录，VM 停止即清空。
- 处理：加 `persistent_state='yes'`，确认 `/var/lib/libvirt/swtpm/` 属主与 SELinux 标签。
- 教训：vTPM 不是"加上就完事"，持久化与状态目录备份同样要规划。

**案例 D：多租户环境收到侧信道告警**

- 现象：安全扫描指出同物理核跨租户运行（SMT），或担心 KSM 去重带来的侧信道风险。
- 定位：`grep . /sys/devices/system/cpu/vulnerabilities/*`、
  `cat /sys/kernel/mm/ksm/run`、`virsh dumpxml | grep -i cputune`。
- 根因：SMT 让不同 vCPU 共享物理核；KSM 跨 VM 合并相同内存页，暴露访问模式。
- 处理：敏感场景关 KSM（`echo 0 > /sys/kernel/mm/ksm/run`）、按租户绑定物理核或
  关 SMT，必要时用核心调度（`sched_core=1`）。
- 教训：KSM、超分、SMT 这类"性能手段"本质上都是**隔离与密度的取舍**，
  要按威胁模型逐场景决定，而不是默认全开。

---

## 动手实验

**实验 10-1：观察 sVirt 标签与 MCS 隔离**

```bash
ps -eZ | grep qemu
ls -Z /var/lib/libvirt/images/
# 起两台 VM，对比两台的 category 是否不同
ps -eZ | grep qemu | grep svirt
```

**实验 10-2：制造一次 SELinux 拦截并修复**

```bash
mkdir -p /data/vms && mv /var/lib/libvirt/images/*.qcow2 /data/vms/
# 改 XML 指向新路径，尝试启动 → 失败
sudo ausearch -m avc -ts recent | tail
# 正确打标签后恢复
sudo semanage fcontext -a -t svirt_image_t "/data/vms(/.*)?"
sudo restorecon -Rv /data/vms
```

**实验 10-3：启用 vTPM 并验证持久化**

装 swtpm，按 10.8 加 TPM，进 guest 验证 `tpm2_getrandom`。
记下 PCR 值，重启 VM 后再读一次，确认 `persistent_state='yes'` 生效。

**实验 10-4：加密盘透明读写**

按 10.6 方案 B 创建加密 qcow2，用 libvirt secret 接入，进 guest 读写验证透明加密；
在宿主用 `qemu-img info` 确认 `encrypted: true`，直接 `strings` 镜像确认看不到明文。

**实验 10-5：观察沙箱与能力收敛**

```bash
ps -ef | grep '[q]emu-system' | tr ' ' '\n' | grep -i sandbox
grep -E 'CapEff|CapBnd' /proc/$(pgrep -f qemu-system-x86_64 | head -1)/status
# 对比：把 qemu.conf 的 clear_emulator_capabilities 关掉再起，看差异
```

**实验 10-6：核对侧信道缓解状态**

```bash
grep . /sys/devices/system/cpu/vulnerabilities/*
cat /sys/kernel/mm/ksm/run
```

---

## 排错指南

| 现象 | 根因 | 定位命令 | 处理 |
| --- | --- | --- | --- |
| VM 启动被拒，报 permission denied | SELinux 标签不匹配（目录未标 `svirt_image_t`） | `ausearch -m avc -ts recent`、`ls -Z`、`virsh start --debug` | `semanage fcontext -a -t svirt_image_t "..."; restorecon -Rv` |
| AppArmor deny，QEMU 被 profile 挡 | 路径不在白名单 | `aa-status`、`dmesg \| grep -i apparmor` | 调整 `libvirt-<uuid>.files` 或改用标准路径 |
| Secure Boot 下 guest 无法启动 | bootloader/内核未签名，或 `db` 无对应密钥 | guest 内 `mokutil --sb-state`、`dmesg \| grep -i 'secure boot'` | 换签名镜像、`mokutil --import`，或临时关 Secure Boot |
| `mokutil` 显示 Secure Boot disabled | 固件用了非 secboot 版 OVMF | `virsh dumpxml \| grep -i loader`、`ls /usr/share/OVMF` | 改用 `OVMF_VARS.secboot.fd` 与 `secure='yes'` |
| vTPM 起不来 / QEMU 报错 | 缺 `swtpm` 或状态目录权限/标签不对 | `journalctl -u virtqemud`、`ls -lZ /var/lib/libvirt/swtpm/` | 装 `swtpm`，修属主与 SELinux 标签 |
| 每次重启 guest 都丢 TPM 密钥 | `persistent_state` 未开 | `virsh dumpxml <name> \| grep -A2 tpm` | 设 `persistent_state='yes'` 并重建 |
| 加密盘打不开，报 decrypt 失败 | libvirt secret 未定义或口令错 | `virsh secret-list`、`/var/log/libvirt/qemu/<name>.log` | `virsh secret-define/-set-value` 重设 |
| 换宿主机后加密盘失效 | 新宿主没有同名 secret | `virsh secret-list` | 迁移前在目标端预置 secret 或改用 key file |
| KSM 开启后内存抖动 / 疑侧信道 | 跨 VM 页去重 | `cat /sys/kernel/mm/ksm/pages_sharing` | 敏感场景 `echo 0 > /sys/kernel/mm/ksm/run` |
| 直通设备报 IOMMU fault / 被拒 | VT-d 未开或 ACS 分组不干净 | `dmesg \| grep -i DMAR`、`ls /sys/kernel/iommu_groups/` | 开 `intel_iommu=on iommu=pt`，换直通组合 |
| 多租户下 CPU 侧信道告警 | SMT 同核跨租户 | `grep . /sys/devices/system/cpu/vulnerabilities/*` | `l1tf=full,force`/`mds=full,nosmt`，或 `sched_core=1`/关 SMT |
| QEMU 进程能力过大 | `clear_emulator_capabilities=0` | `grep -i clear_emulator /etc/libvirt/qemu.conf`、`grep CapEff /proc/<pid>/status` | 恢复默认 1，重启 `virtqemud` 与 VM |
| 热插盘后 guest 看不到 | 动态属主/标签未更新 | `ls -lZ`、`ausearch -m avc` | 保持 `dynamic_ownership=1`，正确打标签 |
| audit 日志被 AVC 刷屏 | 策略未收敛、路径不标准 | `aureport -a`、`ausearch -m avc \| audit2allow` | 优先打标签；确需放行再评估生成策略 |
| SEV/TDX VM 起不来 | 平台/固件/内核不支持 | `dmesg \| grep -iE 'sev\|tdx'`、`virsh domcapabilities` | 检查 BIOS、内核与 QEMU 版本 |

---

## 练习

1. 解释 sVirt 如何防止"一个 VM 逃逸后横向攻击其他 VM"。MCS category 在其中扮演什么角色？
2. 什么时候应该关闭 KSM？为什么？如果既要省内存又要抗侧信道，有什么折中？
3. 对比 SELinux 与 AppArmor 在虚拟化场景下的隔离能力，说明各自适用环境。
4. 设计一个满足等保/合规要求的 VM 安全加固清单，并给出每条的验证命令。
5. 画图说明一次"guest 通过模拟网卡触发 QEMU 漏洞"可能影响到的资源边界，
   以及 sVirt/cgroup/IOMMU 各自拦在哪一层。
6. 说明 Secure Boot 的 `PK/KEK/db/dbx` 链，以及 guest 自签名驱动如何通过 `MOK` 登记。

## 延伸阅读

- SELinux 与虚拟化：`man virt_selinux`、`man 8 virtqemud_selinux`
- libvirt 安全驱动：<https://libvirt.org/drvsecurity.html>
- libvirt secret 与加密：<https://libvirt.org/formatsecret.html>、<https://libvirt.org/kbase/secureboot.html>
- QEMU 沙箱与安全：<https://www.qemu.org/docs/master/system/security.html>
- 内核漏洞缓解：`Documentation/admin-guide/hw-vuln/`（含 l1tf、mds、mmio_stale_data 等）
- Intel TDX / AMD SEV 官方文档
