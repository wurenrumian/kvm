# 模块 09 · 迁移与高可用

> 迁移是虚拟化"值钱"的地方：硬件维护、负载均衡、故障规避都靠它。
> 但迁移成功 ≠ 业务无感——停机时间、网络、存储、CPU 兼容任何一环出问题，
> 业务都会感知。本模块讲清原理、参数、监控、回滚和 HA。

## 学习目标

- 理解热迁移（live migration）的完整流程与前置条件；
- 吃透 pre-copy 的迭代与脏页跟踪、收敛条件、停机时间，以及 post-copy、
  multifd、压缩、自动收敛各自的适用场景；
- 区分共享存储、`copy-storage-all/inc`、`blockcopy` 三种存储迁移路径；
- 掌握 `virsh migrate` 的关键参数、迁移监控与失败回滚；
- 会设计 HA 方案：Pacemaker + corosync、fencing/STONITH、防脑裂，
  以及 Proxmox / oVirt / OpenStack / KubeVirt 的差异；
- 能做故障恢复与演练。

---

## 9.1 迁移的类型与适用场景

| 类型 | 说明 | 停机时间 | 前提 |
| --- | --- | --- | --- |
| **冷迁移** | 关机后迁移 | 分钟级 | 无 |
| **热迁移（live）** | 运行中迁移，业务几乎无感 | 毫秒~秒级 | CPU/存储/网络兼容 |
| **在线存储迁移** | 磁盘也一起搬 | 视数据量 | 额外带宽 |
| **离线存储迁移** | 关机搬盘 | 停机 | 无 |
| **疏散（evacuation）** | 故障/维护时批量迁走 | 视策略 | 编排层支持 |

同构迁移（同型号 CPU/同架构）最简单；**异构迁移**（跨 CPU 代际）需要
CPU 基线，跨架构（x86→ARM）则只能冷迁移 + 转换。

---

## 9.2 热迁移原理：pre-copy / post-copy / multifd

### pre-copy：迭代复制

QEMU 侧实现在 `migration/migration.c`、`migration/ram.c`，
核心结构是 `MigrationState`。流程：

```text
1. 目标机创建空 VM（接收方），建立迁移通道
2. 第一轮：全量复制所有内存页（bulk phase）
3. guest 继续运行，期间被写过的页变"脏"，记录在 dirty bitmap
4. 下一轮：只复制脏页；同时 guest 继续产生新的脏页
5. 重复迭代，直到脏页量足够小（收敛）
6. 暂停源 guest（stop-and-copy），复制最后一批脏页 + 设备状态
7. 目标机接管，源机销毁
```

**脏页跟踪**：KVM 通过内存槽的 dirty logging 记录写操作
（`KVM_SET_USER_MEMORY_REGION` 开 logging，`KVM_GET_DIRTY_LOG` 取脏页）。
较新的 `KVM_CAP_DIRTY_LOG_RING` 用脏页环替代位图，在高脏页率下开销更低。

**收敛条件**：脏页产生速率 < 迁移带宽。若 guest 是内存密集写负载，
脏页永远追不上，就会一直迭代 → 迁移不收敛。

**停机时间（downtime）** = 最后一批脏页传输时间 + 设备状态传输时间。
目标机的 `downtime-limit` 决定何时强制进入停机阶段。

```text
带宽 ▲
     │        收敛区（脏页率 < 带宽）
     │   ████████████
     │   ████  ← 每轮要传的脏页量逐轮下降
     │   ██
     │   █
     └──────────────────► 迭代次数
     若这条曲线不下降 → 不收敛 → 需要 auto-converge / post-copy
```

### 加速与收敛手段

| 手段 | 原理 | 适用 | 代价 |
| --- | --- | --- | --- |
| **auto-converge** | 自动节流 guest vCPU，降低脏页率 | 写密集、要保 pre-copy | guest 变慢 |
| **xbzrle** | XOR + 缓存，只传差异 | 页变化小、熵低 | 高熵负载无效 |
| **压缩（compress）** | 传输前压缩页 | 带宽紧张 | CPU 开销 |
| **multifd** | 多 TCP 通道并行传页 | 高带宽链路 | 需两端支持 |
| **零页检测** | 跳过全零页 | 稀疏内存 | 一般自动 |
| **限速（max-bandwidth）** | 控制迁移占用带宽 | 保护业务网络 | 迁移变慢 |

```xml
<!-- libvirt 迁移调优（也可用 virsh migrate --migrate-* 覆盖） -->
<migration>
  <bandwidth>
    <postcopy/>
  </bandwidth>
  <downtime-limit>300</downtime-limit>   <!-- ms -->
</migration>
```

### post-copy：先跑起来，缺页再拉

pre-copy 收敛不了时的替代方案：**先暂停源机、立即在目标机运行**，
guest 访问尚未传输的页时触发缺页，由目标机向源机按需拉取
（Linux 侧用 `userfaultfd` 机制）。

- 优点：停机时间极短且**与内存大小无关**；
- 缺点：**源机必须存活**直到所有页拉完，否则 VM 丢失；
  缺页拉取会带来运行期延迟抖动。

```text
pre-copy:  传输(长) → 停机(短) → 目标运行
post-copy: 传输(短) → 停机(极短) → 目标运行 + 按需拉页(源机须存活)
```

可在 pre-copy 迭代若干轮后再切换 post-copy（`postcopy-after-precopy`），
兼顾两者。

### multifd 与压缩的取舍

- multifd 在两端各起多个线程/通道，页按通道并行发送，充分利用高带宽；
- 压缩（`compress`）省带宽但吃 CPU，适合**带宽受限**而非 CPU 受限的场景；
- 二者可组合，但要评估两端 CPU 余量；
- 开启前后用 `virsh domjobinfo` 的 bandwidth 字段对比实际效果。

---

## 9.3 前置条件与 CPU 兼容性

### 前置检查清单

1. **网络互通**：源/目标能互相访问 libvirt，迁移端口放行
   （libvirt 默认 49152–49215，QEMU 直连端口可自定义）；
2. **CPU 特性兼容**：跨型号必须用兼容模型；
3. **存储**：共享存储，或启用块迁移；
4. **目标资源**：足够的内存/CPU/磁盘；
5. **时钟同步**：chrony，避免迁移后时钟漂移；
6. **软件版本**：QEMU/libvirt/内核尽量一致；
7. **设备**：直通设备不可迁移，需先热拔。

```bash
# 检查 CPU 兼容性（跨主机迁移前）
virsh domcapabilities --machine q35 --virttype kvm > /tmp/domcaps-a.xml
virsh capabilities > /tmp/caps-a.xml
# 在目标机也导出 capabilities 到 /tmp/caps-b.xml，然后计算兼容基线：
virsh cpu-baseline /tmp/caps-a.xml /tmp/caps-b.xml > /tmp/baseline.xml
# 校验某台 VM 的 CPU 定义能否被当前宿主接受：
virsh cpu-compare /etc/libvirt/qemu/web01.xml
```

### CPU 模型怎么选

| 模型 | 迁移性 | 说明 |
| --- | --- | --- |
| `host-passthrough` | 差 | 仅同型号可迁；透传全部特性 |
| `host-model` | 较好 | libvirt 按宿主生成，可迁移 |
| `custom` | 最好 | 显式基线，跨代兼容 |
| `maximum` | 差 | 全特性，基本不可迁 |

```xml
<!-- 跨代兼容的显式基线示例 -->
<cpu mode='custom' match='exact' check='full'>
  <model fallback='forbid'>Skylake-Client</model>
  <feature policy='require' name='ssse3'/>
  <feature policy='require' name='sse4.2'/>
  <feature policy='disable' name='hle'/>
</cpu>
```

注意点：

- **`invtsc` 不可迁移**（TSC 频率随 CPU 变），跨机迁移不要强开；
- 同一 `machine` 类型（如 `q35` 的具体版本）两端要对齐；
- 嵌套虚拟化特性（`vmx`/`svm`）会影响兼容性；
- `check='full'` 会在启动时校验，避免"能迁但跑不起来"。

---

## 9.4 存储迁移

### 共享存储（最快）

磁盘放在 NFS / Ceph / iSCSI 上，两台宿主都能访问，迁移时**只传内存和设备状态**。

```text
[源宿主] ──内存+状态──► [目标宿主]
     \                      /
      └── 同一共享存储 ─────┘
```

优点：快、简单；缺点：需要共享存储基础设施，且共享存储本身是单点
（需 HA 存储，如 Ceph）。

### 非共享存储：块迁移

```bash
# 全量：把磁盘完整复制到目标
virsh migrate --live --copy-storage-all web01 qemu+ssh://target/system

# 增量：只复制变化的块（要求两端有相同的基础镜像）
virsh migrate --live --copy-storage-inc web01 qemu+ssh://target/system

# 只迁移指定磁盘
virsh migrate --live --copy-storage-all --migrate-disks vdb web01 \
  qemu+ssh://target/system
```

### blockcopy：先同步磁盘再迁内存

底层是 QEMU 的 `blockdev-mirror`，适合"存储先迁、内存后迁"的解耦：

```bash
# 把 vda 镜像到共享存储上的新文件
virsh blockcopy web01 vda /shared/web01.qcow2 --wait --verbose
# 完成后切换到新盘
virsh blockcopy web01 vda /shared/web01.qcow2 --finish --pivot
```

相关命令：`blockpull`（把 backing 链合并进顶层）、`blockcommit`
（把增量合并回基础镜像），常用于清理快照链。

```text
blockcopy:  源盘 ──mirror──► 目标盘（后台同步，完成后 pivot）
blockpull:  backing 链 ──► 合并到活动层（向上合并）
blockcommit:活动层 ──► 合并回基础镜像（向下合并）
```

---

## 9.5 迁移命令与参数详解

```bash
# 基本热迁移（需目标机 libvirt 可访问）
virsh migrate --live web01 qemu+ssh://target-host/system

# 全量块迁移 + 压缩 + 自动收敛
virsh migrate --live --copy-storage-all --compressed --auto-converge \
  web01 qemu+ssh://target/system

# post-copy（收敛困难时）
virsh migrate --live --postcopy --postcopy-after-precopy \
  web01 qemu+ssh://target/system

# 指定带宽与停机上限
virsh migrate --live --bandwidth 2000 --timeout 300 \
  web01 qemu+ssh://target/system

# 多通道并行（multifd）
virsh migrate --live --parallel --parallel-connections 4 \
  web01 qemu+ssh://target/system

# 迁移并保留源机定义（便于回滚）
virsh migrate --live --persistent --undefinesource web01 \
  qemu+ssh://target/system

# 查看迁移能力与进度
virsh migrate --help
virsh domjobinfo web01
```

| 参数 | 作用 |
| --- | --- |
| `--live` | 运行中迁移 |
| `--p2p` | peer-to-peer，libvirt 直连 libvirt |
| `--tunnelled` | 迁移数据走 libvirt 连接（防火墙友好，较慢） |
| `--compressed` | 压缩传输 |
| `--auto-converge` | 自动节流以收敛 |
| `--postcopy` / `--postcopy-after-precopy` | 启用 post-copy |
| `--parallel` | multifd 多通道 |
| `--copy-storage-all/inc` | 全量/增量块迁移 |
| `--migrate-disks` | 指定要迁的盘 |
| `--bandwidth` | 带宽上限（MiB/s） |
| `--timeout` | 迁移超时（秒） |
| `--persistent` / `--undefinesource` | 保留定义/删源定义 |
| `--migrateuri` | 显式指定迁移 URI |

> `--tunnelled` 与 `--p2p` 的差别：前者把数据塞进 libvirt 的加密连接里，
> 穿越防火墙方便但吞吐低；后者 libvirt 之间控制、QEMU 之间直连数据面，
> 性能更好但需要放行更多端口。

---

## 9.6 迁移监控、回滚与调优

### 监控

```bash
# 迁移中：查看进度、剩余内存、带宽、迭代轮次
virsh domjobinfo web01
# 关键字段：
#   Data processed / Data remaining / Data total
#   Memory processed / Memory remaining / Memory total
#   Dirty rate / Iteration / Expected downtime

# 直接看 QEMU monitor（调试用）
virsh qemu-monitor-command web01 --hmp 'info migrate'
virsh qemu-monitor-command web01 --hmp 'info migrate_parameters'
```

### 失败回滚

```bash
# 中止正在进行的迁移
virsh domjobabort web01

# 源机上的 VM 若已被暂停，恢复运行
virsh resume web01
```

要点：

- pre-copy 在**停机阶段之前**失败，源机 VM 仍在运行，通常无需回滚；
- 失败多发生在目标机接管阶段，此时要确认哪一端是"活的"，避免双跑；
- `--persistent` 保留源机定义，便于失败后重新拉起；
- 迁移前对关键 VM 做快照/备份，是最后的兜底。

### 常见调优方向

| 症状 | 调整 |
| --- | --- |
| 不收敛 | `--auto-converge`，或 `--postcopy-after-precopy` |
| 带宽打满影响业务 | `--bandwidth` 限速 |
| 带宽没用满 | `--parallel`/multifd、`--compressed` |
| 停机时间偏长 | 降 `downtime-limit`，先让脏页收敛 |
| CPU 吃紧 | 关压缩，改限速 |

---

## 9.7 高可用（HA）体系

KVM 本身**不带 HA**，需要上层编排。HA 的目标是在宿主机故障时，
把 VM 在另一台机器上自动拉起，使服务尽快恢复。

### 核心概念

- **RTO / RPO**：恢复时间目标 / 数据丢失容忍。HA 降低 RTO，备份降低 RPO。
- **Quorum（法定票数）**：集群多数派存活才能做决策，防止脑裂。
- **Fencing / STONITH**：Shoot The Other Node In The Head——在接管资源前，
  先把故障节点**强制断电/隔离**，确保它不再访问共享存储。
- **Watchdog**：节点自身失联时自动重启，配合 SBD 实现自 fencing。
- **脑裂（split-brain）**：两个节点都认为自己是主，同时操作共享存储，
  可能损坏数据。**防脑裂 = quorum + fencing + watchdog**。

```text
正常：  [Node A 主] ──心跳── [Node B 备] ──共享存储──
故障：  [Node A ✗]  ──心跳断── [Node B] → 先 fencing A，再接管
        若没有 fencing：A 可能还活着 → 双写共享存储 → 脑裂
```

### Pacemaker + corosync

- **corosync**：集群成员与消息层（totem 协议），负责心跳；
- **pacemaker**：集群资源管理器（CRM），决定资源在哪跑；
- **资源代理（RA）**：`ocf:heartbeat:VirtualDomain` 管理 libvirt VM，
  `ocf:heartbeat:Filesystem` 挂载存储等；
- **fencing 代理**：`fence_ipmilan`、`fence_redfish`、`fence_ilo` 等。

```bash
sudo dnf install -y pacemaker corosync pcs fence-agents-all
sudo systemctl enable --now pcsd
sudo passwd hacluster
sudo pcs host auth node1 node2 -u hacluster
sudo pcs cluster setup mycluster node1 node2
sudo pcs cluster start --all
sudo pcs cluster enable --all

# 配置 fencing（务必先验证！）
sudo pcs stonith create fence1 fence_ipmilan \
  ipaddr=10.0.0.10 login=admin passwd=xxx lanplus=1
sudo stonith_admin --list-installed

# 定义 VM 资源
sudo pcs resource create web01 ocf:heartbeat:VirtualDomain \
  config=/etc/libvirt/qemu/web01.xml \
  migration_transport=ssh \
  meta allow-migrate=true

sudo pcs status
sudo corosync-quorumtool
```

要点：**fencing 必须实际验证有效**，否则 HA 切换会在"无法确认旧节点已死"
时卡住，资源起不来。两节点集群需配 `two_node: 1` 和
`wait_for_all`，并谨慎设置 `no-quorum-policy`。

### 各平台 HA 对比

| 方案 | 机制 | 适用 | 备注 |
| --- | --- | --- | --- |
| **Pacemaker + corosync** | 心跳 + RA + fencing | 传统小集群 | 通用、需自己搭 |
| **Proxmox VE HA** | `pve-ha-crm` + LRM + watchdog | 中小规模 | 内置、易用 |
| **oVirt / RHV** | engine HA + 电源管理 fencing | 大型企业 | 功能全、重 |
| **OpenStack** | nova-compute HA + 实例疏散（masakari） | 云平台 | 面向大规模 |
| **KubeVirt** | K8s 调度 + Pod 自愈 + LiveMigrate | 云原生 | 与 K8s 生态融合 |

### Proxmox / oVirt / OpenStack / KubeVirt 要点

- **Proxmox**：`ha-manager` 管理 HA 资源，LRM 在节点上执行，节点故障时
  由 CRM 重新调度；依赖 watchdog 做自 fencing。
- **oVirt**：engine 是大脑，通过电源管理（IPMI/Redfish）fencing 主机，
  `ovirt-hosted-engine-ha` 还能让 engine 自身高可用。
- **OpenStack**：`masakari` 做实例 HA，配合 `reserved_host_memory`、
  host aggregates；实例疏散依赖共享存储和网络。
- **KubeVirt**：VM 跑在 `virt-launcher` Pod 里；节点故障时 K8s 重新调度
  Pod；`evictionStrategy: LiveMigrate` 让驱逐走热迁移；存储需 **RWX**
  （如 CephFS）才能多节点访问。

```yaml
# KubeVirt：启用驱逐时热迁移
apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: web01
spec:
  runStrategy: Always
  template:
    spec:
      evictionStrategy: LiveMigrate
      domain:
        devices: {}
```

---

## 9.8 故障恢复与演练

### 故障恢复

```bash
# 共享存储上的 VM，在另一台宿主机直接拉起
virsh start web01

# 若定义丢失，从备份恢复
virsh define /backup/web01.xml
```

恢复顺序建议：**先确认 fencing 已完成（旧节点确实死了）→ 挂载/确认存储
→ 检查网络 → 再启动 VM**。跳过 fencing 直接启动是脑裂和数据损坏的根源。

### 备份与演练

```bash
# 导出定义（轻量，必做）
virsh dumpxml web01 > /backup/web01.xml
virsh dumpxml --migratable web01 > /backup/web01-migratable.xml

# 镜像快照/备份
virsh snapshot-create-as web01 snap1 --disk-only --atomic
# 或用 qemu-img / 备份工具做增量
```

演练清单：

1. 主动 `poweroff`/断网一个节点，观察 HA 是否按预期切换；
2. 记录 **RTO**（从故障到业务恢复的时间）；
3. 验证 fencing 是否真的切断了旧节点（看 IPMI 日志）；
4. 验证存储一致性（`fsck`/数据库校验）；
5. 恢复节点，验证能重新加入集群（`pcs status` 干净）。

```bash
# 演练时观察
sudo pcs status
sudo corosync-quorumtool -s
journalctl -u pacemaker -u corosync -b | tail
```

### 监控与告警

- 集群：`pcs status`、`corosync-cfgtool -s`、`corosync-quorumtool`；
- VM：`virsh domstats`、`virt-top`、Prometheus `libvirt_exporter`；
- 存储：Ceph `ceph -s`、NFS 挂载健康；
- 网络：心跳链路延迟/丢包单独监控。

---

## 动手实验

**实验 9-1：本机 SSH 热迁移**

用 `qemu+ssh://localhost/system` 模拟迁移到自身，观察流程
（需允许 root SSH 或配好用户级连接）。

```bash
virsh migrate --live --verbose web01 qemu+ssh://localhost/system
```

**实验 9-2：观察迁移进度**

```bash
virsh migrate --live --verbose web01 qemu+ssh://target/system &
virsh domjobinfo web01
```

**实验 9-3：跨主机迁移（两台机器/两台 VM 模拟）**

配置 SSH 免密 + libvirt 远程访问，完成一次真实迁移，记录停机时间。

**实验 9-4：模拟迁移失败**

故意在写压力下迁移，观察 pre-copy 收敛困难，改用 `--postcopy` 或
`--auto-converge`，对比结果。

**实验 9-5：CPU 基线计算**

```bash
virsh capabilities > /tmp/caps-a.xml   # 在 A 机
virsh capabilities > /tmp/caps-b.xml   # 在 B 机
virsh cpu-baseline /tmp/caps-a.xml /tmp/caps-b.xml   # 计算兼容基线
virsh cpu-compare /etc/libvirt/qemu/web01.xml        # 校验现有 VM 定义
```

**实验 9-6（进阶）：Pacemaker 两节点 HA**

用两台 VM 模拟集群节点，配置 corosync + pacemaker + `VirtualDomain`，
fencing 可先用 `fence_dummy` 模拟，再换成真实电源管理。

---

## 排错指南

| 现象 | 根因 | 定位命令 | 处理 |
| --- | --- | --- | --- |
| `migrate: unsupported configuration` | CPU 特性不兼容 | `virsh cpu-compare`、`domcapabilities` | 改 `host-model`/自定义基线 |
| 迁移卡住不收敛 | 内存写压力大 | `virsh domjobinfo`（Dirty rate/Iteration） | `--auto-converge`、`--postcopy` |
| `Timed out during operation` | 带宽不足/网络慢 | `domjobinfo`、`iperf3` | 提带宽、`--parallel`、`--compressed` |
| 目标机 `no space left` | 存储不足 | `df -h`、`virsh vol-info` | 扩容或清理 |
| 迁移后网络不通 | 目标网络定义不一致 | `virsh domiflist`、`ip a` | 统一网桥/VLAN/防火墙 |
| `Cannot access storage file` | 共享存储未挂/权限 | `mount`、`ls -l` | 修挂载与权限 |
| `Unable to find security driver` | SELinux/AppArmor 标签 | `ausearch`、libvirtd 日志 | 调标签或策略 |
| 迁移后时钟漂移 | TSC/时间同步 | guest `dmesg`、`chronyc tracking` | 同步时间、避免强开 `invtsc` |
| post-copy 后 VM 挂 | 源机故障，页拉不全 | `dmesg`、migration 日志 | 保源机存活，或改 pre-copy |
| HA 不切换 | fencing 失败/quorum 丢失 | `pcs status`、`stonith_admin` | 修复 fencing 与 quorum |
| 双主/脑裂 | 无 quorum/watchdog | `pcs status`、`corosync-quorumtool` | 配 quorum + fencing + watchdog |
| `migration is active` | 前一次未结束 | `virsh domjobinfo` | `virsh domjobabort` 后重试 |
| 迁移速度慢 | 未启用并行/压缩 | `domjobinfo` 带宽字段 | `--parallel`、`--compressed` |
| 目标机报版本错误 | QEMU/libvirt 版本不一致 | 两端 `qemu-system-x86_64 --version` | 升级对齐版本 |
| KubeVirt 迁移 Pending | 存储非 RWX / 无 LiveMigrate | `kubectl describe vmim` | 配共享存储与驱逐策略 |

### 案例复盘 A：迁移迟迟不收敛

**现象**：一台数据库 VM 热迁移，进度条反复从 90% 回退，始终不完成。

**定位**：`virsh domjobinfo` 显示 `Dirty rate` 持续高于带宽，
`Iteration` 不断增加，`Memory remaining` 不下降。

**根因**：数据库持续高频写入，脏页产生速率超过迁移带宽，pre-copy
永远追不上。

**处理**：加 `--auto-converge` 节流 guest vCPU，脏页率下降后收敛；
若仍不行，用 `--postcopy-after-precopy`。代价是迁移期间 guest 性能下降，
应放在业务低峰执行。**教训：迁移窗口要和业务负载匹配**。

### 案例复盘 B：迁移成功，业务却报连接超时

**现象**：`virsh migrate` 返回成功，VM 在新宿主运行，但业务监控报大量
连接超时，几分钟后恢复。

**定位**：`virsh domiflist` 对比两端，发现目标机网桥没配对应的 VLAN；
`tcpdump` 看到 ARP 广播但无回应。

**根因**：只迁移了 VM，没保证**目标机网络配置一致**，迁移后网络短暂
不可达。

**处理**：统一两端的网桥/VLAN/防火墙；把网络检查加入迁移前置清单。
**结论：迁移前置检查要覆盖网络，而不只是 CPU 和存储**。

### 案例复盘 C：HA 切换失败，资源起不来

**现象**：节点 A 断电，节点 B 检测到故障，但 VM 迟迟没有在 B 上启动。

**定位**：`pcs status` 显示资源卡在 "starting"，日志报无法完成 fencing；
检查发现 IPMI 地址配错，fencing 一直失败。

**根因**：fencing 设备未验证有效，Pacemaker 出于安全**拒绝**在没有确认
旧节点已死的情况下接管共享资源。

**处理**：修正 IPMI 配置，用 `stonith_admin --reboot nodeA` 实际验证；
之后切换正常。**教训：fencing 必须演练验证，不能只配置不测试**。

### 案例复盘 D：跨代 CPU 迁移失败

**现象**：从老服务器迁到新服务器时报 CPU 不兼容，迁移中止。

**定位**：源 VM 用 `host-passthrough`，透传了老 CPU 没有、新 CPU 才有的
指令集特性，目标机校验不通过。

**根因**：`host-passthrough` 只适合同型号宿主，跨代不可迁。

**处理**：改用 `host-model` 或显式 `custom` 基线，并加
`check='full'` 提前暴露问题。**结论：要迁移就别用 passthrough**。

### 案例复盘 E：KubeVirt VM 迁移一直 Pending

**现象**：`kubectl` 触发迁移，VirtualMachineInstanceMigration 一直
Pending，`describe` 显示调度失败。

**定位**：`kubectl describe vmim` 报存储卷访问模式问题；PVC 是 RWO，
同一时间只能被一个节点挂载。

**根因**：热迁移要求目标节点能同时访问磁盘，RWO 不满足。

**处理**：改用 RWX 存储（如 CephFS/共享块），或配置支持多挂载的存储类。
**结论：KubeVirt 迁移的前提是共享存储**。

---

## 练习

1. 列出热迁移的完整前置检查清单（CPU/存储/网络/时钟/设备/版本）。
2. pre-copy 和 post-copy 各自的风险是什么？分别在什么场景选哪个？
3. 解释迁移"收敛条件"，并给出三种让不收敛变收敛的手段及其代价。
4. 设计一个"共享存储 + Pacemaker"的两节点 HA 方案，说明 fencing 的作用
   和脑裂如何避免。
5. `--copy-storage-all` 与 `--copy-storage-inc` 有什么区别？增量迁移的
   前提是什么？
6. 给一个 KubeVirt 上的 VM 设计"节点故障自动恢复 + 驱逐时热迁移"的方案，
   说明存储与策略要求。

## 延伸阅读

- libvirt 迁移文档：<https://libvirt.org/migration.html>
- QEMU live migration：<https://www.qemu.org/docs/master/devel/migration.html>
- QEMU 迁移源码：`migration/migration.c`、`migration/ram.c`
- KVM dirty ring：`Documentation/virt/kvm/`、`virt/kvm/dirty_ring.c`
- Pacemaker 文档：<https://clusterlabs.org/pacemaker/>
- Proxmox HA：<https://pve.proxmox.com/wiki/High_Availability>
- oVirt HA：<https://www.ovirt.org/documentation/>
- KubeVirt 迁移：<https://kubevirt.io/user-guide/operations/live_migration/>
