# 模块 06 · 网络虚拟化

> 虚拟网络是"能通就行"和"讲得清、调得动、排得出"之间差距最大的一层。
> 这一模块从 tun/tap 到 SR-IOV，把数据包到底怎么走讲清楚，再给一套排错方法。

## 学习目标

- 掌握 libvirt 虚拟网络的几种模式（NAT / 桥接 / 隔离 / 路由 / 直通）；
- 理解 tun/tap、veth pair、Linux bridge、macvtap 各自解决什么问题；
- 讲清 vhost-net 数据路径与多队列的工作方式；
- 会配置网桥、VLAN、Open vSwitch、SR-IOV VF；
- 能画出数据包路径，并用 `ip`/`bridge`/`tcpdump` 定位网络故障。

---

## 6.1 虚拟网络的四种基本模式

| 模式 | XML `type` | 特点 | 适用 |
| --- | --- | --- | --- |
| **NAT** | `network=default` | guest 可出网，外部不能主动入 | 开发、单机 |
| **桥接** | `bridge=br0` | guest 与宿主同网段，双向可达 | 生产、需被外部访问 |
| **隔离** | 自定义 network | 仅同网络内互通 | 内部集群 |
| **路由** | `forward mode='route'` | 三层转发 | 特殊拓扑 |
| **直通（macvtap）** | `type='direct'` | guest 直接挂物理网卡 | 高性能、无需网桥 |
| **SR-IOV VF** | `type='hostdev'` | 硬件虚拟化网卡 | 接近原生性能 |

```text
                    ┌─────────────── libvirt 网络模式 ───────────────┐
                    │                                                 │
  ┌──────────┐  ┌───┴────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
  │  NAT     │  │ 桥接    │  │ 隔离      │  │ 路由      │  │ macvtap  │ │
  │ virbr0   │  │ br0    │  │ virbr1   │  │ virbr2   │  │ 物理网卡  │ │
  │ +masq    │  │ +物理口 │  │ 仅内部    │  │ +转发规则 │  │ 直挂     │ │
  └──────────┘  └────────┘  └──────────┘  └──────────┘  └──────────┘ │
                    └─────────────────────────────────────────────────┘
```

选择原则：

- 需要被外部直接访问、且宿主与 guest 互通 → **桥接**；
- 只要 guest 能出网、图省事 → **NAT**；
- 多租户 / 集群内部 → **隔离** 或 **VLAN/OVS**；
- 追求网络性能、且不需要宿主与 guest 二层互通 → **macvtap**；
- 极致性能、能接受独占 VF → **SR-IOV**。

## 6.2 数据通路：从 guest 到物理网卡

```text
guest 内应用
   │ socket
guest 内核协议栈
   │ virtio-net 驱动 ── 共享内存 virtqueue ──┐
   ▼                                        │
QEMU 进程（设备模型）                          │
   │ tap 设备（/dev/net/tun）                 │
   ▼                                        │
vhost_net 内核线程（数据面下沉，可选）◄────────┘
   │
   ▼
Linux bridge (br0)  ◄── 物理网卡 (enp3s0) 挂上来
   │
   ▼
物理网络
```

关键组件：

- **tap 设备**：给 QEMU 用的虚拟二层网卡，一端在 QEMU，一端在宿主机内核。
  基于 `/dev/net/tun`，`TAP` 是二层、`TUN` 是三层。
- **veth pair**：一对虚拟网线，一端进 A 命名空间、一端进 B，常用于容器/netns。
  libvirt 桥接模式用 tap，不用 veth。
- **Linux bridge**：软件交换机，把 tap 和物理网卡桥到一起，靠 MAC 学习转发表（FDB）。
- **macvtap**：物理网卡的虚拟化接口，guest 直接挂上去，省掉网桥。

### 建立 tap 与 veth 的最小实验

```bash
# 手动建一个 tap（QEMU 用的就是这种）
sudo ip tuntap add dev tap-test mode tap
ip -d link show tap-test

# 手动建一对 veth，连接两个 netns
sudo ip netns add ns1
sudo ip netns add ns2
sudo ip link add veth1 type veth peer name veth2
sudo ip link set veth1 netns ns1
sudo ip link set veth2 netns ns2
sudo ip netns exec ns1 ip addr add 10.0.0.1/24 dev veth1
sudo ip netns exec ns2 ip addr add 10.0.0.2/24 dev veth2
sudo ip netns exec ns1 ping -c1 10.0.0.2
```

**vhost-net**：把 tap 的数据面从 QEMU 用户态移到内核 `vhost_net` 工作线程，
减少一次用户态/内核态切换和内存拷贝。libvirt 默认开启，XML 里表现为
`<driver name='vhost'/>`。

```text
无 vhost：guest ─► QEMU 用户态处理 ─► 内核 tap ─► bridge
有 vhost：guest ─► vhost_net 内核线程 ─► bridge（QEMU 只做控制面）
```

## 6.3 配置方式

### NAT（default 网络）

```bash
virsh net-dumpxml default
# 网段默认 192.168.122.0/24，网关是 virbr0
```

```xml
<interface type='network'>
  <source network='default'/>
  <model type='virtio'/>
</interface>
```

default 网络背后由三部分组成：

- **`virbr0`**：宿主机上的网桥，地址 `192.168.122.1/24`；
- **`dnsmasq`**：libvirt 为每个 NAT 网络起一个实例，提供 DHCP 和 DNS 转发；
- **防火墙规则**：对出网流量做 masquerade（源地址伪装）。新版本 libvirt 用
  nftables，老版本用 iptables；规则由 libvirt 按网络 XML 自动增删。

```bash
# 观察 NAT 的实现
ip -d link show virbr0
virsh net-dhcp-leases default
ps -ef | grep dnsmasq | grep virbr0
sudo nft list ruleset | grep -A5 virbr0     # 新系统
sudo iptables -t nat -L POSTROUTING -n      # 老系统
```

### 桥接

```bash
# 创建网桥（Fedora 用 NetworkManager）
sudo nmcli con add type bridge ifname br0 con-name br0
sudo nmcli con add type bridge-slave ifname enp3s0 master br0
sudo nmcli con up br0
```

```xml
<interface type='bridge'>
  <source bridge='br0'/>
  <model type='virtio'/>
</interface>
```

> 桥接需注意：物理网卡被桥接管后，宿主机 IP 应配到 br0 上。如果只把 enp3s0
> 挂进 br0 却把 IP 留在 enp3s0，宿主机就会断网。

### 隔离网络

```bash
virsh net-define <<'EOF'
<network>
  <name>isolated</name>
  <bridge name='virbr1' stp='on' delay='0'/>
  <ip address='10.10.0.1' netmask='255.255.255.0'>
    <dhcp><range start='10.10.0.10' end='10.10.0.100'/></dhcp>
  </ip>
</network>
EOF
virsh net-start isolated && virsh net-autostart isolated
```

> 隔离网络里，guest 之间可互通，也能到宿主 `10.10.0.1`，但没有到外部的转发，
> 因此访问不了外网。想放行特定流量可用 `net-update` 加转发规则。

### 路由模式

```xml
<network>
  <name>routed</name>
  <forward mode='route'/>
  <ip address='10.20.0.1' netmask='255.255.255.0'/>
</network>
```

> 路由模式下，宿主充当三层网关。外部要能访问 guest，需要在对端加静态路由指向
> 宿主机；guest 的出网则依赖宿主转发。比 NAT 更"干净"，但配置更麻烦。

### macvtap 直通

`type='direct'`，让 guest 直接挂到物理网卡，性能好，但宿主机与 guest
二层隔离（同网卡下宿主机和 guest 不能直接通信）。

```xml
<interface type='direct'>
  <source dev='enp3s0' mode='bridge'/>
  <model type='virtio'/>
</interface>
```

macvtap 的四种 mode：

| mode | 行为 | 宿主与 guest 互通 |
| --- | --- | --- |
| `bridge` | 像网桥一样直连外部网络 | 否（同物理网卡下） |
| `vepa` | 发往外部交换机再回环 | 否 |
| `private` | 同物理口 guest 之间也隔离 | 否 |
| `passthrough` | 把物理网卡直给 guest（单 guest） | 视情况 |

> 这是 macvtap 的**固有特性**，不是 bug：设计上它就是"绕过宿主网络栈"的。
> 需要宿主与 guest 通信就选 bridge，不要用 macvtap。

## 6.4 vhost-net 与性能相关

| 手段 | 说明 |
| --- | --- |
| virtio-net | 必选，替代 e1000 |
| vhost-net | 默认开启，确认 `<driver name='vhost'/>` |
| 多队列 | `queues=N`，让多个 vCPU 并行处理收包 |
| 巨帧 | MTU 9000，降低拷贝与分片开销 |
| XDP/eBPF | 宿主机侧加速 |
| SR-IOV VF 直通 | 接近原生性能（模块 07） |

多队列配置：

```xml
<interface type='network'>
  <source network='default'/>
  <model type='virtio'/>
  <driver name='vhost' queues='4'/>
</interface>
```

> queues 数量建议 ≤ vCPU 数；guest 内还需启用多队列（通常自动）。
> 验证：`ethtool -L eth0 combined 4`，宿主 `ls /sys/class/net/vnet0/queues/`
> 应看到 rx/tx 多个队列。

多队列原理：

```text
单队列：             多队列：
所有 vCPU ─► 一个       vCPU0 ─► queue0 ─► vhost 线程0
            virtqueue   vCPU1 ─► queue1 ─► vhost 线程1
                        vCPU2 ─► queue2 ─► vhost 线程2
（锁竞争、单核瓶颈）    （并行、缓存局部性好）
```

vhost-net 验证与调优：

```bash
lsmod | grep vhost
ls /sys/class/net/vnet0/queues/ 2>/dev/null
ethtool -k vnet0 | grep -i offload
# 宿主侧查看丢包/错误
ip -s link show vnet0
```

## 6.5 VLAN

```xml
<interface type='bridge'>
  <source bridge='br0'/>
  <vlan>
    <tag id='100'/>
  </vlan>
  <model type='virtio'/>
</interface>
```

或用 `<vlan><tag id='100' nativeMode='untagged'/></vlan>` 控制标记方式。

```text
带 VLAN 的数据包：
guest ─► tap（libvirt 打上 802.1Q tag 100）─► br0 ─► 物理交换机 trunk 口
                                                    │
                                        交换机按 tag 转发到 VLAN 100
```

也可以在网桥上用 VLAN filtering 做更细的控制：

```bash
sudo bridge vlan add dev enp3s0 vid 100
sudo bridge vlan show
```

> 排错关键：VLAN 不通要**两端一起查**——guest 侧 tag 配置、宿主网桥/交换机
> trunk 是否放行、对端是否在同一 VLAN。只查一头永远找不到。

## 6.6 Open vSwitch

OVS 提供更丰富的流表、VLAN、隧道（VXLAN/GRE），适合 SDN 场景。

```bash
sudo dnf install -y openvswitch
sudo systemctl enable --now openvswitch
sudo ovs-vsctl add-br ovsbr0
sudo ovs-vsctl add-port ovsbr0 enp3s0
```

```xml
<interface type='bridge'>
  <source bridge='ovsbr0'/>
  <virtualport type='openvswitch'/>
  <model type='virtio'/>
</interface>
```

常用操作与调试：

```bash
ovs-vsctl show
ovs-vsctl list-ports ovsbr0
ovs-ofctl dump-flows ovsbr0
ovs-appctl fdb/show ovsbr0
# 隧道示例：VXLAN 连接两台宿主
ovs-vsctl add-port ovsbr0 vxlan0 -- set interface vxlan0 \
  type=vxlan options:remote_ip=10.0.0.2
```

> OVS 的 `<virtualport type='openvswitch'/>` 里还能带 `interfaceid`/`profileid`，
> 供 OVN/OpenStack 标识端口，这是 SDN 编排的基础。

## 6.7 SR-IOV 简介

物理网卡（PF）虚拟出多个虚拟功能（VF），每个 VF 可直通给 guest，
绕过软件交换机，性能接近原生。详见模块 07。

```bash
# 查看网卡是否支持 SR-IOV
lspci | grep -i ethernet
cat /sys/class/net/enp3s0/device/sriov_numvfs 2>/dev/null
# 开启 VF（示例 4 个）
echo 4 | sudo tee /sys/class/net/enp3s0/device/sriov_numvfs
```

```bash
# 查看 VF 与设置 VF 的 MAC/VLAN
ip link show enp3s0
sudo ip link set dev enp3s0 vf 0 mac 52:54:00:11:22:33
sudo ip link set dev enp3s0 vf 0 vlan 100
```

```xml
<interface type='hostdev' managed='yes'>
  <source>
    <address type='pci' domain='0x0000' bus='0x03' slot='0x10' function='0x0'/>
  </source>
</interface>
```

> VF 直通需要 IOMMU（Intel VT-d）开启，宿主把 VF 从驱动解绑交给 VFIO。
> 让 SR-IOV 持久化要在 NetworkManager/udev 或 systemd 里设置 `sriov_numvfs`，
> 否则重启后 VF 消失。

## 6.8 数据包路径与 NAT 实现细节

以 default NAT 网络为例，guest 访问外网的完整路径：

```text
guest 应用
  │ 目标 8.8.8.8，源 192.168.122.50
  ▼
guest 路由表：默认网关 192.168.122.1
  │
  ▼ virtio-net → tap/vnet0
virbr0（宿主，192.168.122.1）
  │ 宿主路由：目标非本地，查默认路由
  ▼
netfilter POSTROUTING：MASQUERADE
  │ 源地址改写为宿主物理口地址（如 203.0.113.10）
  ▼
物理网卡 enp3s0 ─► 交换机 ─► 路由器 ─► 外网
```

回包：

```text
外网 ─► 宿主物理口
  │ conntrack 记录：此流属于 192.168.122.50 的连接
  ▼
netfilter 反向 NAT，目标改回 192.168.122.50
  ▼
virbr0 ─► vnet0 ─► guest
```

要点：

- NAT 依赖 **IP 转发**（`net.ipv4.ip_forward=1`）和 **masquerade** 规则；
- libvirt 为 default 网络自动加规则，但**手改防火墙或重启 firewalld 可能冲掉**；
- `dnsmasq` 负责给 guest 发 IP 和 DNS，guest 里 `resolv.conf` 指向 `192.168.122.1`；
- 端口转发（外部主动访问 guest）要用 `<forward>` 里的 `<nat><port>` 或
  `virsh net-update` 加 `forward` 规则。

```bash
# 检查转发与规则
sysctl net.ipv4.ip_forward
virsh net-dumpxml default | grep -A5 forward
sudo nft list chain inet libvirt_network ...   # 视 libvirt 版本
sudo conntrack -L | grep 192.168.122           # 看 NAT 会话
```

## 6.9 NetworkManager 与网桥管理

Fedora 用 NetworkManager 管网卡，libvirt 的桥接、VLAN、bond 都建议交给 NM。
关键原则：**一个连接、一个设备、一份 IP**。

```bash
# 查看设备与连接
nmcli device status
nmcli connection show

# 建网桥：先建 bridge，再把物理口作为 slave 加入
sudo nmcli connection add type bridge ifname br0 con-name br0 \
  ipv4.method manual ipv4.addresses 192.168.1.10/24 ipv4.gateway 192.168.1.1
sudo nmcli connection add type bridge-slave ifname enp3s0 con-name br0-slave master br0
sudo nmcli connection up br0

# 建 VLAN 接口
sudo nmcli connection add type vlan con-name vlan100 dev enp3s0 id 100 \
  ipv4.method manual ipv4.addresses 192.168.100.10/24
```

> 迁移宿主机网络配置时，常见事故是"IP 还挂在物理口上"。切换后确认
> `ip addr show br0` 有地址、`ip addr show enp3s0` 没有地址，再断开旧连接。

## 6.10 排错方法论

网络故障按层排查，别跳步：

```text
1. 设备层：tap/veth/网桥是否存在？    ip -d link show / bridge link show
2. 链路层：vnet0 是否 master 到 br0？ bridge link show / brctl show
3. 地址层：guest 有没有 IP？网关对不对？ ip a / ip r（guest 内）
4. 二层：ARP 通不通？                 arping / tcpdump -e arp
5. 三层：ping 网关、ping 外网          ping / traceroute
6. 策略：防火墙、转发、NAT 规则         nft/iptables、sysctl ip_forward
7. 性能：队列、offload、丢包            ethtool -S、ip -s link
```

```bash
# 抓包定位（宿主侧，看 guest 到底有没有发包）
sudo tcpdump -i vnet0 -nn -e
sudo tcpdump -i br0 -nn -e
# 对比两个接口，能快速判断丢在哪一段
```

---

## 动手实验

**实验 6-1：观察 NAT 网络的完整链路**

```bash
virsh net-dumpxml default
ip -d link show virbr0
ip -d link show | grep -A3 tap        # 找 guest 的 tap 设备
bridge link show                      # 或 brctl show
```

**实验 6-2：搭一个隔离网络并验证**

```bash
# 按 6.3 定义 isolated 网络，两台 guest 接入
# 验证：同网段互通，但访问不了外网
```

**实验 6-3：多队列压测**

用 `iperf3` 对比单队列 vs 4 队列的吞吐：

```bash
# guest 内
sudo dnf install -y iperf3
iperf3 -c <host-ip> -P 8
```

**实验 6-4：vhost-net 验证**

```bash
lsmod | grep vhost
# vhost_net 应该已加载
ls /sys/class/net/vnet0/queues/
```

**实验 6-5：抓包画路径**
在宿主对 `vnet0` 和物理口同时抓包，让 guest `ping` 外网，对比两个接口的
源/目的地址，亲眼看到 MASQUERADE 前后地址的变化。

**实验 6-6：macvtap 复现宿主-Guest 不通**
建一个 macvtap 接口给 guest，从宿主 ping guest，确认不通，再换成 bridge
确认能通，理解固有差异。

---

## 排错指南

| 现象 | 根因 | 定位命令 | 处理 |
| --- | --- | --- | --- |
| guest 无网络 | 网桥没起 / DHCP 未响应 | `ip link show br0`、`virsh net-start` | 启动网络，检查 dnsmasq |
| guest 有 IP 但出不了网 | 转发关闭 / NAT 规则丢失 | `sysctl net.ipv4.ip_forward`、`nft list ruleset` | 开启转发，重启网络或重建规则 |
| 桥接后宿主机断网 | 物理网卡 IP 没移到 br0 | `ip addr show br0`、`ip addr show enp3s0` | 用 nmcli 把 IP 配到 br0 |
| 吞吐低 | 单队列 / 未开 vhost | `ethtool -L`、`lsmod \| grep vhost` | 加 `queues`、确认 vhost |
| macvtap 下宿主与 guest 不通 | macvtap 固有特性 | `ip -d link show` | 换 bridge 模式 |
| VLAN 不通 | tag 配置 / 交换机 trunk 未开 | `bridge vlan show`、两端配置 | 检查两端 VLAN 与 trunk |
| guest 之间隔离网络不通 | 网桥未起 / STP 收敛 | `bridge link show`、`virsh net-dumpxml` | 启动网络，等待 STP |
| 多队列不生效 | queues > vCPU / guest 未启用 | `ls /sys/class/net/vnet0/queues/` | 调小 queues，guest 内 `ethtool -L` |
| DNS 解析失败 | dnsmasq 挂了 / resolv.conf 错 | `ps -ef \| grep dnsmasq` | 重启网络，检查 guest DNS 配置 |
| 重启后网络消失 | 未 `net-autostart` / NM 未持久化 | `virsh net-list --all` | `net-autostart`，NM 保存连接 |
| OVS 端口不通 | 流表/端口未配置 | `ovs-ofctl dump-flows` | 检查流表与 port |
| VF 直通失败 | IOMMU 未开 / VF 未绑 VFIO | `dmesg \| grep -i iommu`、`lspci -k` | 开 VT-d，绑定 VFIO |
| 巨型帧不通 | 链路 MTU 不一致 | `ip link show`、`ping -M do -s` | 全链路统一 MTU |
| 丢包/错误计数上升 | 队列满 / offload 问题 | `ip -s link`、`ethtool -S` | 调队列、关有问题的 offload |

### 案例复盘 1：桥接后宿主机失联，SSH 断线

现象：运维远程 `nmcli con add type bridge ...` 把 `enp3s0` 挂进 `br0`，但没把
IP 从 `enp3s0` 移到 `br0`，SSH 立刻断开，物理机房也连不上。

定位：桥接管物理口后，原 `enp3s0` 连接被停用，其 IP 随之消失；`br0` 没有地址，
宿主机失去管理地址。

处理：到控制台把 IP 配到 `br0`，或删除桥接恢复。正确做法是**在一个 nmcli 命令
里同时**建 bridge 并带上 IP，再用 bridge-slave 挂物理口。

教训：改管理网络要么有带外控制台，要么用 `nmcli` 的原子配置，别分步裸改。

### 案例复盘 2：NAT 网络突然出不了网

现象：一台 VM 昨天还能上网，今天 ping 外网不通，但同宿主其他 VM 正常。

定位：`ip addr` 显示 VM 有 `192.168.122.x`，`ping 192.168.122.1` 通；宿主
`nft list ruleset` 发现 default 网络的 masquerade 规则不见了——有人重启
firewalld 并手动清过规则。

处理：`virsh net-destroy default && virsh net-start default` 让 libvirt 重建规则，
或 `virsh net-update` 触发刷新。根治是让 libvirt 管理防火墙，不要手工清。

教训：**NAT 规则由 libvirt 自动生成，不是静态配置**。手工动防火墙前先想清楚
会不会把虚拟网络规则一起冲掉。

### 案例复盘 3：多队列配了 8 个，吞吐反而下降

现象：一台 2 vCPU 的 VM 把 `queues` 设成 8，期望高吞吐，实际 iperf3 结果比
单队列还差，宿主 `top` 看到大量 `ksoftirqd` 和 vhost 线程抢占。

定位：`queues` 超过 vCPU 数，vhost 内核线程和 vCPU 争抢同一批 CPU；而且
guest 侧根本没协商到 8 队列，部分队列空转，徒增调度开销。

处理：把 `queues` 调到 ≤ vCPU（如 2），并在 guest 内确认
`ethtool -L eth0 combined 2`。

教训：**并行度不是越大越好**，要和 vCPU、NUMA 一起配。多队列的收益来自
"每队列一个 CPU 并行处理"，队列数超过 CPU 数只会互相干扰。

### 案例复盘 4：VLAN 只在宿主侧配了，guest 不通

现象：宿主编好了 `br0` 的 VLAN 100，guest 也加了 `<vlan><tag id='100'/></vlan>`，
但 guest 拿不到 VLAN 100 的地址。

定位：交换机上联口是 access 口而非 trunk，VLAN 100 的帧被丢弃；同时 guest
里网卡没有对应 VLAN 子接口。

处理：把交换机端口改成 trunk 并放行 VLAN 100；确认 guest 侧 VLAN 处理方式
（libvirt 打 tag 还是 guest 内建 VLAN 子接口，两者选一，不能重复打 tag）。

教训：**VLAN 是端到端的**，宿主、交换机、guest 三处必须一致；打 tag 的位置
只能有一个，重复打 tag 会导致双层 VLAN 不通。

---

## 练习

1. 画图说明 NAT 模式下 guest 访问外网的数据包路径，标出 MASQUERADE 前后地址变化。
2. 什么场景该用桥接，什么场景该用 NAT？各举一个真实例子并说明理由。
3. 设计一个用 OVS + VLAN 隔离多租户的网络方案，画出端口与 VLAN 分配。
4. 解释 macvtap 为什么导致宿主与 guest 不通，并给出两种绕过办法。
5. 设计一套多队列 + SR-IOV 的高性能网络方案，说明队列数、CPU 绑定与 IOMMU 的关系。

## 延伸阅读

- libvirt 网络 XML：<https://libvirt.org/formatnetwork.html>
- Linux 内核文档：`Documentation/networking/tuntap.rst`、`vhost-net`
- Open vSwitch 文档：<https://docs.openvswitch.org/>
- `man ip-link`、`man bridge`、`man nmcli`、`man tc`
- Linux 内核 SR-IOV：`Documentation/PCI/pci-iov-howto.rst`
