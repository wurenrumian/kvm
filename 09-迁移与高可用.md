# 模块 09 · 迁移与高可用

## 学习目标

- 理解在线迁移（live migration）的完整流程与前置条件；
- 区分 pre-copy / post-copy / 块迁移的适用场景；
- 能设计高可用（HA）与故障恢复方案。

---

## 9.1 迁移的三种类型

| 类型 | 说明 | 停机时间 |
| --- | --- | --- |
| **冷迁移** | 关机后迁移 | 分钟级 |
| **热迁移（live）** | 运行中迁移，业务几乎无感 | 毫秒~秒级 |
| **在线存储迁移** | 磁盘也一起搬 | 视数据量 |

## 9.2 热迁移前置条件

1. 源和目标宿主机**网络互通**，且能互相访问 libvirt；
2. **CPU 特性兼容**：`host-model` 或自定义 CPU 基线（跨型号必需）；
3. **存储共享**：NFS / Ceph / iSCSI，或启用块迁移；
4. 目标宿主机有足够资源；
5. 时钟/时间同步（chrony）；
6. 防火墙放行迁移端口（libvirt 默认 49152-49215）。

```bash
# 检查 CPU 兼容性（跨主机迁移前）
virsh domcapabilities --machine q35 --virttype kvm > /tmp/domcaps-a.xml
# 在目标机对比，或直接用 host-model
```

## 9.3 热迁移流程（pre-copy）

```
1. 目标机创建空 VM（接收方）
2. 迭代复制内存页到目标机
3. 期间 guest 继续运行，脏页被重复复制
4. 脏页速率收敛后，暂停源 guest
5. 复制最后一批脏页 + 设备状态
6. 目标机接管，源机销毁
```

若脏页速率一直收敛不了（内存密集写负载），pre-copy 会失败 → 用 **post-copy**：
先暂停源机、立即在目标机运行，缺页时按需从源机拉取（风险：源机故障会丢 VM）。

## 9.4 执行迁移

```bash
# 基本热迁移（需目标机 libvirt 可访问）
virsh migrate --live web01 qemu+ssh://target-host/system

# 指定迁移方式与带宽
virsh migrate --live --copy-storage-all web01 \
  qemu+ssh://target/system

# post-copy
virsh migrate --live --postcopy web01 qemu+ssh://target/system

# 迁移并保持源机定义（不停机场景的回滚）
virsh migrate --live --persistent --undefinesource web01 qemu+ssh://target/system

# 查看迁移能力
virsh migrate --help
virsh domjobinfo web01        # 迁移中查看进度
```

## 9.5 存储迁移

**共享存储**：磁盘在 NFS/Ceph 上，两台宿主都能访问 → 只迁移内存，最快。

**非共享存储**：加 `--copy-storage-all`（全量）或 `--copy-storage-inc`（增量），
或用 `blockcopy` 先同步磁盘：

```bash
virsh blockcopy web01 vda /shared/web01.qcow2 --wait --verbose
virsh blockcopy web01 vda /shared/web01.qcow2 --finish --pivot
```

## 9.6 高可用（HA）

KVM 本身不带 HA，需要上层编排：

| 方案 | 机制 | 适用 |
| --- | --- | --- |
| **Pacemaker + corosync** | 集群心跳 + 资源代理，自动重启 VM | 传统小集群 |
| **Proxmox VE HA** | 内置 HA manager | 中小规模 |
| **oVirt / RHV** | 企业级 HA | 大型 |
| **OpenStack** | 计算节点 HA + 实例疏散 | 云平台 |
| **KubeVirt** | K8s 调度与自愈 | 云原生 |

HA 的前提是**共享存储 + 电源管理（fencing）**，否则可能脑裂导致同一 VM 跑两份。

## 9.7 故障恢复

```bash
# 宿主机故障后，在另一台恢复
virsh start web01          # 共享存储上的 VM 直接拉起

# 若需修复配置
virsh define /backup/web01.xml
```

---

## 动手实验

**实验 9-1：本机 SSH 热迁移**
用 `qemu+ssh://localhost/system` 模拟迁移到自身，观察流程（需允许 root ssh）。

**实验 9-2：观察迁移进度**

```bash
virsh migrate --live --verbose web01 qemu+ssh://target/system &
virsh domjobinfo web01
```

**实验 9-3：跨主机迁移（两台机器/两台 VM 模拟）**
配置 SSH 免密 + libvirt 远程访问，完成一次真实迁移。

**实验 9-4：模拟迁移失败**
故意在写压力下迁移，观察 pre-copy 收敛困难，改用 post-copy。

---

## 排错指南

| 报错 | 原因 | 处理 |
| --- | --- | --- |
| `migrate: unsupported configuration` | CPU 特性不兼容 | 改用 host-model |
| 迁移卡住不收敛 | 内存写压力大 | post-copy 或限速 |
| 目标机 `no space` | 存储不足 | 检查目标存储 |
| 迁移后网络不通 | 目标网络配置不一致 | 统一网桥/网络定义 |
| SSH 连接失败 | 免密/防火墙 | 配 authorized_keys、放行端口 |

---

## 练习

1. 列出热迁移的完整前置检查清单。
2. pre-copy 和 post-copy 各自的风险是什么？何时选哪个？
3. 设计一个"共享存储 + Pacemaker"的两节点 HA 方案。

## 延伸阅读

- libvirt 迁移文档：<https://libvirt.org/migration.html>
- QEMU live migration：<https://www.qemu.org/docs/master/devel/migration.html>
- Pacemaker 文档：<https://clusterlabs.org/pacemaker/>
