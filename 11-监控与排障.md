# 模块 11 · 监控与排障

> 排障不是"猜 + 试"，而是"分层定位 + 缩小范围 + 用数据证伪"。
> 这一模块把 KVM 的观测手段、字段含义、以及从症状到根因的路径讲成一套可复用的方法。

## 学习目标

- 建立 KVM 分层排障方法论（宿主机 → QEMU/KVM → guest），掌握"自下而上"的定位原则；
- 熟练使用 `kvm_stat`、`perf kvm`、`virsh domstats`/`domblkstat`/`domifstat`/`dommemstat`、
  `trace-cmd`/ftrace、`bpftrace`、`pidstat -t`、`iostat`、`sar`/`vmstat`；
- 能读懂关键字段（`st` steal、`await`、VM-Exit 原因、`pages_sharing` 等）并判断瓶颈归属；
- 能根据症状（卡顿、IO 慢、网络差、内存紧、VM 起不来、迁移失败）快速走通定位路径；
- 会找、会读 libvirt/QEMU/内核/SELinux 日志，并把一次真实故障复盘成方法。

---

## 11.1 分层排障方法论

虚拟化把系统切成三层，故障可能出现在任意一层。方法论的核心是：
**先确定"问题在哪一层"，再在该层内部找"是哪个资源"**，不要一上来就进 guest 折腾。

```text
症状（用户视角：卡、慢、断、起不来）
  │
  ├─ 宿主机层（先排）：CPU steal/争抢、内存压力/OOM、磁盘 IO、网卡丢包、NUMA 错配
  │     工具：top/mpstat、vmstat、iostat、sar、numastat、/proc/pressure
  │
  ├─ QEMU/KVM 层：vCPU 线程状态、VM-Exit 频率与原因、设备模拟、气球、迁移
  │     工具：kvm_stat、perf kvm、trace-cmd/ftrace、bpftrace、pidstat -t
  │
  └─ guest 层：guest 内核、驱动、应用、guest 自身配置
        工具：guest 内 top/iostat/ss/perf、guest dmesg、应用日志
```

原则：

1. **自下而上**：先排除宿主机资源问题（这是最常见根因），再看虚拟化层，最后进 guest。
2. **用数据证伪**：每个假设都要有一个能推翻它的观测值。例如怀疑 CPU 不足，
   就去看 `st`（steal）和每核 `%idle`，而不是凭感觉。
3. **对照组**：和"同宿主上正常的 VM"或"历史基线"对比。没有基线的监控等于没有监控。
4. **一次只改一个变量**：改完观察，否则无法归因。
5. **区分"guest 内部问题"与"虚拟化问题"**：宿主指标正常、guest 内部指标异常 →
   多半在 guest 内；两者都异常 → 先解决宿主。

> 常见误区：把"VM 慢"直接归因于虚拟化开销。实际上多数"VM 慢"是宿主 CPU 超分、
> 磁盘打满、或 guest 自身配置问题，虚拟化层只是放大器。

## 11.2 常用工具速览

| 工具 | 层次 | 用途 | 关键输出 |
| --- | --- | --- | --- |
| `top` / `htop` | 宿主 | CPU/内存总览、steal time | `%st` |
| `mpstat -P ALL 1` | 宿主 | 每核利用率，定位热点核 | `%usr/%sys/%soft/%steal/%idle` |
| `kvm_stat` | KVM | VM-Exit 原因统计 | `exit_*` 计数 |
| `perf kvm` | KVM | VM-Exit/guest 剖析 | 原因分布、guest 符号 |
| `virsh domstats` | libvirt | VM 级指标 | vCPU 时间、块、网络 |
| `virsh domblkstat/domifstat/dommemstat` | libvirt | 块/网络/内存统计 | reqs/bytes/drop |
| `trace-cmd` / ftrace | 内核 | 跟踪 kvm 事件、函数耗时 | `kvm_entry/exit` |
| `bpftrace` / bcc | 内核 | 自定义聚合、延迟直方图 | 计数/`hist()` |
| `pidstat -t` | 宿主 | 按线程看 vCPU/IO 线程 | 每 TID 的 CPU |
| `iostat -x` / `iotop` / `blktrace` | 宿主 | 磁盘压力与请求延迟 | `%util`、`await` |
| `sar` / `vmstat` | 宿主 | 历史趋势、上下文切换 | `st`、`cs`、`si/so` |
| `/proc/pressure/*`（PSI） | 宿主 | CPU/内存/IO 饱和 | `some avg10` |
| `numastat` | 宿主 | NUMA 本地/远端访问 | `local_node`/`other_node` |

## 11.3 kvm_stat

`kvm_stat` 读取 KVM 的 tracepoint（内核 `arch/x86/kvm/trace.h` 定义），
把每次 VM-Exit 按原因归类计数，是判断"虚拟化开销花在哪"的第一站。

```bash
sudo dnf install -y kvm_stat
sudo kvm_stat                 # 实时
sudo kvm_stat --once          # 一次性
sudo kvm_stat -l              # 列出所有可统计字段
sudo kvm_stat -f exit_io_instr -f exit_msr_write --once
sudo kvm_stat -p <qemu-pid> --once   # 只看某台 VM
```

关注字段（字段名随内核版本略有差异）：

| 字段 | 含义 | 异常偏高的可能原因 |
| --- | --- | --- |
| `kvm_entry` / `kvm_exit` | VM-Entry/Exit 总次数 | 绝对值高 = exit 频繁，开销大 |
| `exit_io_instr` | PIO 退出（模拟设备寄存器） | 用了 IDE/PS2 等模拟设备，换 virtio |
| `exit_mmio` | MMIO 退出（设备寄存器访问） | 模拟设备寄存器访问多，看是否 virtio |
| `exit_msr_read` / `exit_msr_write` | MSR 读写退出 | guest 频繁读写未 passthrough 的 MSR |
| `exit_halt` | guest HLT（空闲） | 高很正常；配合 halt_poll 看空转 |
| `exit_eoi` | EOI 中断结束退出 | 中断路径未硬件加速（APICv 未生效） |
| `exit_external_interrupt` | 外部中断注入退出 | 中断频繁、可能多队列/绑核问题 |
| `exit_apic_timer` | APIC 定时器退出 | guest 忙轮询或定时器精度问题 |
| `exit_hypercall` | hypercall | 半虚拟化操作频繁 |
| `halt_poll_success_ns` 等 | HLT 轮询 | 调 `halt_poll_ns` 优化空闲 |
| `mmu_*` | 内存管理相关事件 | 内存虚拟化压力 |
| `kvm_apic_accept_irq` / `kvm_msi_set_irq` | 中断投递 | 中断负载 |

解读方法：

- **某类 exit 异常高 → 对应路径是瓶颈**。典型：`exit_io_instr`/`exit_mmio` 高 →
  说明 guest 在反复访问模拟设备，换 virtio 或开 vhost 能立刻改善（模块 03/05/06）。
- `kvm_exit` 的**绝对速率**是关键：正常 CPU 密集 guest 每秒几百到几千次；
  若达到几十万次/秒，说明 exit 风暴，性能一定差。
- 用 `perf kvm stat report` 可以看到按 `exit_reason` 的分布，比 `kvm_stat` 更细。

## 11.4 perf kvm

`perf kvm` 建立在 `perf_event` 与 KVM tracepoint 之上，能做**带符号的** guest 剖析。

```bash
sudo perf kvm stat live
sudo perf kvm stat record -a sleep 10
sudo perf kvm stat report
sudo perf kvm stat report --event=vmexit      # 只看 VM-Exit

# 剖析 guest 内热点（需要 guest 符号）
sudo perf kvm --host --guest record -a sleep 5
sudo perf kvm --host --guest report
```

要让 guest 符号解析出来，需提供 guest 的符号表与模块：

```bash
# 方式一：拷贝 guest 的 /proc/kallsyms 与模块目录
sudo perf kvm --host --guest \
  --guestkallsyms /tmp/guest-kallsyms \
  --guestmodules /tmp/guest-modules \
  record -a sleep 5
sudo perf kvm --host --guest report

# 方式二：把 guest 根挂到宿主（需要 guest 可达），一次性自动抓符号
sudo perf kvm --host --guest --guestmount=/tmp/guestfs record -a sleep 5
sudo perf kvm --host --guest report
```

与 `kvm_stat` 的区别：

| 维度 | `kvm_stat` | `perf kvm` |
| --- | --- | --- |
| 数据来源 | 只读 tracepoint 计数 | perf 采样 + tracepoint 事件 |
| 能不能看 guest 热点 | 不能 | 能（带符号） |
| 粒度 | 全局/按 PID 的退出计数 | 事件级、可按栈聚合 |
| 适用 | 快速判断"exit 花在哪" | 精确定位 guest 内核函数热点 |
| 开销 | 低 | 采样有开销，注意范围 |

## 11.5 virsh domstats

`virsh domstats` 是 libvirt 的 VM 级指标聚合入口，一次拿到状态、CPU、内存、块、网络。

```bash
virsh domstats web01                    # 全量
virsh domstats web01 --state --cpu-total --balloon --block --interface
virsh domstats web01 --vcpu             # 每个 vCPU
virsh domstats --list-running           # 所有运行中 VM
virsh domstats --list-active --raw      # 见下方字段
```

常看字段：

| 字段 | 含义 |
| --- | --- |
| `state.state` / `state.reason` | 域状态（running/paused…）与原因 |
| `cpu.time` | 该域累计 CPU 时间（纳秒） |
| `cpu.user` / `cpu.system` | 用户/内核态时间 |
| `vcpu.<n>.time` | 第 n 个 vCPU 的累计时间 |
| `vcpu.<n>.state` | vCPU 状态（running/blocked） |
| `vcpu.<n>.wait` | vCPU 等待时间（被宿主抢占的证据） |
| `balloon.current` / `balloon.maximum` | 当前/最大气球内存（KiB） |
| `balloon.swap_in` / `swap_out` | guest 内换页量 |
| `balloon.major_fault` / `minor_fault` | guest 缺页 |
| `block.<n>.name` / `.rd.reqs` / `.wr.reqs` | 块设备名与读写请求数 |
| `block.<n>.rd.bytes` / `.wr.bytes` | 读写字节数 |
| `block.<n>.fl.reqs` | flush 次数 |
| `block.<n>.allocation` / `.capacity` / `.physical` | 已分配/容量/物理占用 |
| `net.<n>.rx.pkts` / `.rx.bytes` / `.rx.drop` / `.rx.errs` | 收包统计 |
| `net.<n>.tx.pkts` / `.tx.bytes` / `.tx.drop` / `.tx.errs` | 发包统计 |

解读：

- `vcpu.<n>.wait` 持续增长 = vCPU 想跑但被宿主调度延迟 → 超分/绑核问题（模块 08）；
- `block.*.rd.bytes` 大而 `data` 慢 → 后端存储瓶颈；`fl.reqs` 高 → cache 模式或 fsync 重；
- `net.*.drop` 非零 → 队列/中断跟不上，看多队列与 vhost。

## 11.6 块、网络与内存统计

```bash
virsh domblklist web01
virsh domblkstat web01 vda
virsh domiflist web01
virsh domifstat web01 vnet0
virsh dommemstat web01

# guest 内看磁盘/网卡
virsh qemu-monitor-command web01 --hmp 'info block'
virsh qemu-monitor-command web01 --hmp 'info network'
virsh qemu-monitor-command web01 --hmp 'info cpus'
```

`virsh domblkstat` 字段：`rd_req`、`rd_bytes`、`wr_req`、`wr_bytes`、
`flush_operations`、`flush_total_times`。

- `flush_total_times` 大 → guest 频繁 fsync，后端如果是 writeback/network 会放大延迟。

`virsh domifstat` 字段：`rx_bytes`、`rx_packets`、`rx_errs`、`rx_drop`、
`tx_bytes`、`tx_packets`、`tx_errs`、`tx_drop`。

- `drop` 在收向（rx）通常意味着 vhost/tap 队列满或 vCPU 来不及处理。

`virsh dommemstat` 字段：`actual`、`unused`、`available`、`usable`、
`swap_in`、`swap_out`、`major_fault`、`minor_fault`、`disk_caches`、`rss`、`last_update`。

- **前提**：统计走 virtio-balloon 驱动；guest 没装/没开 balloon 时，多数字段取不到，
  命令会报错或只给 `actual`。这是虚拟机监控"探针"的现实前提。
- `rss`（宿主实际驻留）与 `actual`（guest 认为的可用）差异大 → 超分或 balloon 正在回收。

## 11.7 内核跟踪：trace-cmd 与 ftrace

`kvm_stat`/`perf kvm` 够用后，需要"看事件序列和耗时"时用 trace-cmd/ftrace。
tracefs 在新内核可能在 `/sys/kernel/tracing/`，旧路径是 `/sys/kernel/debug/tracing/`。

```bash
sudo dnf install -y trace-cmd
sudo trace-cmd record -e kvm -e kvmmmu sleep 5
sudo trace-cmd report | head -50

# 看单类事件，例如 VM-Exit 原因
sudo trace-cmd record -e kvm:kvm_exit sleep 5
sudo trace-cmd report

# 用 ftrace 直接看可用事件与开关
sudo ls /sys/kernel/tracing/events/kvm/
```

常用 kvm 事件（不同内核略有差异）：

```text
kvm:kvm_entry            进入 guest
kvm:kvm_exit             离开 guest（带 exit_reason）
kvm:kvm_msr              MSR 访问
kvm:kvm_cr               控制寄存器访问
kvm:kvm_apic             APIC 访问
kvm:kvm_pio              PIO 访问（模拟设备）
kvm:kvm_mmio             MMIO 访问
kvm:kvm_page_fault       缺页
kvm:kvm_emulate_insn     指令模拟（需显式开启）
kvm:kvm_nested_vmexit    嵌套虚拟化的 L2 退出
kvm:vcpu_match / vcpu_mismatch   vCPU 调度到期望物理核/未匹配
```

用 `trace-cmd profile` 或 ftrace 的 `function_graph` 看某个 KVM 函数的耗时分布。
`vcpu_match`/`vcpu_mismatch` 是排查"vCPU 频繁迁移导致抖动"的利器。

## 11.8 bpftrace 与 eBPF

`bpftrace` 让你对 KVM tracepoint 做任意聚合，是排障里的"瑞士军刀"。
先列出可用事件：

```bash
sudo bpftrace -l 'tracepoint:kvm:*'
```

统计 VM-Exit 原因分布（替换 `perf kvm stat` 的实时版）：

```bash
sudo bpftrace -e '
tracepoint:kvm:kvm_exit
{
  @[args->exit_reason] = count();
}'
```

统计"每次 VM-Exit 在 KVM 内核里停留了多久"的延迟直方图（识别人为放大的 exit 处理）：

```bash
sudo bpftrace -e '
tracepoint:kvm:kvm_entry { @start[tid] = nsecs; }
tracepoint:kvm:kvm_exit /@start[tid]/
{
  @exit_ns = hist(nsecs - @start[tid]);
  delete(@start[tid]);
}'
```

按调用栈聚合退出来源：

```bash
sudo bpftrace -e '
tracepoint:kvm:kvm_exit { @[kstack] = count(); }'
```

其他常用 eBPF 工具（bcc/`bpf-tools` 包）：

| 工具 | 用途 |
| --- | --- |
| `runqlat` | 调度等待延迟直方图 |
| `runqlen` | 运行队列长度 |
| `cpudist` | 线程在 CPU 上的时间分布 |
| `biolatency` | 块 IO 延迟直方图 |
| `biosnoop` | 逐条块请求（含进程名） |
| `cachestat` | 页缓存命中/回写 |
| `virtiostat` | virtio 设备收发统计 |
| `offcputime` | 线程被换出的时间与栈 |

## 11.9 CPU 与调度观测

vCPU 在宿主上就是普通线程，名字形如 `CPU 0/KVM`；IO 线程可能是 `vhost-<pid>`。

```bash
# 按线程看某个 QEMU 进程的 CPU
sudo pidstat -t -p $(pgrep -f qemu-system-x86_64 | head -1) 1

# 看每个 vCPU 线程绑在哪个物理核
ps -eLo pid,tid,psr,pcpu,comm | grep -E 'CPU [0-9]+/KVM'

# 每核利用率与 steal
mpstat -P ALL 1
```

解读：

- `pidstat -t` 里某个 vCPU 线程长期 100% = 该 vCPU 饱和；多个 vCPU 同时 100%
  而 guest 内也忙 → guest 真的缺 CPU，考虑加 vCPU 或优化；
- 若是 `%st`（steal）高而 guest 内 CPU 不高 → 宿主超分/争抢，问题在宿主；
- `ps ... psr` 显示 vCPU 在不同物理核间跳变 → 抖动来源，考虑绑核（模块 08）；
- `/proc/pressure/cpu` 的 `some avg10` 反映宿主 CPU 饱和，比单看 `%idle` 更准。

```bash
cat /proc/pressure/cpu
# some avg10=... avg60=... avg300=... total=...
```

## 11.10 内存观测

```bash
virsh dommemstat web01
grep -E 'MemTotal|MemAvailable|Committed_AS|CommitLimit|SwapTotal|SwapFree' /proc/meminfo
cat /proc/pressure/memory

# NUMA 本地/远端访问
numastat -p $(pgrep -f qemu-system-x86_64 | head -1)
```

解读：

- `MemAvailable` 低 + `/proc/pressure/memory` 的 `full avg10` 上升 → 宿主内存真正吃紧；
- `Committed_AS` 接近/超过 `CommitLimit` → 有 OOM 风险（取决于 `vm.overcommit_memory`）；
- `numastat` 的 `other_node` 大 → 跨 NUMA 访问，内存性能下降（模块 08）；
- KSM 状态与风险见模块 10：

```bash
grep -E 'pages_shared|pages_sharing|pages_volatile' /sys/kernel/mm/ksm/*
```

## 11.11 存储观测

```bash
iostat -x 1 5          # 宿主磁盘：%util、await、aqu-sz
iotop -o               # 哪个进程在写
blktrace -d /dev/sda -o - | blkparse -i -   # 逐请求跟踪（谨慎使用）
```

`iostat -x` 关键字段：

| 字段 | 含义 | 判读 |
| --- | --- | --- |
| `r/s` / `w/s` | 每秒读/写请求数 | 与 VM 负载对齐 |
| `rkB/s` / `wkB/s` | 每秒读写吞吐 | 对比后端能力 |
| `avgqu-sz`（`aqu-sz`） | 平均队列长度 | 持续 >1 说明排队 |
| `await` | 平均 IO 等待（ms） | 明显高于基线 = 后端变慢 |
| `r_await` / `w_await` | 读/写分别的等待 | 区分读写瓶颈 |
| `%util` | 设备忙碌比例 | 接近 100% = 饱和 |

- 宿主盘 `%util` 高 + VM 内 `await` 高 → 后端存储瓶颈，考虑 SSD/分布式存储/缓存；
- VM 内 `await` 高但宿主盘 `%util` 低 → 可能 cache 模式或 QEMU 线程瓶颈（模块 05）；
- cgroup v2 可直接看某台 VM 的 IO：`/sys/fs/cgroup/.../io.stat`。

## 11.12 典型症状 → 定位路径

### 症状：VM 卡顿、延迟高

```bash
top                          # 看 steal time
mpstat -P ALL 1              # 是否单核打满/不均衡
pidstat -t -p <qemu-pid> 1   # 哪个 vCPU 忙
virsh domstats web01 --cpu-total --vcpu
sudo perf kvm stat live      # 看 VM-Exit
# 若 steal 高 → 宿主机超分/绑核问题（模块 08）
# 若某类 exit 高 → 设备模拟/中断路径（模块 03）
```

### 症状：磁盘 I/O 慢

```bash
virsh domblkstat web01 vda
iostat -x 1                  # 宿主磁盘 util / await
# 检查 cache 模式、是否 virtio、后端是否瓶颈（模块 05/08）
cat /proc/pressure/io
```

### 症状：网络慢/丢包

```bash
virsh domifstat web01 vnet0
ethtool -S enp3s0 | grep -i drop
ip -s link show vnet0
iperf3 -c <ip> -P 8
# 检查多队列、vhost、MTU（模块 06）
```

### 症状：内存不足 / OOM

```bash
virsh dommemstat web01
grep -E 'MemAvailable|Swap' /proc/meminfo
cat /proc/pressure/memory
# 检查 balloon、KSM、超分比例
```

### 症状：VM 起不来

```bash
virsh start web01 --debug
sudo tail -f /var/log/libvirt/qemu/web01.log
sudo ausearch -m avc -ts recent      # SELinux
journalctl -u virtqemud -n 50
sudo virt-host-validate qemu
```

### 症状：迁移失败

```bash
virsh migrate --live --verbose web01 qemu+ssh://dest/system
virsh domjobinfo web01                # 看迁移进度/带宽/脏页
virsh domjobabort web01               # 卡住时中止
# 常见根因：CPU 特性不兼容、存储非共享、网络未打通、目标端资源不足
```

## 11.13 日志位置与阅读方法

| 日志 | 路径 | 看什么 |
| --- | --- | --- |
| libvirt QEMU 日志 | `/var/log/libvirt/qemu/<name>.log` | QEMU stderr：设备错误、启动失败原因 |
| libvirt 守护进程 | `journalctl -u virtqemud -u virtlogd` | 启动/权限/配置错误 |
| 旧版单体守护 | `journalctl -u libvirtd` | 同上（若用单体架构） |
| 内核 | `dmesg`、`journalctl -k` | KVM/IOMMU/内存/OOM 报错 |
| SELinux | `/var/log/audit/audit.log`、`ausearch -m avc` | 被拒绝的资源访问 |
| 虚拟机内部 | guest 的 `dmesg`、应用日志 | guest 内核/驱动/应用问题 |

阅读方法：

- **先看时间线**：把宿主 `dmesg`、libvirt 日志、SELinux 审计的时间对齐，判断因果；
- **看第一条错误**，别被后续的连锁报错带偏；
- 提高日志级别定位：`/etc/libvirt/libvirtd.conf` 的 `log_filters`/`log_outputs`，
  或环境变量 `LIBVIRT_LOG_FILTERS="1:qemu"`；
- `virt-host-validate qemu` 在装好环境后先跑一遍，排除基础环境问题。

## 11.14 案例复盘

**案例 A：定时卡顿，steal 是元凶**

现象：生产 VM 每小时卡顿数分钟，guest 内 CPU 不高。
定位：`mpstat -P ALL 1` 发现个别核 `%steal` 尖峰；
`pidstat -t` 看到 vCPU 线程被挤；`ps -eLo psr` 显示 vCPU 在核间跳。
根因：宿主超分 + 未绑核，同一物理核上跑着别的负载。
处理：vCPU pinning + 隔离核（模块 08），并限制超分比。
教训：`st` 是虚拟化最该盯的指标，guest 内部的"忙"可能只是假象。

**案例 B：数据库 IO 慢，实为低效路径**

现象：DB 写入延迟高，guest 内 `iostat` 的 `await` 很大。
定位：宿主 `iostat -x` 的 `%util` 并不高，`domblkstat` 显示 `flush_total_times` 暴涨。
根因：cache 模式与频繁 fsync 叠加，QEMU 线程成为串行点。
处理：换 `cache=none`/`directsync`、virtio-scsi、适当合并 flush、后端换 NVMe。
教训：**guest 内慢不等于后端盘慢**，要在 QEMU 层找串行点。

**案例 C：VM 起不来，错误被淹没**

现象：`virsh start` 只报一句泛泛错误。
定位：`/var/log/libvirt/qemu/<name>.log` 尾部有真正的报错；
`ausearch -m avc -ts recent` 有 SVC 拒绝；`journalctl -u virtqemud` 给出权限线索。
根因：镜像搬到自建目录后 SELinux 标签不对。
处理：`semanage fcontext` + `restorecon` 打标签后恢复。
教训：**libvirt 的顶层错误往往不是根因**，第一现场是 QEMU 日志与审计日志。

**案例 D：迁移失败，CPU 不兼容**

现象：`virsh migrate --live` 报 CPU 特性无法迁移。
定位：`virsh domcapabilities` 对比两端 CPU 模型；`virsh dumpxml` 看 `host-passthrough`。
根因：源端用了 `host-passthrough`，目标端 CPU 缺某个特性。
处理：统一 CPU 基线（`host-model` 或自定义 model + `check='none'`），或迁移前
确认两端同型号；必要时在维护窗口重启切换 CPU 模型（涉及 vCPU 配置变更）。
教训：性能优先的 `host-passthrough` 与可迁移性天然冲突，要在部署时就选好基线。

---

## 动手实验

**实验 11-1：建立监控基线**

```bash
virsh domstats web01 --cpu-total --vcpu --balloon --block --interface
# 记下空闲态数值，作为后续对比基线
sudo kvm_stat --once
```

**实验 11-2：制造 IO 压力并定位**

```bash
# guest 内跑 fio，宿主用 kvm_stat 和 iostat 观察
# guest: fio --name=t --rw=randwrite --size=1G --bs=4k --iodepth=32 --runtime=60
iostat -x 1 10
sudo kvm_stat -f exit_io_instr -f exit_mmio -f exit_msr_write --once
```

**实验 11-3：perf kvm 剖析**

```bash
sudo perf kvm stat record -a sleep 5 && sudo perf kvm stat report
```

**实验 11-4：用 bpftrace 看 exit 分布与处理延迟**

```bash
sudo bpftrace -l 'tracepoint:kvm:*'
sudo bpftrace -e 'tracepoint:kvm:kvm_exit { @[args->exit_reason] = count(); }'
```

**实验 11-5：故意制造启动失败**

把镜像路径改错，观察 libvirt 日志和 SELinux 审计日志；
再故意把镜像移到未打标签的目录，对比两次报错差异。

**实验 11-6：观察 vCPU 线程调度**

```bash
ps -eLo pid,tid,psr,pcpu,comm | grep -E 'CPU [0-9]+/KVM'
sudo pidstat -t -p <qemu-pid> 1
cat /proc/pressure/cpu
```

---

## 排错指南

| 现象 | 根因 | 定位命令 | 处理 |
| --- | --- | --- | --- |
| VM 卡顿、延迟高 | 宿主 CPU 超分/未绑核，`steal` 高 | `mpstat -P ALL 1`、`pidstat -t -p <pid> 1`、`virsh domstats --vcpu` | vCPU pinning + 隔离核、降低超分比（模块 08） |
| guest 内 CPU 不高但宿主某核 100% | 单个 vCPU 饱和或负载不均衡 | `pidstat -t`、`ps -eLo psr` | 加 vCPU、绑核、检查 guest 侧并发 |
| VM-Exit 速率异常高 | 模拟设备/中断路径未加速 | `sudo kvm_stat --once`、`perf kvm stat live` | 换 virtio、开 vhost、确认 APICv |
| 磁盘读写慢，guest `await` 高 | 后端存储饱和或 cache 模式不当 | `iostat -x 1`、`virsh domblkstat` | 换 cache 模式/virtio-scsi、后端换 SSD/NVMe（模块 05） |
| `flush_total_times` 暴涨 | guest 频繁 fsync，QEMU 串行处理 | `virsh domblkstat web01 vda` | 调整 cache、减少 fsync、后端低延迟 |
| 网络丢包/带宽低 | tap/vhost 队列不足或中断跟不上 | `virsh domifstat`、`ip -s link`、`ethtool -S` | 多队列、vhost、调 MTU（模块 06） |
| guest 内存不足/OOM | 超分过高、balloon 回收、KSM 失效 | `virsh dommemstat`、`/proc/meminfo`、`/proc/pressure/memory` | 降超分、调 balloon、必要时加内存 |
| 宿主内存压力大但 VM 空闲 | KSM/页缓存/超分 | `cat /proc/pressure/memory`、`/sys/kernel/mm/ksm/*` | 关闭/调整 KSM、清理页缓存、降超分 |
| VM 起不来，错误笼统 | SELinux/权限/固件/资源缺失 | `/var/log/libvirt/qemu/<name>.log`、`ausearch -m avc`、`journalctl -u virtqemud` | 按日志根因修复；`virt-host-validate qemu` 预检 |
| VM 启动报 device 打不开 | 镜像路径/权限/加密 secret 问题 | `virsh start --debug`、`virsh secret-list` | 修路径与属主、`virsh secret-set-value` |
| guest 内无网络/无 balloon 统计 | guest 缺驱动或设备未挂 | `virsh domiflist`、guest 内 `ip a`、`virsh dommemstat` | 装 virtio 驱动、补设备 |
| 迁移失败，CPU 特性不兼容 | `host-passthrough` 两端不一致 | `virsh domcapabilities`、`virsh dumpxml` | 统一 CPU 基线，必要时重启切换模型 |
| 迁移卡住/超时 | 带宽不足、脏页追不上、存储非共享 | `virsh domjobinfo`、`virsh migrate --verbose` | 限速/调并发、post-copy、共享存储（模块 09） |
| 热迁移后 guest 抖动 | 目标端 NUMA/绑核与源端不一致 | 目标端 `numastat`、`ps -eLo psr` | 对齐 CPU/NUMA 拓扑与 pinning |
| 监控指标缺失/命令报错 | 守护进程未用模块化/探针缺失 | `systemctl status virtqemud`、`virsh domstats` | 启用对应 virt*d 与 guest 驱动 |

---

## 练习

1. 给出一套"VM 变慢"的完整排查流程图，标出每步看哪个指标、如何据此分支。
2. 说明 `kvm_stat` 与 `perf kvm` 的区别和各自适用场景。
3. 如何判断瓶颈在 guest 内部而非虚拟化层？给出至少三个可证的判据。
4. `%steal`、`vcpu.<n>.wait`、`/proc/pressure/cpu` 三者各反映什么？如何一起用？
5. 写一个 bpftrace 脚本，统计某台 VM 每秒的 VM-Exit 次数并输出直方图。
6. 复盘一次你遇到的"VM 起不来"，按"现象—定位—根因—处理—教训"写出来。

## 延伸阅读

- `man kvm_stat`、`perf kvm --help`、`man virsh`
- libvirt 监控 API：<https://libvirt.org/html/libvirt-libvirt-domain.html>
- 内核 tracepoint：`/sys/kernel/tracing/events/kvm/`
- PSI：`Documentation/accounting/psi.rst`
- bpftrace 参考：<https://github.com/bpftrace/bpftrace>、bcc：<https://github.com/iovisor/bcc>
- 内核文档：`Documentation/virt/kvm/`、`Documentation/admin-guide/perf-security.rst`
