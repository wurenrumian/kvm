# 模块 12 · 高级主题与生态

## 学习目标

- 掌握嵌套虚拟化、机密计算（SEV/TDX）、GPU 虚拟化等前沿方向；
- 了解 KVM 在云原生时代的演进（KubeVirt、Firecracker、Cloud Hypervisor）；
- 能根据场景选择合适的虚拟化技术栈。

---

## 12.1 嵌套虚拟化（Nested Virtualization）

在 guest 里再跑 KVM，用于 CI 测试、培训环境、云上开发。

**Intel**：

```bash
# 宿主机开启嵌套
sudo modprobe -r kvm_intel
sudo modprobe kvm_intel nested=1
# 永久：/etc/modprobe.d/kvm.conf
#   options kvm_intel nested=1
# 验证
cat /sys/module/kvm_intel/parameters/nested   # Y
```

**AMD**：

```bash
echo 1 | sudo tee /sys/module/kvm_amd/parameters/nested
```

guest 侧需用 `-cpu host` 或暴露 `vmx`/`svm` 特性：

```xml
<cpu mode='host-passthrough' check='none'/>
```

```bash
# guest 内验证
grep -oE 'vmx|svm' /proc/cpuinfo | head -1
ls /dev/kvm
```

> 性能：L1（最外层）接近原生，L2（嵌套层）有额外 VM-Exit 开销，通常 30%~50% 损耗。

## 12.2 机密计算

### AMD SEV / SEV-ES / SEV-SNP

- **SEV**：内存加密（每 VM 独立密钥）；
- **SEV-ES**：加密寄存器状态；
- **SEV-SNP**：增加完整性保护，防恶意 hypervisor 篡改。

```bash
# 宿主机检查支持
dmesg | grep -i sev
cat /sys/module/kvm_amd/parameters/sev
# guest 启动（QEMU）
qemu-system-x86_64 -enable-kvm -machine q35,confidential-guest-support=sev0 \
  -object sev-guest,id=sev0,cbitpos=51,reduced-phys-bits=1 ...
```

### Intel TDX

Trust Domain Extensions，硬件隔离的机密 VM，由 TDX Module 管理。
需要支持 TDX 的 CPU + 新版内核 + QEMU。

```bash
dmesg | grep -i tdx
# QEMU: -object tdx-guest,id=tdx0 -machine q35,confidential-guest-support=tdx0
```

**适用**：多租户云、合规要求数据"运行中也不可被云厂商读取"的场景。

## 12.3 GPU 虚拟化（回顾与补充）

| 技术 | 厂商 | 一卡多机 |
| --- | --- | --- |
| VFIO 整卡直通 | 通用 | 否 |
| NVIDIA vGPU | NVIDIA | 是 |
| SR-IOV (Intel) | Intel | 是 |
| GVT-g | Intel（已逐步被 SR-IOV 取代） | 是 |
| virtio-gpu + venus | 开源 | 是（共享渲染） |

## 12.4 云原生：KubeVirt

KubeVirt 让 KVM 虚拟机成为 Kubernetes 的一等公民（`VirtualMachine` CRD），
用 Pod 承载 virt-launcher + QEMU，实现"容器编排 VM"。

```yaml
apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: vm1
spec:
  running: true
  template:
    spec:
      domain:
        cpu: {cores: 2}
        resources:
          requests: {memory: 2Gi}
        devices:
          disks:
            - name: rootdisk
              disk: {bus: virtio}
      volumes:
        - name: rootdisk
          containerDisk: {image: quay.io/kubevirt/fedora-cloud-container-disk-demo}
```

适用：既有容器又有 VM 的混合平台，统一调度与运维。

## 12.5 轻量 VMM

| 项目 | 特点 | 典型用户 |
| --- | --- | --- |
| **Firecracker** | 极简 VMM，毫秒启动，专为 serverless | AWS Lambda、Fargate |
| **Cloud Hypervisor** | Rust 编写，精简、安全 | 云原生、Intel |
| **crosvm** | ChromeOS 的 VMM | ChromeOS / Android |
| **rust-vmm** | 共享组件库 | 上述项目的底座 |

这些项目的共同思路：**去掉 QEMU 里 serverless 用不到的设备模型**，缩小攻击面、
加快启动。

## 12.6 与容器的融合

- **Kata Containers**：容器接口 + VM 隔离（每个 Pod 一个轻量 VM）；
- **gVisor**：用户态内核，非 VM 但有强隔离；
- **Firecracker + containerd**：AWS Fargate 的实现路径。

选择逻辑：

```
需要异构 OS / 强隔离 / GPU 直通   → KVM
需要极致密度 / 快速启动            → 容器
两者都要                          → Kata / Firecracker / KubeVirt
```

## 12.7 实时与确定性

- **PREEMPT_RT** 内核 + KVM 可用于软实时；
- vCPU pinning + 隔离核 + `isolcpus` 降低抖动；
- 注意：完全硬实时通常用非虚拟化或专用 RTOS。

## 12.8 生态地图

```
        编排层    OpenStack | oVirt | Proxmox | Kubernetes/KubeVirt
          │
        管理层    libvirt
          │
        执行层    QEMU  |  Cloud Hypervisor  |  Firecracker
          │
        内核层    KVM (+ vhost, vfio, iommu)
          │
        硬件层    VT-x/AMD-V, EPT/NPT, VT-d, SEV/TDX, SR-IOV
```

---

## 动手实验

**实验 12-1：开启嵌套并跑 L2 VM**

```bash
cat /sys/module/kvm_intel/parameters/nested
# 若为 N，按 12.1 开启后重启
# 在 guest 内再装 KVM，跑一台 L2 VM，观察性能
```

**实验 12-2：检查机密计算能力**

```bash
dmesg | grep -iE 'sev|tdx'
ls /sys/module/kvm_amd/parameters/ 2>/dev/null
```

**实验 12-3（选做）：体验 Firecracker**

```bash
# 下载 firecracker + 内核 + rootfs，跑一个 microVM
# 观察启动时间（毫秒级）
```

---

## 排错指南

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| guest 内无 `/dev/kvm` | 未开嵌套 / CPU 未暴露 vmx | 开 nested=1，用 host-passthrough |
| SEV 启动失败 | 平台不支持/固件未开 | 检查 dmesg、BIOS 设置 |
| KubeVirt VM 起不来 | 节点缺 KVM/资源 | 检查 virt-handler、节点标签 |
| Firecracker 启动慢 | 内核/rootfs 不合适 | 用官方 microVM 镜像 |

---

## 练习

1. 什么业务场景值得上机密计算？成本收益如何权衡？
2. 对比 KubeVirt 与传统 OpenStack 的适用边界。
3. 为"多租户 AI 推理平台"选一套技术栈并说明理由。

## 延伸阅读

- AMD SEV：<https://www.amd.com/en/developer/sev.html>
- Intel TDX：<https://www.intel.com/content/www/us/en/developer/tools/trust-domain-extensions/overview.html>
- KubeVirt：<https://kubevirt.io/>
- Firecracker：<https://firecracker-microvm.github.io/>
- Cloud Hypervisor：<https://www.cloudhypervisor.org/>
