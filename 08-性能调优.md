# 模块 08 · 性能调优

## 学习目标

- 建立"CPU / 内存 / 存储 / 网络"四维调优框架；
- 掌握 vCPU pinning、NUMA 亲和、HugePages、io_uring 等实战手段；
- 会用压测工具量化调优效果。

---

## 8.1 调优总原则

1. **先测量，再优化**：没有基线数据的调优都是玄学。
2. **找瓶颈**：CPU、内存、磁盘、网络，一次只动一个变量。
3. **隔离噪声**：宿主机的其他负载会污染结果。
4. **代价意识**：性能、隔离、可迁移性、成本四者互相牵制。

## 8.2 CPU 调优

### vCPU 就是宿主机线程

每个 vCPU 对应宿主机一个线程，由 CFS 调度。调度抖动、跨 NUMA 访存是主要损耗。

### CPU pinning（绑核）

把 vCPU 固定到物理核，避免迁移抖动：

```xml
<vcpu placement='static'>4</vcpu>
<cputune>
  <vcpupin vcpu='0' cpuset='2'/>
  <vcpupin vcpu='1' cpuset='3'/>
  <vcpupin vcpu='2' cpuset='4'/>
  <vcpupin vcpu='3' cpuset='5'/>
  <emulatorpin cpuset='0-1'/>
</cputune>
```

- `vcpupin`：vCPU 绑核；
- `emulatorpin`：QEMU 主线程/IO 线程绑核，避免和 vCPU 抢核；
- 配合 `<cpu mode='host-passthrough'/>` 获得最佳特性。

### NUMA 亲和

跨 NUMA 节点访存延迟高，应让 vCPU 与内存同节点：

```xml
<numatune>
  <memory mode='strict' nodeset='0'/>
</numatune>
```

```bash
# 查看宿主机 NUMA 拓扑
numactl --hardware
lscpu | grep -i numa
```

### 其他

- **巨页 + 透明大页**：内存密集负载显著受益。
- **禁用 CFS 节流**：`<vcpu ... cpuset>` + `sched` 参数，或调 `cpu.cfs_quota_us`。
- **core scheduling**：缓解 SMT 侧信道的同时保持性能。

## 8.3 内存调优

| 手段 | 效果 | 代价 |
| --- | --- | --- |
| HugePages（2M/1G） | 减少 TLB miss，提升吞吐 | 需预留，不能 balloon |
| KSM | 合并相同页，省内存 | CPU 开销 + 侧信道风险 |
| Ballooning | 动态回收空闲内存 | 有抖动，实时性差 |
| 内存绑定 NUMA | 降低访存延迟 | 配置复杂 |

**配置 HugePages**：

```bash
# 预留 1GB 大页 4 个
echo 4 | sudo tee /sys/kernel/mm/hugepages/hugepages-1048576kB/nr_hugepages
grep HugePages /proc/meminfo
```

```xml
<memoryBacking>
  <hugepages/>
</memoryBacking>
```

## 8.4 存储调优

| 手段 | 说明 |
| --- | --- |
| `cache=none, io=native/io_uring` | 绕过宿主页缓存，低延迟 |
| `bus='virtio'` 或 virtio-scsi | 必选 |
| 多队列 virtio | `<driver queues='N'/>` |
| iothread | 独立线程处理 IO，避免与 vCPU 抢 |
| 后端用 SSD/NVMe/LVM | 物理层是上限 |
| 预分配镜像 | `qemu-img create -o preallocation=metadata` |

iothread 配置：

```xml
<iothreads>1</iothreads>
<disk ...>
  <driver name='qemu' type='qcow2' cache='none' io='io_uring' queues='4'/>
  <target dev='vda' bus='virtio'/>
</disk>
```

## 8.5 网络调优

- virtio-net + vhost-net（默认）；
- 多队列 `<driver queues='N'/>`；
- 巨帧（MTU 9000）减少分片；
- SR-IOV VF 直通（性能最优，牺牲灵活性）；
- 宿主机 `ethtool` 调中断合并、ring buffer。

```bash
ethtool -l enp3s0          # 队列数
ethtool -g enp3s0          # ring buffer
ethtool -k enp3s0 | grep -i offload
```

## 8.6 压测工具与指标

| 维度 | 工具 | 关注指标 |
| --- | --- | --- |
| CPU | `stress-ng`、`sysbench cpu` | 上下文切换、steal time |
| 内存 | `sysbench memory`、`stream` | 带宽、延迟 |
| 磁盘 | `fio` | IOPS、吞吐、延迟 |
| 网络 | `iperf3`、`netperf` | 吞吐、PPS、重传 |
| 整体 | `perf kvm stat` | VM-Exit 原因 |

```bash
# CPU 压力
sudo dnf install -y stress-ng
stress-ng --cpu 4 --timeout 60s

# 磁盘（guest 内）
fio --name=t --ioengine=libaio --rw=randread --bs=4k \
    --iodepth=32 --numjobs=4 --size=1G --runtime=30 --group_reporting

# 网络
iperf3 -s              # 一端做服务端
iperf3 -c <ip> -P 8    # 另一端压
```

---

## 动手实验

**实验 8-1：建立性能基线**
分别用 fio / iperf3 记录默认配置下的数据，作为对照。

**实验 8-2：vCPU 绑核对比**
同一负载，`placement='auto'` vs `static + vcpupin`，对比延迟抖动（fio 的 p99）。

**实验 8-3：HugePages 收益**
内存密集负载（如 `sysbench memory`）开启巨页前后对比。

**实验 8-4：perf kvm 定位热点**

```bash
sudo perf kvm stat live
# 观察 EXTERNAL_INTERRUPT / MSR_WRITE / IO_INSTRUCTION 等占比
```

---

## 排错指南

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| 延迟抖动大 | vCPU 被调度走 | pinning + 隔离核 |
| 吞吐上不去 | 单队列 / 磁盘后端瓶颈 | 多队列 + 换后端 |
| `steal time` 高 | 宿主机超分严重 | 减少超分或绑核 |
| 巨页分配失败 | 内存碎片/预留不足 | 重启或调大 nr_hugepages |

---

## 练习

1. 为一个 8 vCPU 的数据库 VM 设计完整的 CPU/内存/存储调优方案。
2. 解释 `emulatorpin` 为什么必要。
3. 用数据说明 `cache=none` 相比 `writeback` 的延迟优势与吞吐代价。

## 延伸阅读

- `Documentation/admin-guide/mm/`（HugePages、KSM）
- `Documentation/virt/kvm/`
- Red Hat 虚拟化调优指南
- `man numactl`、`man taskset`
