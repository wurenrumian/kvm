# 模块 10 · 安全与隔离

## 学习目标

- 理解 KVM 的安全模型：虚拟机边界、sVirt、SELinux/AppArmor；
- 掌握镜像加密、Secure Boot、vTPM 的配置；
- 认识侧信道风险与缓解手段。

---

## 10.1 安全模型

KVM 的安全边界本质上是 **QEMU 进程边界**：guest 逃逸 = 攻破 QEMU 进程。
因此防护分三层：

1. **进程隔离**：用非 root 用户、cgroup、namespace 限制 QEMU；
2. **强制访问控制**：sVirt（SELinux/AppArmor）给每个 VM 打标签，禁止跨 VM 访问；
3. **硬件/固件**：IOMMU 隔离、Secure Boot、vTPM、内存加密。

## 10.2 sVirt 与 SELinux

sVirt 把每个 QEMU 进程和它的磁盘文件打上唯一标签，即使 QEMU 被攻破，
也无法读写其他 VM 的文件。

```bash
# 查看 SELinux 状态
getenforce
sestatus

# 查看 VM 进程标签
ps -eZ | grep qemu
# system_u:system_r:svirt_t:s0:c123,c456 ...

# 查看镜像文件标签
ls -Z /var/lib/libvirt/images/*.qcow2
# system_u:object_r:svirt_image_t:s0:c123,c456

# 给自定义目录打标签（否则 VM 起不来）
sudo semanage fcontext -a -t svirt_image_t "/data/vms(/.*)?"
sudo restorecon -Rv /data/vms
```

> 常见故障：把镜像放到 `/home` 或自建目录，SELinux 拦截导致 VM 无法启动。
> 解决：正确打标签，或（不推荐）`setenforce 0`。

## 10.3 AppArmor（Debian/Ubuntu）

libvirt 会为每个 VM 生成 `/etc/apparmor.d/libvirt/libvirt-<uuid>.files`，
限制 QEMU 可访问的路径。

```bash
sudo aa-status | grep libvirt
sudo cat /etc/apparmor.d/libvirt/libvirt-*.files
```

## 10.4 磁盘加密

**方案 A：LUKS 加密块设备**

```bash
sudo cryptsetup luksFormat /dev/vg0/vm-secret
sudo cryptsetup open /dev/vg0/vm-secret vm-secret
# 把 /dev/mapper/vm-secret 作为 guest 磁盘
```

**方案 B：qcow2 内置加密（LUKS 格式）**

```bash
qemu-img create -f qcow2 --object secret,id=sec0,data=passphrase \
  -o encrypt.format=luks,encrypt.key-secret=sec0 disk-enc.qcow2 20G
```

```xml
<disk type='file' device='disk'>
  <driver name='qemu' type='qcow2'/>
  <source file='/data/disk-enc.qcow2'/>
  <encryption format='luks'>
    <secret type='passphrase' uuid='...'/>
  </encryption>
</disk>
```

## 10.5 UEFI Secure Boot

防止 guest 被植入未签名的 bootloader：

```xml
<os firmware='efi'>
  <type arch='x86_64' machine='q35'>hvm</type>
  <loader secure='yes'/>
  <nvram template='/usr/share/OVMF/OVMF_VARS.secboot.fd'/>
</os>
```

```bash
# Fedora 上需要 OVMF
sudo dnf install -y edk2-ovmf
# guest 内验证
mokutil --sb-state
```

## 10.6 vTPM

为 guest 提供 TPM 2.0，用于 BitLocker、密钥密封、远程证明：

```xml
<devices>
  <tpm model='tpm-crb'>
    <backend type='emulator' version='2.0'/>
  </tpm>
</devices>
```

```bash
sudo dnf install -y swtpm swtpm-tools
# guest 内
dmesg | grep -i tpm
```

## 10.7 侧信道与缓解

| 攻击 | 原理 | 缓解 |
| --- | --- | --- |
| Spectre/Meltdown | 推测执行泄露 | CPU 微码 + 内核缓解 |
| L1TF | 页表侧信道 | `l1tf=full` + 关 SMT |
| MDS | 微架构缓冲泄露 | 微码 + `mds=full` |
| KSM 侧信道 | 内存合并暴露访问模式 | 敏感场景关闭 KSM |

```bash
# 关闭 KSM（敏感场景）
echo 0 | sudo tee /sys/kernel/mm/ksm/run

# 核心调度缓解 SMT 侧信道
# 内核参数：sched_core=1
```

## 10.8 机密计算（预告）

- **AMD SEV / SEV-SNP**：内存加密 + 完整性保护；
- **Intel TDX**：Trust Domain，硬件隔离的 VM；
- 详见模块 12。

## 10.9 其他加固清单

- 用 `qemu:///session` 跑非特权 VM；
- 限制 QEMU 的 cgroup 资源；
- 禁用不必要的设备（USB、串口、图形）；
- 定期更新 QEMU/libvirt/内核（漏洞多在这些组件）；
- 网络层做隔离（VLAN/安全组）；
- 最小化 guest 镜像（减少攻击面）。

---

## 动手实验

**实验 10-1：观察 sVirt 标签**

```bash
ps -eZ | grep qemu
ls -Z /var/lib/libvirt/images/
```

**实验 10-2：制造一次 SELinux 拦截**

```bash
mkdir -p /data/vms && mv /var/lib/libvirt/images/*.qcow2 /data/vms/
# 改 XML 指向新路径，尝试启动 → 失败
sudo ausearch -m avc -ts recent | tail
# 正确打标签后恢复
sudo semanage fcontext -a -t svirt_image_t "/data/vms(/.*)?"
sudo restorecon -Rv /data/vms
```

**实验 10-3：启用 vTPM**

装 swtpm，按 10.6 加 TPM，进 guest 验证 `tpm2_getrandom`。

**实验 10-4：加密盘**

按 10.4 方案 B 创建加密 qcow2，进 guest 读写验证透明加密。

---

## 排错指南

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| VM 启动被拒 | SELinux 标签不对 | `restorecon` / `semanage fcontext` |
| AppArmor deny | 路径不在白名单 | 编辑 profile 或调整路径 |
| Secure Boot 下无法启动 | bootloader 未签名 | 用签名镜像或关 Secure Boot |
| vTPM 起不来 | 缺 swtpm | 安装 `swtpm` 并确认权限 |

---

## 练习

1. 解释 sVirt 如何防止"一个 VM 逃逸后横向攻击其他 VM"。
2. 什么时候应该关闭 KSM？为什么？
3. 设计一个满足等保/合规要求的 VM 安全加固清单。

## 延伸阅读

- SELinux 与虚拟化：`man virt_selinux`
- libvirt 安全驱动：<https://libvirt.org/drvsecurity.html>
- Intel TDX / AMD SEV 官方文档
