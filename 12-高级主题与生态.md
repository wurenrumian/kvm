# 模块 12 · 高级主题与生态

> 前面 11 个模块解决的是"单机把 KVM 用明白"。本模块把视角拉到前沿与生态：
> 嵌套、机密计算、GPU、云原生、轻量 VMM、实时。目标不是背名词，而是能在选型会上
> 讲清每种技术的**信任边界、性能代价和落地前提**。

## 学习目标

- 讲清嵌套虚拟化的 L0/L1/L2 执行模型、影子 VMCS/VMCB 机制与性能损耗来源；
- 分清 AMD SEV/SEV-ES/SEV-SNP 与 Intel TDX 的信任根、保护范围与配置差异；
- 理解 KubeVirt 各组件职责（virt-operator/controller/handler/launcher）与 containerDisk；
- 对比 Firecracker / Cloud Hypervisor / crosvm / rust-vmm 的取舍，知道何时不用 QEMU；
- 说清 Kata / gVisor 与 KVM 的关系，以及"容器隔离 vs VM 隔离"的边界；
- 掌握 PREEMPT_RT + 隔离核 + 绑核降低抖动的方法与硬实时的天花板；
- 能按场景画出一条从需求到技术栈的选型决策路径。

---

## 12.1 嵌套虚拟化（Nested Virtualization）

### 12.1.1 为什么要嵌套

在 guest 里再跑 KVM，常见诉求：

- **CI/CD**：流水线里测试 KVM/libvirt 本身、验证镜像、跑 Kata/Firecracker（它们需要 `/dev/kvm`）；
- **培训与演示**：一台云主机里搭出"多台宿主"的教学环境；
- **云上开发**：Android 模拟器、WSL2、GKE/AKS 的嵌套节点；
- **验证 hypervisor 代码**：不想为每次改动准备裸机。

术语约定：

```text
L0 = 裸机 hypervisor（本课程的 Fedora 44 + kvm_intel）
L1 = 第一层 guest；它自己又运行 KVM，所以对 L2 而言它是 hypervisor
L2 = 最内层 guest，由 L1 里的 KVM 启动
```

### 12.1.2 原理：硬件只有一套 VMX，怎么再套一层

Intel VT-x 只有两种模式：**VMX root**（hypervisor）和 **VMX non-root**（guest）。
嵌套的难点在于：L1 里的 KVM 也想执行 `VMXON`/`VMLAUNCH`/`VMRESUME` 和读写 VMCS，
但硬件只有一套 VMCS，L0 不能把它直接让给 L1。

```text
             ┌──────────────── L0 裸机 ────────────────┐
             │  kvm_intel（VMX root）                   │
             │   ├─ vmcs01：L0 跑 L1 的控制结构          │
             │   └─ vmcs02：真正承载 L2 的控制结构       │
             │        （L1 以为自己在用的叫 vmcs12，      │
             │          L0 截获 VMWRITE 后代为保存）      │
             │                                          │
             │   ┌────────── L1 guest ──────────┐        │
             │   │  Linux + kvm_intel           │        │
             │   │   （对 L2 是 VMX root）       │        │
             │   │   ┌──── L2 guest ────┐       │        │
             │   │   │  VMX non-root    │       │        │
             │   │   └──────────────────┘       │        │
             │   └──────────────────────────────┘        │
             └──────────────────────────────────────────┘
```

关键机制：

- **Intel 影子 VMCS（shadow VMCS）**：L0 维护 `vmcs01`（跑 L1）与 `vmcs02`（跑 L2）。
  L1 对 VMCS 的读写被 L0 截获，映射到 `vmcs12`/影子结构上。开启 **VMCS shadowing**
  （`enable_shadow_vmcs`，默认 Y）后，L1 读写 shadow VMCS 不再触发 VM-Exit，性能关键。
  源码：`arch/x86/kvm/vmx/nested.c`、`arch/x86/kvm/vmx/vmx.c`；核心结构
  `struct vcpu_vmx`（含 `vmcs01`、`vmcs02`、`nested`）。
- **AMD VMCB shadowing**：同一思路，`struct vcpu_svm` 的 `nested` 中带 `vmcb01`/`vmcb02`，
  源码 `arch/x86/kvm/svm/nested.c`。
- **一次 L2 退出的路径**：L2 触发 VM-Exit → 硬件直接陷入 **L0**（不是 L1）→ L0 判断
  该退出是否属于 L1 想拦截的类型 → 若是，L0 合成一个"假的" VM-Exit 注入 L1
  （nested VM-Exit）→ L1 处理完 `VMRESUME` → L0 再 `VM-Entry` 回 L2。
  所以 L2 的一次退出可能引发**两次 guest/host 切换**，这是嵌套开销的根本来源。
- **Nested EPT**：L1 若给 L2 用 EPT，则地址翻译变成 `GVA → GPA(L2) → GPA(L1) → HPA`，
  L0 需要合并两套页表。开启条件是 L0 的 `ept=1` 且 L1 能看到 `ept` 特性。
  没有 nested EPT 时退回影子页表，开销显著上升。

### 12.1.3 开启嵌套

**Intel：**

```bash
# 查看当前状态（现代内核约 4.7 起 kvm_intel 的 nested 默认已为 Y）
cat /sys/module/kvm_intel/parameters/nested

# 若为 N 才需要手动开启
sudo modprobe -r kvm_intel
sudo modprobe kvm_intel nested=1

# 永久生效
echo 'options kvm_intel nested=1' | sudo tee /etc/modprobe.d/kvm.conf
```

**AMD：**

```bash
# 默认通常为 1
cat /sys/module/kvm_amd/parameters/nested
echo 1 | sudo tee /sys/module/kvm_amd/parameters/nested
```

> 说明：`kvm_intel.nested` 与 `kvm_amd.nested` 的默认值在新内核上都是**开启**的，
> 老内核/老发行版才需要手动。若 `cat` 出来是 `N`/`0`，按上面改并重载模块。

guest 侧必须能看到 `vmx`（Intel）或 `svm`（AMD）特性。最省事是 CPU 透传：

```xml
<cpu mode='host-passthrough' check='none'/>
```

需要兼顾迁移时，用 host-model 显式要求该特性：

```xml
<cpu mode='host-model' check='none'>
  <feature policy='require' name='vmx'/>
</cpu>
```

`virt-install` 等价写法：`--cpu host-passthrough`。

guest 内验证：

```bash
grep -oE 'vmx|svm' /proc/cpuinfo | head -1
ls -l /dev/kvm
lsmod | grep -E '^kvm'
```

### 12.1.4 性能损耗与调优

损耗高度依赖负载类型，下面是有代表性的经验区间（不是绝对值）：

| 层级 / 负载 | 典型损耗 | 主因 |
| --- | --- | --- |
| L1 CPU 密集 | 2%~5% | 常规 VM-Exit |
| L2 CPU 密集 | 5%~20% | 双重退出、影子结构维护 |
| L2 I/O / 退出密集 | 30%~50%+ | 每次 I/O 可能两次 guest/host 切换 |
| 无 nested EPT（退回影子页表） | 可达数倍 | 页表拦截与模拟 |

调优方向：

- 确保 L0 开启 `ept=1`（Intel）/ `npt=1`（AMD），并让 L1 暴露 EPT 给 L2；
- L1 里给 L2 尽量用 virtio + vhost，减少模拟设备；
- L0 侧用 `kvm_stat`/`perf kvm` 看退出分布，判断是 CPU 还是 I/O 瓶颈；
- Intel 可关注 PAUSE-loop exiting 参数（`ple_gap`/`ple_window`），
  锁竞争重的负载下调整可减少退出；APICv 在某些嵌套场景下需关闭才能稳定。

### 12.1.5 常见坑

- L0 没开 nested，L1 里永远看不到 `vmx`；
- L1 用 `host-model` 却没显式 `require` `vmx`，特性被裁掉；
- 嵌套层数越深，**迁移**越难（VMCS 状态、CPU 特性兼容性）；
- Windows L2 需要 Hyper-V enlightenments（`<hyperv>`）才能发挥性能。

## 12.2 机密计算（Confidential Computing）

### 12.2.1 威胁模型：防的是"可信的云厂商"

传统虚拟化里，hypervisor 能读 guest 的全部内存与寄存器——这既是管理能力，也是攻击面。
机密计算把 hypervisor 移出信任边界：**即使宿主被攻陷，guest 的内存与运行状态也不可读、
不可篡改，并可通过远程证明自证身份。**

三要素：

| 要素 | 目标 | 技术手段 |
| --- | --- | --- |
| 机密性 Confidentiality | 内存/状态不可读 | 硬件内存加密 |
| 完整性 Integrity | 不可被重映射/篡改 | 反向映射表、安全 EPT |
| 证明 Attestation | 向对端证明"我是什么" | 硬件签名的度量报告 |

### 12.2.2 AMD SEV / SEV-ES / SEV-SNP

| 特性 | 加密对象 | 关键机制 | 需要代际 |
| --- | --- | --- | --- |
| SEV | guest 内存 | 每 VM 独立密钥（VEK），由 AMD-SP（原 PSP）管理；页表 C-bit 标记 | EPYC Naples+ |
| SEV-ES | 内存 + CPU 寄存器状态（VMSA） | VM-Exit 时寄存器加密，guest 通过 `#VC` 异常与 hypervisor 通信 | EPYC Rome+ |
| SEV-SNP | 内存 + 寄存器 + 完整性 | RMP 反向映射表防重映射/重放；支持证明、安全中断 | EPYC Milan+ |

- 密钥不落内存明文：由片上 **AMD Secure Processor** 生成与保管，CPU 透明加解密；
- `#VC`（VMM Communication）是 SEV-ES 的核心：guest 与 hypervisor 的所有交互都变成
  受控的异常，hypervisor 看不到寄存器内容；
- SNP 的 **RMP（Reverse Map Table）** 为每个物理页记录归属，阻止 hypervisor 把某 VM 的
  页映射给另一个 VM 或重放旧数据。

宿主机检查：

```bash
dmesg | grep -i -e sev -e sme
cat /sys/module/kvm_amd/parameters/sev
cat /sys/module/kvm_amd/parameters/sev_es
cat /sys/module/kvm_amd/parameters/sev_snp
ls -l /dev/sev /dev/sev-guest
```

QEMU 启动（SEV，参数由平台决定）：

```bash
qemu-system-x86_64 -enable-kvm -machine q35,confidential-guest-support=sev0 \
  -cpu EPYC -smp 2 -m 2048 \
  -object sev-guest,id=sev0,cbitpos=51,reduced-phys-bits=1,policy=0x1 \
  -drive file=disk.qcow2,if=virtio,format=qcow2 \
  -bios /usr/share/OVMF/OVMF_CODE.fd
```

> `cbitpos` / `reduced-phys-bits` 随平台而变（Rome/Milan 常见 `51`/`1`），
> 可用 QMP `query-sev-capabilities` 查询。`policy` 是位掩码：`0x1` 禁调试、
> `0x2` 禁密钥共享、`0x4` 要求 SEV-ES、`0x20` 要求 SNP。SEV 必须配 **SEV-aware OVMF**。

libvirt 侧：

```xml
<launchSecurity type='sev'>
  <policy>0x0001</policy>
  <cbitpos>51</cbitpos>
  <reducedPhysBits>1</reducedPhysBits>
</launchSecurity>
```

SEV-SNP 用 `type='sev-snp'`，另需 `<idBlock>`/`<idAuth>` 等证明材料。

### 12.2.3 Intel TDX

TDX（Trust Domain Extensions）思路不同：在 VMM 之下、硬件之上引入一个**被信任的
TDX Module**，运行在 **SEAM**（Secure Arbitration Mode）模式，充当"微型可信 hypervisor"。

```text
        普通 VM          TD（Trust Domain）
          │                    │
     ┌────┴────┐          ┌────┴────┐
     │  VMM (QEMU/KVM)      │  TDX Module（SEAM）
     │  └─ 传统 EPT          │  └─ Secure EPT (SEPT)
     └─────────┘          └─────────┘
                硬件（MKTME/TDX 内存加密）
```

- **TD** 就是机密 VM；内存由 TDX 的密钥加密；
- **SEPT（Secure EPT）** 由 TDX Module 维护，VMM 只能提供 GPA→HPA 的候选映射，
  不能任意改写，从而保护完整性；
- guest 通过 `TDVMCALL` 与 VMM 通信（对应 SEV-ES 的 `#VC` 角色），VMM 无法访问 TD 私有内存；
- guest 内 `/dev/tdx_guest` 提供证明接口（TDREPORT）。

宿主机检查：

```bash
dmesg | grep -i tdx
cat /sys/module/kvm_intel/parameters/tdx
```

QEMU 启动：

```bash
qemu-system-x86_64 -enable-kvm \
  -machine q35,confidential-guest-support=tdx0,kernel_irqchip=split \
  -cpu host -smp 2 -m 2048 \
  -object tdx-guest,id=tdx0,quote-generation-service=vsock:2:4050 \
  -drive file=disk.qcow2,if=virtio,format=qcow2
```

### 12.2.4 SEV 与 TDX 对比

| 维度 | AMD SEV-SNP | Intel TDX |
| --- | --- | --- |
| 信任根 | AMD-SP（片上安全处理器） | TDX Module（SEAM 模式） |
| 内存加密 | SME，C-bit | MKTME/TDX |
| 完整性 | RMP 反向映射表 | Secure EPT + TDX Module |
| 寄存器保护 | VMSA 加密 + `#VC` | Module 管理 + `TDVMCALL` |
| 证明 | SNP report（VLEK/ARPK 等） | TDREPORT + Quote（QGS 生成） |
| 硬件代际 | EPYC Milan/Genoa+ | Sapphire Rapids+ |
| 设备直通 | TDISP（逐步落地） | TDISP（逐步落地） |
| 迁移 | 需 SNP 迁移支持 | 支持 TD 迁移（新内核/QEMU） |

### 12.2.5 适用场景与限制

**适用**：多租户公有云、金融/医疗合规、机密 AI（模型权重与数据都不暴露给平台）、
跨机构联合计算。

**限制与代价**：

- 内存加密 + 完整性有开销，通常个位数到约 10%，随负载波动；
- 许多优化被禁用或受限：KSM、balloon、部分设备直通、早期 live migration；
- guest 需要配合（`#VC`/`TDVMCALL` 驱动、SEV/TDX-aware 内核与固件）；
- 落地依赖**证明基础设施**（attestation service / KBS），否则"加密"无法被对端采信。

生态上，AMD SNP 与 Intel TDX 都已和 Kata Containers / Confidential Containers（CoCo）
打通，形成"机密容器"路径。

## 12.3 GPU 虚拟化（回顾与补充）

| 技术 | 厂商 | 一卡多机 | 隔离级别 | 典型场景 |
| --- | --- | --- | --- | --- |
| VFIO 整卡直通 | 通用 | 否 | 硬件（IOMMU） | 单租户 AI 训练、工作站 |
| NVIDIA vGPU | NVIDIA | 是（mdev） | 硬件分片 | VDI、云桌面、推理 |
| NVIDIA MIG | NVIDIA | 是（硬分片） | 硬件隔离 | A100/H100 多租户推理 |
| SR-IOV | Intel/NVIDIA 等 | 是（VF） | 硬件隔离 | 高密度推理、云游戏 |
| GVT-g | Intel | 是 | 半虚拟化 | 已逐步被 SR-IOV 取代 |
| virtio-gpu + venus | 开源 | 是（共享渲染） | 软件 | 云桌面 3D，**不支持 CUDA** |

补充要点：

- **VFIO 整卡直通**依赖 IOMMU 分组，GPU 往往和音频/USB 控制器同组，需要 ACS 拆分或整组直通；
- 消费级 NVIDIA 直通常见 **Error 43**，需要 `kvm=off` 隐藏虚拟化标志，必要时提供 `romfile`；
- **vGPU** 走 mediated device（`mdev`）：宿主机开 vGPU 模式、装 vGPU host 驱动，guest 装对应
  guest 驱动，常见类型形如 `nvidia-256`，有授权成本；
- **MIG** 是硬件硬分片，隔离强、无需 vGPU，但配置粒度受 GPU 型号限制；
- **SR-IOV** 性能最好，需 GPU 与平台同时支持，配置 VF 与普通网卡类似；
- **virtio-gpu/venus** 只解决渲染共享，AI/CUDA 场景不适用。

## 12.4 云原生：KubeVirt

### 12.4.1 一句话定位

KubeVirt 让 KVM 虚拟机成为 Kubernetes 的一等公民：用 **Pod 承载 virt-launcher + QEMU**，
用 **CRD 描述 VM**，从而把 VM 和容器放进同一套调度、网络、存储与运维体系。

### 12.4.2 架构与组件

```text
kubectl / API
      │
┌─────┴────────────────── Kubernetes 控制面 ──────────────────┐
│  virt-api      ：API 聚合、admission webhook、console/VNC 子资源 │
│  virt-controller：监听 VM/VMI，创建/更新 virt-launcher Pod、迁移  │
│  virt-operator ：部署与升级上面这些控制面组件                    │
└─────────────────────┬───────────────────────────────────────┘
                      │ 创建 virt-launcher Pod
┌─────────────────────▼───────────────────────────────────────┐
│  Node                                                        │
│   virt-handler（DaemonSet，节点代理）                          │
│    └─ virt-launcher Pod                                       │
│         ├─ libvirtd / virtqemud（unix socket）                 │
│         └─ qemu-system-x86_64（= 这台 VMI 的进程）             │
└──────────────────────────────────────────────────────────────┘
```

| 组件 | 类型 | 职责 |
| --- | --- | --- |
| virt-operator | Deployment | 部署/升级/生命周期管理控制面组件 |
| virt-api | Deployment | API 入口、准入校验、子资源（console、vnc、migration） |
| virt-controller | Deployment | VM/VMI → Pod 的编排，迁移协调 |
| virt-handler | DaemonSet | 节点级代理，驱动 virt-launcher，上报能力 |
| virt-launcher | 每 VMI 一个 Pod | 运行 libvirt + QEMU，生命周期与 Pod 绑定 |

关键 CRD：

| CRD | 类比 | 说明 |
| --- | --- | --- |
| VirtualMachine（VM） | Deployment | 期望状态（模板、running、runStrategy） |
| VirtualMachineInstance（VMI） | Pod | 运行中的实例，调度与生命周期的实际单位 |
| VirtualMachineInstanceReplicaSet | ReplicaSet | VMI 副本集 |
| VirtualMachineInstanceMigration | Job | 触发 live migration |
| VirtualMachineSnapshot | — | 快照 |
| VirtualMachineExport | — | 导出镜像/卷 |

> **VM 与 VMI 的关系**：VM 是"我要一直有这台机器"，VMI 是"此刻正在跑的这一份"。
> `running: true` 的 VM 被控制器转成一个 VMI，VMI 再变成一个 virt-launcher Pod。

### 12.4.3 存储与网络

- **containerDisk**：把磁盘镜像打进 OCI 镜像，随 registry 分发；virt-launcher 拉取后挂给 QEMU。
  优点是分发简单、启动快；缺点是**镜像层不适合承载大量持久数据**，且实例销毁后数据不留存。
- **DataVolume（CDI）**：把镜像/URL/PVC 导入为 PVC，适合需要持久化的场景。
- 网络支持 **masquerade**（默认 NAT）、**bridge**、**SR-IOV**、Multus 多网卡；
  较新版本可用 `passt` 做用户态网络。
- 节点需要打标签 `kubevirt.io/schedulable=true`，并确保 `/dev/kvm` 可用；
  `virt-handler` 会通过 node-labeller 把 CPU 特性暴露为节点标签，供调度与 CPU 模型匹配。

```yaml
apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: vm1
spec:
  running: true
  template:
    spec:
      domain:
        cpu: {cores: 2}
        resources:
          requests: {memory: 2Gi}
        devices:
          disks:
            - name: rootdisk
              disk: {bus: virtio}
      volumes:
        - name: rootdisk
          containerDisk: {image: quay.io/kubevirt/fedora-cloud-container-disk-demo}
```

适用：既有容器又有 VM 的混合平台，统一调度与运维。

### 12.4.4 与传统 OpenStack 的边界

| 维度 | KubeVirt | OpenStack |
| --- | --- | --- |
| 编排基础 | Kubernetes | Nova/Neutron/Cinder 等 |
| 调度粒度 | Pod/VMI（云原生调度器） | 实例（Nova scheduler） |
| 网络 | CNI/Multus/SR-IOV | Neutron（OVS/Linux bridge） |
| 存储 | PVC/CDI | Cinder |
| 优势 | 与容器同平台、CI 友好、声明式 | 功能全、生态成熟、多租户完善 |
| 劣势 | 大规模 VM 管理经验较少 | 组件重、与容器割裂 |

## 12.5 轻量 VMM

### 12.5.1 动机

QEMU 是"通用大而全"：支持几乎所有设备、几乎所有架构。但 serverless / 云原生场景
只需要极少设备，QEMU 的**设备模型数量和代码量本身就是攻击面与启动延迟**。
轻量 VMM 的共同思路是：**砍掉用不到的设备模型，只保留 virtio，缩小攻击面、加快启动。**

| 项目 | 语言 | 设备模型 | 启动 | 迁移 | 特点 | 典型用户 |
| --- | --- | --- | --- | --- | --- | --- |
| Firecracker | Rust | 仅 virtio-mmio | ~毫秒级 | 不支持 | jailer 沙箱、API socket | AWS Lambda/Fargate |
| Cloud Hypervisor | Rust | virtio-pci、ACPI | 快 | 支持 | 热插拔、vhost-user、TDX/SEV | 云原生、Intel |
| crosvm | Rust | virtio | 快 | 有限 | 设备进程沙箱、GPU/Wayland | ChromeOS/Android |
| rust-vmm | Rust | 组件库 | — | — | 上述项目的共享底座 | 生态基础 |
| QEMU `-M microvm` | C | virtio-mmio | 较快 | 支持 | 在 QEMU 内体验"轻量" | 实验/过渡 |

### 12.5.2 各自要点

- **Firecracker**：只实现 virtio-net/block/vsock/balloon，**无 PCI、无 BIOS**，用极简内核；
  一个 microVM 通常只跑一个 workload；配 **jailer**（cgroup/namespace/seccomp）做隔离；
  通过 Unix socket 暴露 REST API。不支持 live migration。
- **Cloud Hypervisor**：基于 rust-vmm，支持 PCI、ACPI、热插拔、vhost-user、迁移，
  并已支持 TDX/SEV 机密计算，定位是"轻量但完整"。
- **crosvm**：ChromeOS 的 VMM，强调设备进程沙箱化与图形（Wayland/GPU）支持。
- **rust-vmm**：把 virtio 队列、`kvm-ioctls`、`vm-memory`、`linux-loader` 等抽成共享 crate，
  避免每个 VMM 重造轮子。
- **QEMU microvm**：`qemu-system-x86_64 -M microvm` 能在熟悉的 QEMU 里体验无 PCI 的极简机型，
  适合做对照实验。

## 12.6 与容器的融合

| 方案 | 隔离边界 | 兼容性 | 启动 | 开销 | 适用 |
| --- | --- | --- | --- | --- | --- |
| runc（普通容器） | namespace/cgroup | 最高 | 极快 | 最低 | 可信多租户内、同内核 |
| Kata Containers | 每 Pod 一个轻量 VM | 高（OCI 接口） | 较快 | 中 | 强隔离 + 容器体验 |
| gVisor | 用户态内核拦截 syscall | 中（syscall 覆盖有缺口） | 快 | 中 | 不可信代码、沙箱 |
| Firecracker + containerd | microVM | 高 | 毫秒级 | 低 | FaaS、多租户函数 |

- **Kata Containers**：对上暴露 OCI runtime（`kata-runtime`）+ containerd/CRI-O shim，
  对下用 QEMU/Cloud Hypervisor/Firecracker 起一个轻量 VM，guest 里跑 `kata-agent`，
  通过 virtio/vsock 通信。**容器接口 + VM 隔离**。
- **gVisor**：`runsc` 实现一个用户态内核（Sentry）来解释 Linux syscall，
  **不是 VM**，但对不可信负载有很强隔离；syscall 兼容性是主要风险。
- **Firecracker + containerd**：AWS Fargate 的实现路径，微秒级隔离边界、毫秒级启动。

选择逻辑：

```text
需要异构 OS / 强隔离 / GPU 直通   → KVM
需要极致密度 / 快速启动            → 容器
两者都要                          → Kata / Firecracker / KubeVirt
```

## 12.7 实时与确定性

实时的本质是**确定性**：关心的不是平均延迟，而是**最坏情况延迟（worst-case latency）**。

- **PREEMPT_RT**：让内核几乎完全可抢占，把最坏延迟压到几十微秒量级；
  相关能力已并入 Linux 主线（6.12 起），发行版可能单独提供 `kernel-rt` 包。
- KVM 上的实时 = **PREEMPT_RT 宿主 + 隔离核 + 绑核 + 锁内存**，缺一不可。

调优清单：

| 项目 | 做法 | 作用 |
| --- | --- | --- |
| 内核抢占 | 用 PREEMPT_RT 内核 | 降低宿主内核引入的抖动 |
| CPU 隔离 | `isolcpus`、`nohz_full`、`rcu_nocbs` | 让隔离核不被普通任务/时钟中断打扰 |
| 绑核 | `virsh vcpupin`、`emulatorpin`、`iothreadpin` | vCPU/模拟器/I/O 线程固定到隔离核 |
| 锁内存 | `<memoryBacking><locked/></memoryBacking>` | 避免缺页导致延迟尖峰 |
| 大页 | HugePages 1G/2M | 减少 TLB miss 与页表开销 |
| 关噪声 | 禁 KSM、禁 balloon、禁自动迁移 | 去掉周期性扫描/回收抖动 |
| 调度 | `chrt` 给 vCPU 线程 SCHED_FIFO | 保证优先级 |
| 测量 | `cyclictest`（rt-tests） | 量化最坏延迟 |

抖动来源要心里有数：

| 来源 | 说明 |
| --- | --- |
| VM-Exit | I/O、特权操作、中断注入 |
| 宿主中断/软中断 | 未隔离核上的网络、时钟 |
| SMI | 固件系统管理模式，hypervisor 不可见 |
| 缺页 / balloon / KSM | 内存路径上的不确定开销 |
| 频率调节 | CPU 调频引入延迟变化 |
| live migration | 迁移期间延迟不可控 |

> 天花板：**完全硬实时（几微秒级、确定性 jitter）通常不用虚拟化**，而是裸机或专用 RTOS。
> KVM + PREEMPT_RT 适合软实时/工业控制等"几十微秒可接受"的场景。

## 12.8 生态地图

```text
        编排层    OpenStack | oVirt | Proxmox | Kubernetes/KubeVirt | Kata/CoCo
          │
        管理层    libvirt | KubeVirt virt-controller | CloudStack
          │
        执行层    QEMU  |  Cloud Hypervisor  |  Firecracker  |  crosvm
          │
        运行时    rust-vmm 组件库 | vhost/vhost-user | DPDK/SPDK
          │
        内核层    KVM (+ vhost, vfio, iommu, vfio-user)
          │
        硬件层    VT-x/AMD-V, EPT/NPT, VT-d/AMD-Vi, SEV/TDX, SR-IOV, GPU
```

## 12.9 选型决策树

```text
需求起点
  │
  ├─ 是否要求"平台方也读不到数据"？ ──是─→ 机密计算
  │        ├─ AMD 平台 → SEV-SNP（配合 CoCo/Kata）
  │        └─ Intel 平台 → TDX
  │
  ├─ 是否需要异构 OS / 强隔离 / GPU 直通？ ──是─→ KVM
  │        ├─ 要编排/多租户 → KubeVirt / OpenStack
  │        └─ 单机管理 → libvirt + QEMU
  │
  ├─ 是否 serverless / 极致启动与密度？ ──是─→ Firecracker
  │
  ├─ 是否要"轻量但完整"（热插拔/迁移/机密）？ ──是─→ Cloud Hypervisor
  │
  ├─ 是否容器接口 + VM 隔离？ ──是─→ Kata Containers
  │
  ├─ 是否不可信代码沙箱（不要求 VM）？ ──是─→ gVisor
  │
  └─ 是否软实时/工业控制？ ──是─→ PREEMPT_RT 宿主 + 隔离核 + 绑核
```

---

## 动手实验

**实验 12-1：开启嵌套并跑 L2 VM**

```bash
cat /sys/module/kvm_intel/parameters/nested
# 若为 N，按 12.1 开启后重载模块或重启
# 在 guest 内再装 KVM，跑一台 L2 VM，观察性能
```

要点：L1 内先确认 `grep -oE 'vmx|svm' /proc/cpuinfo` 有输出、`/dev/kvm` 存在，
再用 host-passthrough 起 L2；对比 L1 与 L2 上同一个 `stress-ng`/`fio` 的差异。

**实验 12-2：检查机密计算能力**

```bash
dmesg | grep -iE 'sev|tdx'
ls /sys/module/kvm_amd/parameters/ 2>/dev/null
ls -l /dev/sev* 2>/dev/null
```

若能起 SEV guest，用 QMP 查询能力：

```bash
# 在 QEMU monitor 中
{ "execute": "query-sev-capabilities" }
```

**实验 12-3：观察嵌套退出的放大效应**

```bash
# L0 上开一个终端实时看退出
sudo kvm_stat
# 分别在 L1 与 L2 内跑 fio / stress-ng，比较 exit_* 计数与分布
```

**实验 12-4（选做）：体验 Firecracker**

```bash
# 下载 firecracker + 官方 microVM 内核 + rootfs，跑一个 microVM
# 观察启动时间（毫秒级）与内存占用
```

**实验 12-5（选做）：用 cyclictest 量化抖动**

```bash
sudo dnf install -y rt-tests
sudo cyclictest -m -p 90 -i 200 -l 100000 -h 400
# 在隔离核与未隔离核上分别跑，对比 Max 值
```

---

## 排错指南

| 现象 | 根因 | 定位命令 | 处理 |
| --- | --- | --- | --- |
| guest 内无 `/dev/kvm` / 无 `vmx` | L0 未开嵌套或 CPU 未暴露 | L0：`cat /sys/module/kvm_intel/parameters/nested`；guest：`grep -oE 'vmx\|svm' /proc/cpuinfo` | 开 `nested=1`，guest 用 `host-passthrough` |
| L1 里 `kvm_intel` 加载失败 | guest 内核缺模块或未装 qemu-kvm | guest：`lsmod \| grep kvm`、`dmesg` | 安装 KVM 相关包并 `modprobe kvm_intel` |
| L2 极慢 | 未启用 nested EPT / 退出密集 | L0：`kvm_stat`；`perf kvm stat live` | 确认 `ept=1`，L1 用透传，L2 用 virtio |
| SEV 启动报 `Unable to get SEV info` | 固件未开或 `kvm_amd sev=0` | `dmesg \| grep -i sev`、`cat /sys/module/kvm_amd/parameters/sev` | BIOS 开 SME/SEV，加载 `kvm_amd sev=1` |
| SEV guest 卡在 OVMF | 用了非 SEV-aware OVMF 或 cbitpos 错 | QMP `query-sev-capabilities` | 换 SEV 版 OVMF，修正 cbitpos/reduced-phys-bits |
| TDX VM 起不来 | 无 TDX 硬件/固件/内核 | `dmesg \| grep -i tdx`、`cat /sys/module/kvm_intel/parameters/tdx` | 换 SPR+ 平台，开 TDX，升级内核/QEMU |
| KubeVirt VM 一直 Pending | 节点未打标签或无 KVM | `kubectl describe vmi vm1`、`kubectl get nodes --show-labels` | 打 `kubevirt.io/schedulable=true`，确认 `/dev/kvm` |
| KubeVirt VMI 起不来 | virt-handler 异常 / 组件版本不匹配 | `kubectl -n kubevirt get pods`、`kubectl logs ds/virt-handler` | 修复组件，核对 virt-operator 升级 |
| containerDisk 拉取慢/失败 | 镜像大或 registry 限速 | `kubectl describe pod virt-launcher-vm1-xxxx`、`crictl images` | 预热镜像，或改用 DataVolume/PVC |
| GPU 直通后 guest 无输出 | 缺 VBIOS / 驱动问题 / Error 43 | 宿主 `dmesg`；guest `lspci -k` | 提供 `romfile`，加 `kvm=off`，用 vfio-pci 绑定 |
| vGPU 无法创建 mdev | 未切 vGPU 模式 / 驱动不匹配 | `nvidia-smi vgpu`、`ls /sys/bus/pci/devices/*/mdev_supported_types` | 切 vGPU 模式，匹配 host/guest 驱动 |
| Firecracker 启动慢或失败 | 内核缺 virtio-mmio / rootfs 不匹配 | 查看 firecracker 日志与 API 返回 | 用官方 microVM 内核与 rootfs |
| Kata Pod 启动慢 | VM 启动 + guest 内核开销 | `crictl stats`、containerd 日志 | 换 Cloud Hypervisor/Firecracker，预热镜像 |
| 实时 guest 抖动大 | 未绑核/未隔离/未锁内存 | `cyclictest`、宿主 `top` 看 steal | `isolcpus`+`nohz_full`、vcpupin、hugepages、禁 KSM/balloon |
| 机密 VM 无法 live migrate | 缺 SEV/TDX 迁移支持 | `virsh domjobinfo vm1` | 用支持迁移的 SNP/TD 固件与 QEMU，或冷迁移 |

### 案例复盘 A：云上 CI 里 L2 拿不到 `/dev/kvm`

某团队把 CI runner 放在云主机（L1）里，想在流水线中跑 Kata 做集成测试，结果 runner
容器里始终没有 `/dev/kvm`。排查顺序：先在 L1 里 `grep -oE 'vmx' /proc/cpuinfo`——无输出；
再看 L1 的 libvirt 配置，用的是 `host-model` 且没有显式 `require vmx`，CPU 特性被裁掉。
根因有两层：**L0 的 nested 未开**（老内核默认 N），且 **L1 没暴露 vmx**。
处理：L0 开 `nested=1` 并重载模块，L1 改用 `host-passthrough`。重启后 L1 内出现
`vmx` 与 `/dev/kvm`，L2 正常启动。教训：嵌套问题永远是"先看 L0，再看 L1 的 CPU 特性"。

### 案例复盘 B：SEV guest 卡在 OVMF 不动

在 EPYC 平台上按文档起了 SEV guest，QEMU 不报错，但 guest 一直停在 OVMF 界面。
先 `dmesg | grep -i sev` 确认宿主 SEV 已启用；再用 QMP `query-sev-capabilities`
发现实际 `cbitpos` 与配置不一致，且 OVMF 用的是普通版而非 SEV 版。
根因：**cbitpos/reduced-phys-bits 与平台不匹配 + 固件不是 SEV-aware**。
处理：按 `query-sev-capabilities` 的返回值修正参数，换成 SEV 版 OVMF，
guest 顺利进入。教训：SEV 的参数是"平台相关"的，不要照抄别人的 `51/1`。

### 案例复盘 C：KubeVirt 节点上的 VM 永远 Pending

集群装好 KubeVirt 后，`VirtualMachine` 创建成功但 `VMI` 一直 Pending。
`kubectl describe vmi` 的事件提示无可调度节点。`kubectl get nodes --show-labels`
发现节点没有 `kubevirt.io/schedulable=true` 标签，`virt-handler` Pod 也未就绪。
根因：**节点未标记可调度 + `/dev/kvm` 不可用**。处理：确认宿主 KVM 正常后，
给节点打上标签并修复 virt-handler。教训：KubeVirt 把"节点是否具备虚拟化能力"
显式建模成了标签与 DaemonSet 健康状态，调度失败先查这两个。

### 案例复盘 D：实时 guest 的 `cyclictest` 最坏延迟飙到毫秒级

一套基于 KVM 的软实时采集系统，平均延迟很好，但 `cyclictest` 的 Max 偶尔到毫秒级。
排查：宿主 `top` 看到隔离核上仍有 kworker/中断；guest 的 vCPU 线程没绑到隔离核；
`memoryBacking` 没锁内存；KSM 仍在运行。根因：**隔离不彻底 + 缺页 + 后台扫描**。
处理：内核加 `isolcpus`/`nohz_full`/`rcu_nocbs`，`vcpupin`+`emulatorpin`+`iothreadpin`
固定线程，加 `<locked/>` 与 1G 大页，关闭 KSM 与 balloon。处理后 Max 降到几十微秒。
教训：实时优化是"一票否决"——任何一处噪声都会毁掉最坏延迟。

---

## 练习

1. 什么业务场景值得上机密计算？把"加密开销 + 证明基础设施 + 特性限制"折算成成本收益。
2. 画出一次 L2 guest 的磁盘读所经过的完整路径，标出每个可能产生 VM-Exit 的位置。
3. 对比 KubeVirt 与传统 OpenStack 的适用边界，各举一个非它不可的场景。
4. 为"多租户 AI 推理平台"选一套技术栈（隔离、GPU、编排、机密性），逐项说明理由。
5. Firecracker 为什么不支持 live migration？这对你的选型意味着什么？
6. 设计一份"KVM 软实时"的调优清单，并说明每一项针对哪种抖动来源。

## 延伸阅读

- AMD SEV：<https://www.amd.com/en/developer/sev.html>
- Intel TDX：<https://www.intel.com/content/www/us/en/developer/tools/trust-domain-extensions/overview.html>
- KubeVirt：<https://kubevirt.io/>
- Firecracker：<https://firecracker-microvm.github.io/>
- Cloud Hypervisor：<https://www.cloudhypervisor.org/>
- Kata Containers：<https://katacontainers.io/>
- Confidential Containers：<https://confidentialcontainers.org/>
- rust-vmm：<https://github.com/rust-vmm>
- 内核源码：`arch/x86/kvm/vmx/nested.c`、`arch/x86/kvm/svm/nested.c`
- 内核文档：`Documentation/virt/kvm/`（含 nested、TDX、SEV 专题）
