# 模块 05 · 存储虚拟化

> 存储是虚拟化里最容易"性能差、空间爆、数据丢"的一层。这一模块把 qcow2 的内部
> 结构、cache/io 模式、后备链、精简置备讲透，再给一套可实测的对比方法。

## 学习目标

- 讲清 qcow2 的内部结构：L1/L2 表、refcount 表、cluster 与 COW；
- 区分镜像格式（raw / qcow2 / vmdk）与存储后端（本地 / LVM / Ceph / NFS）；
- 理解 cache 模式（page cache、O_DIRECT、flush 语义）与 io 模式（threads/native/io_uring）；
- 会做镜像转换、扩容、check、快照、后备链收敛与精简置备回收；
- 掌握 `qemu-img` 子命令，能用 `fio` 量化不同 cache/io 的差异。

---

## 5.1 镜像格式与 qcow2 内部结构

| 格式 | 特点 | 适用 |
| --- | --- | --- |
| **raw** | 裸块，性能最好，不支持快照/精简置备 | 固定大小、高性能盘 |
| **qcow2** | 支持快照、压缩、加密、精简置备、后备链 | 默认首选 |
| vmdk/vhd | 兼容其他平台 | 迁移/导入 |

### qcow2 的簇与两级映射

qcow2 把文件切成固定大小的 **cluster**（默认 64 KiB，即 `cluster_bits=16`）。
客户机看到的是连续块地址，qcow2 用**两级页表**把它翻译成文件内的偏移：

```text
客户机读 LBA ──► 簇号 = LBA / cluster_size
                     │
                     ▼
        ┌───────────────────────────────┐ offset 0
        │ Header（magic: QFI\xfb）        │
        ├───────────────────────────────┤
        │ L1 表：簇号高位 ──► L2 表位置    │
        ├───────────────────────────────┤
        │ Refcount 表 + Refcount 块       │
        ├───────────────────────────────┤
        │ 快照表（可选）                  │
        ├───────────────────────────────┤
        │ L2 表：簇号低位 ──► 数据簇偏移   │
        ├───────────────────────────────┤
        │ 数据簇 …                        │
        └───────────────────────────────┘
```

- **L1 表**：索引高位，每项指向一个 L2 表所在的簇。
- **L2 表**：索引低位，每项指向数据簇；未分配的项为 0，表示"空洞"（读回全零，
  或从 backing file 取，见 5.5）。这正是**精简置备**的来源：没写过的簇不占空间。
- **refcount 表 / refcount 块**：记录每个簇被引用多少次。快照和后备链会让簇被
  多个逻辑位置共享；写入共享簇时要 **COW**（复制到新簇再改，引用计数减一），
  这也是快照后写性能下降的根因。

### Header 关键字段

| 字段 | 含义 |
| --- | --- |
| `magic` | 固定 `QFI\xfb`，识别 qcow2 |
| `version` | 2 或 3（v3 支持 `lazy_refcounts`、`compat=1.1` 等新特性） |
| `cluster_bits` | 簇大小 = `1 << cluster_bits`，默认 16（64 KiB） |
| `size` | 虚拟磁盘大小（客户机看到的容量） |
| `backing_file_offset` | 后备文件路径位置（有则非零） |
| `l1_size` / `l1_table_offset` | L1 表大小与位置 |
| `refcount_table_offset` | refcount 表位置 |
| `nb_snapshots` / `snapshots_offset` | 内部快照数量与位置 |
| `incompatible_features` | 不兼容特性位（如加密、脏位图） |

创建时可调的关键选项：

```bash
# 更大簇：减少元数据、提升顺序吞吐，但放大随机写放大
qemu-img create -f qcow2 -o cluster_size=1M disk.qcow2 20G

# 预分配：metadata 只预留元数据；falloc 预占空间；full 全量置零（失去精简置备）
qemu-img create -f qcow2 -o preallocation=metadata disk.qcow2 20G

# v3 新特性（默认 compat=1.1），并延迟 refcount 更新以提速
qemu-img create -f qcow2 -o compat=1.1,lazy_refcounts=on disk.qcow2 20G
```

> `lazy_refcounts` 用崩溃一致性换取写入性能：异常掉电后需要 `qemu-img check`
> 修复 refcount。生产是否开启要结合后端是否可靠。

### 完整性检查

```bash
# 只读检查：报告泄漏簇、引用计数错误、损坏的 L2
qemu-img check disk.qcow2

# 尝试修复（-r all 修所有可修项，可能丢少量数据）
qemu-img check -r all disk.qcow2

# 查看分配情况：哪些区间已分配、是否来自 backing file
qemu-img map --output=json disk.qcow2
qemu-img map --output=json disk.qcow2 | head
```

```bash
# 创建
qemu-img create -f qcow2 disk.qcow2 20G        # 精简置备，实际占用很小
qemu-img create -f raw disk.raw 20G            # 立即占 20G

# 查看
qemu-img info disk.qcow2
qemu-img info --backing-chain disk.qcow2       # 看后备链

# 转换
qemu-img convert -f qcow2 -O raw disk.qcow2 disk.raw
qemu-img convert -O qcow2 -c disk.raw disk.qcow2   # -c 压缩

# 扩容（只扩不缩）
qemu-img resize disk.qcow2 +10G
# 之后进 guest 扩容分区和文件系统
```

## 5.2 存储后端

| 后端 | 说明 | 适用场景 |
| --- | --- | --- |
| 本地目录 | `/var/lib/libvirt/images` | 单机、开发 |
| LVM 逻辑卷 | 块设备直接给 guest，性能好 | 需要性能的单机 |
| LVM thin | 精简池，支持快照与超分 | 单机多盘、快照 |
| NFS / iSCSI | 网络共享存储 | 迁移、小集群 |
| Ceph RBD | 分布式块存储 | 云平台、超融合 |
| 本地文件 + 后备链 | 链接克隆、模板 | 快速批量部署 |

```bash
# LVM 卷作为磁盘
virsh vol-create-as mylvm vm1-disk 20G --format raw
# 在 XML 中：<disk type='block'><source dev='/dev/vg0/vm1-disk'/>...

# Ceph RBD（需 libvirt 配置认证）
# <disk type='network'><source protocol='rbd' name='rbd/vm1'/>
#   <host name='ceph-mon1' port='6789'/></source>...
```

### LVM thin：单机版的精简置备

LVM thin pool 在块层实现精简置备与快照，比 qcow2 少一层文件系统开销。

```bash
# 建 thin pool 与 thin 卷
sudo lvcreate --type thin-pool -L 200G -n vg0/thinpool vg0
sudo lvcreate --type thin -V 50G --thinpool vg0/thinpool -n vm1-disk

# 观察实际占用与超分
sudo lvs -a -o +lv_size,data_percent,metadata_percent vg0

# thin 快照（块级快照，可作外部快照后端）
sudo lvcreate -s vg0/vm1-disk -n vm1-snap
```

> 回收要两层透传：guest `fstrim` → 卷层 `discard` → thin pool 释放。LVM 还需
> `/etc/lvm/lvm.conf` 里 `issue_discards = 1`，否则物理盘不会真正释放。

### Ceph RBD 与 NFS / iSCSI

```bash
# Ceph：创建镜像、看差异、做快照、拉平
rbd create vms/web01 --size 20480
rbd diff vms/web01 | head
rbd snap create vms/web01@snap1
rbd flatten vms/web01

# iSCSI：发现与登录
sudo iscsiadm -m discovery -t sendtargets -p 10.0.0.10
sudo iscsiadm -m node -T iqn.2026-01.com.example:storage -l

# NFS 挂载点作为 netfs 池
sudo mount -t nfs nfs.example.com:/export/vms /mnt/nfs
virsh pool-define-as nfspool netfs --source-host nfs.example.com \
  --source-path /export/vms --target /mnt/nfs
```

> Ceph RBD 默认精简置备且自带快照，`rbd flatten` 会断开对父镜像的依赖，
> 但会占用完整空间——这是 RBD 层的"blockpull"。

## 5.3 cache 模式（性能 vs 安全的核心）

QEMU 的 `cache=` 参数控制 guest 写操作如何到达物理盘。要理解它，先记住两层缓存：

```text
guest 页缓存 ──(virtio)──► QEMU ──► 宿主机 page cache ──► 物理盘缓存 ──► 盘片
                                      │
                          cache=writeback/writethrough 经过这层
                          cache=none/directsync 用 O_DIRECT 绕过
```

| 模式 | 行为 | 数据安全 | 性能 |
| --- | --- | --- | --- |
| `none` | 绕过宿主机页缓存，直接落盘（配合 `io=native` 用 O_DIRECT） | 高 | 高 |
| `writeback` | 写入宿主机页缓存，异步刷盘 | 断电可能丢数据 | 最高 |
| `writethrough` | 同时写缓存和盘 | 高 | 较低 |
| `directsync` | 绕过缓存且同步 | 最高 | 最低 |
| `unsafe` | 完全忽略 flush | 最危险 | 最快（仅测试） |

**关键机制：flush 语义**

- guest 文件系统提交事务时会发 **FLUSH**。QEMU 收到后对后端做 `fsync`/`fdatasync`。
- `cache=none`：写走 O_DIRECT，FLUSH 仍被尊重，数据安全性由"盘自身的易失缓存"
  决定；企业盘要开 write cache 保护或禁用盘缓存。
- `cache=writeback`：数据先进宿主机 page cache，宿主断电且未刷盘就丢。
- `cache=unsafe`：连 FLUSH 都忽略，性能最高但断电几乎必丢/损。

**生产常用**：`cache=none,io=native`（本地块设备）或
`cache=none,io=io_uring`（新内核）。

```xml
<driver name='qemu' type='qcow2' cache='none' io='io_uring' discard='unmap'/>
```

> `discard='unmap'` 让 guest 的 TRIM 传递到后端，SSD/精简置备场景重要。
> 对 qcow2，QEMU 用 `fallocate(PUNCH_HOLE)` 打洞释放空间。

> 重要细节：`cache=none` 与 `io=native` 都要求 O_DIRECT。有些文件系统或后端
> （如部分 NFS、overlayfs、某些加密层）不支持 O_DIRECT，QEMU 会退回 buffered
> 模式，此时 `cache=none` 的语义会被削弱——排错时要用
> `virsh qemu-monitor-command <d> --hmp 'info block'` 确认实际打开方式。

## 5.4 io 模式

`io=` 决定 QEMU 用什么异步 I/O 引擎提交请求，与 `cache=` 正交但互相约束。

| io | 说明 | 约束 |
| --- | --- | --- |
| `threads` | 用线程池做异步 I/O（默认，兼容好） | 任意 cache 模式 |
| `native` | 用 Linux AIO（`io_submit`），性能好 | **仅对 O_DIRECT 有效**，需 `cache=none/directsync` |
| `io_uring` | 新内核的高性能异步 I/O，推荐 | 需较新内核与 QEMU；支持 buffered 与 O_DIRECT |

```text
io=threads ：QEMU 线程池 ──► pread/pwrite ──► 内核
io=native  ：QEMU ──► io_submit ──► Linux AIO ──► 内核（O_DIRECT）
io=io_uring：QEMU ──► io_uring 提交/完成队列 ──► 内核（共享环，低开销）
```

> 选型：现代内核 + QEMU 优先 `io_uring`；老环境用 `io=native + cache=none`；
> 不支持 O_DIRECT 的后端（NFS/部分网络存储）只能用 `io=threads`。

## 5.5 快照与后备链

qcow2 支持**后备文件（backing file）**：新盘只存差异，读取时层层回溯。

```bash
# 手动创建后备链
qemu-img create -f qcow2 -b base.qcow2 -F qcow2 overlay1.qcow2

# 查看链
qemu-img info --backing-chain overlay1.qcow2

# 合并（把 overlay 内容合并进 base）
qemu-img commit overlay1.qcow2
```

```text
读路径（未命中时逐层回溯）：
guest 读 ──► active.qcow2 ──未分配──► overlay1.qcow2 ──未分配──► base.qcow2
              │命中                          │命中
              ▼                              ▼
           返回数据                       返回数据
```

链越长，未命中路径越长、元数据查找越多，随机 I/O 越差。`blockcommit` 把上层差异
合并进下层，`blockpull` 把下层数据拉进上层（链变平）。

```bash
# 用 libvirt 管理链（推荐）
virsh blockcommit web01 vda --active --pivot --verbose   # 合并回 base
virsh blockpull web01 vda --wait --verbose               # 拉平 overlay
virsh blockjob web01 vda --info

# qemu-img 层面对等操作
qemu-img commit overlay1.qcow2            # 相当于 commit 进 base
qemu-img rebase -b newbase.qcow2 -F qcow2 -u overlay1.qcow2  # 换 base（-u 不安全模式）
qemu-img rebase -b newbase.qcow2 overlay1.qcow2              # 安全模式会复制数据
```

**风险**：链越长性能越差，且删除中间层会损坏整条链。生产上用 libvirt 管理，
并定期 `blockcommit` 收敛。

> `qemu-img rebase -u`（unsafe）只改指向不搬数据，快但要求新旧 base 内容兼容；
> 不带 `-u` 的 rebase 会把数据复制过去，慢但安全。别在生产上随手 `-u`。

## 5.6 存储池（libvirt 视角）

```bash
virsh pool-list --all
virsh pool-define-as pool_dir dir --target /data/vms
virsh pool-build pool_dir
virsh pool-start pool_dir
virsh pool-autostart pool_dir
virsh vol-list pool_dir
```

支持的池类型：`dir`、`logical`（LVM）、`netfs`（NFS）、`rbd`（Ceph）、`iscsi`、
`disk`、`gluster`。

```bash
# 卷的常用操作
virsh vol-create-as pool_dir disk1.qcow2 20G --format qcow2
virsh vol-clone --pool pool_dir disk1.qcow2 disk2.qcow2
virsh vol-upload --pool pool_dir disk1.qcow2 image.raw     # 上传
virsh vol-download --pool pool_dir disk1.qcow2 out.raw     # 下载
virsh vol-wipe --pool pool_dir disk1.qcow2                 # 安全擦除
virsh vol-resize --pool pool_dir disk1.qcow2 30G
```

> `pool-define-as` 只是登记，`pool-build` 建结构，`pool-start` 激活，
> `pool-autostart` 保证重启后仍在。四步缺一，重启后池消失。

## 5.7 精简置备与空间回收

- **精简置备**：qcow2 / LVM thin / Ceph 都支持，按实际用量占空间。
- **超分风险**：所有虚机声明大小之和可远超物理容量，写满会集体出错。
- **回收**：guest 删文件后需 `fstrim`，并让 `discard` 透传，宿主机才会释放。

```bash
# guest 内
sudo fstrim -av
# 宿主机检查实际占用
du -h --apparent-size disk.qcow2   # 声明大小
du -h disk.qcow2                    # 实际占用
```

```text
回收链路：
guest 文件系统删文件
   │ fstrim / 挂载选项 discard
   ▼
virtio-blk/scsi 发出 UNMAP
   │ QEMU discard=unmap
   ▼
qcow2 punch hole / LVM discard / RBD discard
   │
   ▼
物理盘真正释放
```

> 两个常见断点：① XML 没写 `discard='unmap'`；② 后端（LVM/NFS）不支持或未开
> discard 透传。只做其中一步，空间都不会真正回收。

## 5.8 qemu-img 子命令大全与实测

| 子命令 | 作用 | 典型用法 |
| --- | --- | --- |
| `create` | 创建镜像 | `qemu-img create -f qcow2 disk.qcow2 20G` |
| `info` | 查看格式/大小/后备链 | `qemu-img info --backing-chain disk.qcow2` |
| `convert` | 格式转换/压缩/加密 | `qemu-img convert -O qcow2 -c in.raw out.qcow2` |
| `resize` | 扩容/缩容（缩有风险） | `qemu-img resize disk.qcow2 +10G` |
| `check` | 检查并修复一致性 | `qemu-img check -r all disk.qcow2` |
| `commit` | 把本层差异合并到下层 | `qemu-img commit overlay.qcow2` |
| `rebase` | 更换 backing file | `qemu-img rebase -b new.qcow2 disk.qcow2` |
| `map` | 查看已分配区间 | `qemu-img map --output=json disk.qcow2` |
| `snapshot` | 内部快照增删列切 | `qemu-img snapshot -c s1 disk.qcow2` |
| `amend` | 修改镜像选项 | `qemu-img amend -o compat=1.1 disk.qcow2` |
| `compare` | 比较两个镜像 | `qemu-img compare a.qcow2 b.qcow2` |
| `measure` | 估算转换后大小 | `qemu-img measure -O qcow2 in.raw` |
| `dd` | 直接读写镜像 | `qemu-img dd if=disk.qcow2 of=out.raw bs=1M` |
| `bitmap` | 管理持久化脏位图 | `qemu-img bitmap --add disk.qcow2 bmp0` |
| `bench` | 内置基准测试 | `qemu-img bench -t none -n -c 1000 disk.qcow2` |

实测：创建 / 转换 / 扩容 / check / commit / map 一条龙。

```bash
# 1) 创建与观察精简置备
qemu-img create -f qcow2 base.qcow2 10G
ls -lh base.qcow2                       # 几十 KB
qemu-img info base.qcow2                # virtual size 10G

# 2) 建覆盖层并写差异
qemu-img create -f qcow2 -b base.qcow2 -F qcow2 overlay.qcow2
qemu-img info --backing-chain overlay.qcow2

# 3) 看分配：overlay 里哪些区间已分配
qemu-img map --output=json overlay.qcow2

# 4) 转换与压缩
qemu-img convert -f qcow2 -O qcow2 -c base.qcow2 compressed.qcow2
qemu-img compare base.qcow2 compressed.qcow2

# 5) 扩容
qemu-img resize base.qcow2 +5G

# 6) 合并与检查
qemu-img commit overlay.qcow2
qemu-img check base.qcow2

# 7) 内置基准（不落盘，只测 QEMU 路径）
qemu-img bench -t none -n -c 1000 -s 4k base.qcow2
```

## 5.9 用 fio 对比不同 cache / io

方法：在 guest 内跑 fio，用 `direct=1` 绕过 guest 页缓存，才能真正反映 QEMU 与
后端的差异；保持其他变量不变，只改 XML 的 `cache`/`io`，重启域后重复。

```bash
# guest 内
sudo dnf install -y fio

# 随机写（4K，iodepth 拉满，测 IOPS 与延迟）
fio --name=randwrite --ioengine=libaio --direct=1 --rw=randwrite --bs=4k \
    --iodepth=32 --numjobs=4 --size=1G --runtime=30 --time_based \
    --group_reporting --output-format=json --output=/tmp/fio-rw.json

# 顺序写（1M，测吞吐）
fio --name=seqwrite --ioengine=libaio --direct=1 --rw=write --bs=1M \
    --iodepth=8 --numjobs=1 --size=4G --runtime=30 --time_based \
    --group_reporting

# 混合读写（70/30）
fio --name=mix --ioengine=libaio --direct=1 --rw=randrw --rwmixread=70 \
    --bs=4k --iodepth=32 --size=1G --runtime=30 --time_based --group_reporting
```

```bash
# 快速提取关键指标
jq '.jobs[0].read.iops, .jobs[0].write.iops, .jobs[0].write.clat_ns.mean' \
   /tmp/fio-rw.json
```

对比建议：

| 变量 | 建议做法 |
| --- | --- |
| `cache` | 分别测 `none` / `writeback` / `writethrough` |
| `io` | 分别测 `threads` / `native` / `io_uring` |
| 队列深度 | 同时测 `iodepth=1`（延迟敏感）与 `iodepth=32`（吞吐） |
| 盘类型 | qcow2 vs raw vs LVM 块设备 |
| 结论 | 随机小 I/O 看 cache/io；顺序大 I/O 看簇大小与预分配 |

---

## 动手实验

**实验 5-1：格式转换与精简置备观察**

```bash
qemu-img create -f qcow2 a.qcow2 10G
ls -lh a.qcow2          # 只有几百 KB
qemu-img convert -f qcow2 -O raw a.qcow2 a.raw
ls -lh a.raw            # 10G
```

**实验 5-2：后备链与 commit**

```bash
qemu-img create -f qcow2 -b a.qcow2 -F qcow2 b.qcow2
qemu-img info --backing-chain b.qcow2
qemu-img map --output=json b.qcow2
qemu-img commit b.qcow2
```

**实验 5-3：cache 模式压测对比**

用 `fio` 在 guest 内分别测 `cache=none` 与 `cache=writeback`，记录 IOPS/延迟：

```bash
# guest 内
sudo dnf install -y fio
fio --name=randwrite --ioengine=libaio --direct=1 --rw=randwrite --bs=4k \
    --numjobs=4 --size=1G --runtime=30 --group_reporting
```

**实验 5-4：TRIM 透传**

```bash
# XML 加 discard='unmap'，guest 内
sudo fstrim -av
# 宿主机观察 qcow2 实际占用是否下降
du -h disk.qcow2
```

**实验 5-5：check 与损坏修复**
故意用 `qemu-img create -o lazy_refcounts=on` 建盘，在 guest 内大量写入时
`kill -9` QEMU 进程，再 `qemu-img check` 观察 refcount 泄漏并 `-r all` 修复。

---

## 排错指南

| 现象 | 根因 | 定位命令 | 处理 |
| --- | --- | --- | --- |
| 磁盘空间暴涨 | 精简置备写满 / 超分 | `du -h`、`lvs`、`rbd du` | 查超分比例、清理快照、扩容池 |
| 快照后性能下降 | 后备链过长 / COW 写放大 | `qemu-img info --backing-chain` | `blockcommit` 收敛 |
| `fstrim` 无效 | 未开 `discard` 或后端不透传 | `virsh dumpxml` 看 `discard` | XML 加 `discard='unmap'`，检查 LVM `issue_discards` |
| 数据损坏 | 用 `cache=unsafe` 或宿主断电 | `qemu-img check` | 生产禁用 unsafe，改用 `cache=none` |
| 扩容后 guest 看不到 | 只扩了盘，没扩分区 | `lsblk`、`growpart` | guest 内 `growpart` + `resize2fs`/`xfs_growfs` |
| `qemu-img check` 报 refcount 泄漏 | 非正常关机 / lazy_refcounts | `qemu-img check` | `qemu-img check -r all` 修复 |
| 镜像被锁无法启动 | `virtlockd` / 盘被别的域占用 | `virsh domblklist`、`fuser` | 释放占用，检查是否有残留锁文件 |
| `cache=none` 未生效 | 后端不支持 O_DIRECT | `virsh qemu-monitor-command <d> --hmp 'info block'` | 换支持 O_DIRECT 的后端，或接受 buffered |
| 随机 I/O 很慢 | 簇太小 / 未对齐 / io=threads | `qemu-img info`、fio 对比 | 调 `cluster_size`、开 `io_uring`、对齐分区 |
| LVM thin 空间不回收 | `issue_discards=0` | `lvs -o data_percent` | 改 `lvm.conf` 并重扫 |
| RBD 性能差 | 未用 librbd / 网络瓶颈 | `rbd perf image iostat` | 检查网络、调 `rbd_cache`、多路径 |
| NFS 上 qcow2 损坏 | 锁/缓存语义不匹配 | `qemu-img check`、NFS 日志 | 用 `cache=none`、确保 NFSv4、单写者 |
| 迁移时报盘格式不支持 | raw 无位图 / 未共享存储 | `virsh domblklist` | 转 qcow2、配置共享存储或块迁移 |
| `qemu-img convert` 极慢 | 单线程 / 未压缩权衡 | 观察进度 | 用 `-m` 并发、`-W` 乱序，评估 `-c` |

### 案例复盘 1：精简置备写满，整机集体卡死

现象：宿主机上 20 台 VM 声明总容量 4T，物理盘只有 1T。某天开始多台 VM 随机
卡顿、I/O 报错，`df` 显示宿主机根分区 100%。

定位：`du -sh` 各 qcow2 发现实际占用已逼近物理容量；`lvs` 显示 thin pool
`data_percent=100%`。精简置备是"按需分配"，不是"按声明预留"。

处理：紧急清理快照、删无用盘、把部分 VM 迁到别的存储；长期建立容量告警
（thin pool 80% 阈值）并限制超分比例（如 2:1）。

教训：**超分是把双刃剑**。省空间的前提是有监控和上限，否则"省"出来的就是
一次全平台故障。

### 案例复盘 2：删了"没用"的中间层，整条链崩了

现象：运维看到 `overlay1.qcow2` 很小，认为没用就 `rm` 了，结果 VM 启动报
"Could not open backing file"。

定位：`qemu-img info --backing-chain` 显示链中间缺一环。后备链是**依赖关系**，
不是并列文件；中间层承载了某些已分配簇，删了上层就找不到数据。

处理：从备份恢复该层；若没有备份，只能从别的快照/备份重建，可能丢数据。

教训：**后备链的每一层都是链的一部分**，删除前必须确认没有任何上层引用它，
正确做法是用 `blockcommit` 合并而不是 `rm`。

### 案例复盘 3：断电后文件系统损坏，元凶是 cache=unsafe

现象：测试环境为压测把盘设成 `cache=unsafe`，一次机房断电后多台 VM 文件系统
损坏，`fsck` 大量报错。

定位：`unsafe` 让 QEMU 忽略 guest 的 FLUSH，guest 认为已落盘的数据其实还在
宿主机内存里，断电即丢，且可能丢的是元数据，导致结构性损坏。

处理：生产一律 `cache=none`；测试环境要压测极限性能时，用独立临时盘并接受
数据可丢，绝不用在承载真实数据的盘上。

教训：**性能参数有语义，不是越高越好**。`unsafe` 省下的是"等待落盘"的时间，
代价是崩溃一致性。

### 案例复盘 4：SSD 上空间不回收，盘越用越满

现象：guest 内删掉 50G 文件后，宿主机 `du` 显示 qcow2 仍是原来那么大，
SSD 可用空间持续下降。

定位：`virsh dumpxml` 发现磁盘没有 `discard='unmap'`；guest 内 `fstrim` 因为
virtio 没协商 discard 而无效。即使加了 `discard`，LVM 层 `issue_discards=0`
也会截断回收。

处理：XML 加 `discard='unmap'` 并重启域；LVM 打开 `issue_discards`；
用 `fstrim -av` 验证，宿主机观察 `du` 下降。

教训：**回收是一条链，断一环就全断**。guest、virtio、QEMU、后端、物理盘
任何一层不支持，空间都不会回来。

---

## 练习

1. 设计一个"模板盘 + 链接克隆"的批量部署方案，并说明如何控制后备链长度。
2. 说明 `cache=none` 与 `cache=writeback` 在数据库场景下的取舍，给出你的选择与理由。
3. 解释后备链为什么不能随意删除中间层，并画出读路径未命中的回溯过程。
4. 用 `qemu-img` 把一个 20G qcow2 转成 raw 并压缩回 qcow2，比较三次的大小。
5. 设计一套 `fio` 对比实验，量化 `io=threads` 与 `io=io_uring` 的差异，列出控制变量。

## 延伸阅读

- `qemu-img --help`、`qemu-img <subcommand> --help`
- QEMU 文档：`docs/interop/qcow2.txt`（qcow2 格式权威定义）
- libvirt 存储管理：<https://libvirt.org/storage.html>
- Ceph RBD + libvirt：<https://docs.ceph.com/en/latest/rbd/libvirt/>
- `man fio`、`fio --enghelp`
