# 模块 08 · 性能调优

> 调优不是"抄一组参数"，而是**测量 → 定位 → 单变量验证 → 固化**的循环。
> 本模块先讲方法论，再分 CPU / 内存 / 存储 / 网络四层给手段，
> 最后教你怎么用指标判断"到底卡在哪"。

## 学习目标

- 建立可复用的调优方法论：基线、单变量、USE 方法、尾延迟视角；
- 掌握 CPU：`vcpupin` / `emulatorpin` / `iothreadpin`、NUMA 亲和、CFS 节流、
  core scheduling、`isolcpus`/tickless、SMT 的正确用法；
- 掌握内存：HugePages（2M/1G）、THP、KSM、ballooning、NUMA 绑定、prefault；
- 掌握存储：`cache` / `io` 模式、iothread、多队列、preallocation、raw vs qcow2、后端选型；
- 掌握网络：vhost、多队列、MTU/巨帧、offload、SR-IOV；
- 会做压测并解读指标：steal time、await、p99、IOPS/吞吐，知道常见数字量级。

---

## 8.1 调优方法论

### 六条铁律

1. **先测量，再优化**：没有基线数据的调优都是玄学。先记录"默认配置下的
   吞吐/延迟/资源占用"，才有对照。
2. **一次只动一个变量**：同时改三项，你永远不知道是哪一项起了作用。
3. **隔离噪声**：宿主机的其他负载、后台任务、其他 VM 都会污染结果。
   压测时用 `nice`、cgroup、甚至 `isolcpus` 把噪声隔离出去。
4. **分清瓶颈层次**：guest 内部 → 虚拟化层（KVM/virtio）→ 宿主内核 → 硬件。
   一层层排除，而不是在 guest 里瞎调。
5. **尾延迟比平均值重要**：平均值好看、p99 爆表，对数据库和在线业务毫无意义。
6. **警惕缓存假象**：第一次跑和第二次跑差别巨大，多半是页缓存/CPU 缓存
   在起作用，要 `drop_caches` 或加 `ramp_time` 得到稳态数据。

### USE 方法

对每个资源检查三项：**U**tilization（利用率）、**S**aturation（饱和度）、
**E**rrors（错误）。

| 资源 | 利用率 | 饱和度 | 错误 |
| --- | --- | --- | --- |
| CPU | `mpstat %usr/%sys` | run queue、`%steal` | dmesg MCE |
| 内存 | `free`、`/proc/meminfo` | swap、`vmstat si/so`、PSI | ECC |
| 磁盘 | `iostat %util` | `await`、队列深度 | `dmesg` I/O error |
| 网络 | `sar -n DEV` | 重传、drop | `ethtool -S` errors |

### 两个有用的公式

- **Little's Law**：并发数 = 吞吐 × 延迟。反过来，已知并发和延迟就能估吞吐。
  这也是为什么 `fio` 的 `iodepth` 与延迟直接挂钩。
- **Amdahl 定律**：只优化一部分，整体加速有上限。先找占比最大的那部分。

### 调优流程

```text
定义目标（SLO：吞吐/延迟/p99）
      │
      ▼
建立基线（空闲 + 负载）──► 记录到表格/文件，可复现
      │
      ▼
压测找瓶颈 ──► 用 USE 逐层定位
      │
      ▼
单变量改动 ──► 复测 ──► 对比基线
      │
      ├─ 有收益 → 固化到 XML/配置，写清原因
      └─ 无收益 → 回滚，记录"此路不通"
```

> 性能、隔离、可迁移性、成本四者互相牵制。任何"优化"都要问：
> 代价是什么？会不会破坏迁移？会不会牺牲安全性？

---

## 8.2 CPU 调优

### vCPU 就是宿主机线程

每个 vCPU 对应 QEMU 进程里的一个线程，由 Linux CFS 调度，运行 guest 代码时
在内核里走 `KVM_RUN`。因此**宿主机调度器的任何抖动都会变成 guest 的抖动**。

```bash
# 起一台 VM 后观察 vCPU 线程
ps -eLf | grep '[q]emu-system' | awk '{print $2, $NF}'
# 每个 vCPU 一个线程，另有 IO/主线程
```

主要损耗来源：

- **调度抖动**：vCPU 被抢占/迁到别的核，导致 guest 停顿；
- **跨 NUMA 访存**：vCPU 在节点 0，内存却在节点 1，延迟翻倍；
- **SMT 争抢**：两个 vCPU 落在同一物理核的两个超线程上，互相抢执行单元；
- **VM-Exit 开销**：I/O、特权指令、中断导致的陷出。

### vCPU pinning（绑核）

把 vCPU 固定到物理核，消除迁移抖动，并让缓存局部性更好：

```xml
<vcpu placement='static'>4</vcpu>
<cputune>
  <vcpupin vcpu='0' cpuset='2'/>
  <vcpupin vcpu='1' cpuset='3'/>
  <vcpupin vcpu='2' cpuset='4'/>
  <vcpupin vcpu='3' cpuset='5'/>
  <emulatorpin cpuset='0-1'/>
  <iothreadpin iothread='1' cpuset='6'/>
</cputune>
```

- `vcpupin`：vCPU 绑核；
- `emulatorpin`：QEMU 主线程/事件循环绑核，**避免和 vCPU 抢核**；
- `iothreadpin`：I/O 线程绑核，和 vCPU 分开；
- 配合 `<cpu mode='host-passthrough'/>` 获得最佳特性。

查看物理拓扑，避免把 vCPU 放到同一个核的两个超线程上：

```bash
lscpu -e=CPU,CORE,SOCKET,NODE,ONLINE
cat /sys/devices/system/cpu/cpu2/topology/thread_siblings_list
# 例：2,18 → cpu2 和 cpu18 是同一物理核的两个超线程
```

> 绑核不是越多越好。把 vCPU 绑到少数几个核，会失去调度弹性；
> 绑核的正确姿势是"每个 vCPU 一个独立物理核，且不与关键宿主进程共享"。

### NUMA 亲和

跨 NUMA 节点访存延迟显著更高，应让 vCPU 与内存同节点：

```xml
<cpu>
  <numa>
    <cell id='0' cpus='0-3' memory='8' unit='GiB'/>
  </numa>
</cpu>
<numatune>
  <memory mode='strict' nodeset='0'/>
  <memnode cellid='0' mode='strict' nodeset='0'/>
</numatune>
```

```bash
# 查看宿主机 NUMA 拓扑
numactl --hardware
lscpu | grep -i numa
# 查看某 QEMU 进程的内存分布
numastat -p $(pgrep -f 'guest=web01')
```

> 多 NUMA 节点的大 VM，应把 guest 的 NUMA 拓扑**如实暴露**，
> 让 guest 内调度器/数据库也能做本地化，而不是假装只有一个节点。

### `-cpu` 模型与迁移的取舍

| 模型 | 性能 | 迁移性 | 说明 |
| --- | --- | --- | --- |
| `host-passthrough` | 最高 | 差 | 透传全部特性，仅同型号 CPU 可迁 |
| `host-model` | 高 | 较好 | libvirt 按宿主生成、可迁移 |
| 自定义 `custom` | 中 | 最好 | 显式指定基线特性，跨代兼容 |

- 追求单机性能 → `host-passthrough`；
- 需要热迁移 → `host-model` 或自定义基线（模块 09）；
- 可显式加特性，如 `pcid`（减少 TLB 刷新）、Hyper-V enlightenments
  （Windows guest 显著受益）；`invtsc` 能提供稳定 TSC，但**不可迁移**，
  只在不需要迁移时使用。

```xml
<!-- 单机性能优先：透传宿主全部特性 -->
<cpu mode='host-passthrough' check='none'/>

<!-- 需要迁移：用可迁移的宿主模型，不要强开 invtsc -->
<cpu mode='host-model' check='partial'/>
```

### CFS 节流（throttling）

给 vCPU 设了 quota 后，CFS 会在一个周期内用完配额就**强制停跑**，
guest 里表现为 `%steal` 升高、延迟抖动。对延迟敏感负载**不要设 quota**。

```bash
virsh schedinfo web01
# 查看/清除节流：period=100000, quota=0 表示不限
virsh schedinfo web01 --set vcpu_quota=0
```

```xml
<cputune>
  <period>100000</period>
  <quota>0</quota>   <!-- 0 = 不限，延迟敏感场景应保持 0 -->
</cputune>
```

### core scheduling

SMT 的两个超线程共享执行单元与部分缓存，侧信道攻击（L1TF、MDS）可跨超线程
泄露。**core scheduling** 让调度器只在"信任集合"内共享物理核：
同一物理核上只调度属于同一信任域的线程。

```bash
# 内核是否编译了 core scheduling
grep CONFIG_SCHED_CORE /boot/config-$(uname -r)
# 进程可用 prctl(PR_SCHED_CORE, ...) 建立信任组；SMT 是否开启：
cat /sys/devices/system/cpu/smt/active
```

用途：多租户环境既想用 SMT 提升吞吐，又要防跨租户侧信道。
代价是调度灵活性下降。也可简单用 `nosmt` 或 `lscpu` + 绑核避开同核超线程。

### isolcpus / tickless / 中断隔离

把若干 CPU 从内核调度器里"摘出来"，专供 vCPU 使用，减少定时器中断和
其他进程干扰：

```text
# 内核参数示例
isolcpus=2-9 nohz_full=2-9 rcu_nocbs=2-9 irqaffinity=0-1
```

- `isolcpus`：这些 CPU 不参与普通负载均衡；
- `nohz_full`：在这些 CPU 上尽量停掉调度时钟中断（tickless）；
- `rcu_nocbs`：把 RCU 回调移走，避免打扰；
- `irqaffinity`：把中断赶到管理核，别打断 vCPU。

配合：`virsh emulatorpin`、`taskset`、`cgroup cpuset`、`tuned` 的
`realtime`/`cpu-partitioning` profile。**注意**：`isolcpus` 与 cgroup cpuset
各管一段，别混用冲突。

### SMT 与超分

- SMT 通常提升吞吐（10–30%），但延迟敏感场景可考虑关闭或避开同核；
- **超分比**：vCPU 总数 / 物理线程数。1:1 最稳；1.5:1～3:1 常见；
  再高会明显推高 `%steal`，只适合突发型负载；
- 用 `virsh schedinfo`、`mpstat -P ALL 1` 观察实际争抢。

### 其他内核旋钮

```bash
# halt poll：vCPU 空闲时在宿主侧短暂忙等，降低唤醒延迟
cat /sys/module/kvm/parameters/halt_poll_ns
echo 200000 | sudo tee /sys/module/kvm/parameters/halt_poll_ns

# 观察 VM-Exit 原因分布，定位 CPU 侧开销
sudo perf kvm stat live
```

---

## 8.3 内存调优

| 手段 | 效果 | 代价 | 与迁移 |
| --- | --- | --- | --- |
| HugePages（2M/1G） | 减少 TLB miss，提升吞吐 | 需预留，不可 balloon | 支持 |
| THP | 自动合并大页 | 后台 `khugepaged` 抖动 | 支持 |
| KSM | 合并相同页，省内存 | CPU 开销 + 侧信道风险 | 支持（跨节点需注意） |
| Ballooning | 动态回收空闲内存 | 有抖动，实时性差 | 支持 |
| NUMA 绑定 | 降低访存延迟 | 配置复杂、易绑错 | 支持 |
| prefault/mlock | 消除运行期缺页抖动 | 启动慢、内存不可回收 | 支持 |

### HugePages（2M / 1G）

大页把 TLB 覆盖范围从 4K 提升到 2M/1G，显著减少地址翻译开销。
1G 页效果最好，但需要连续内存，**应在系统启动时预留**。

```bash
# 运行时预留 1G 大页 4 个（需连续内存，可能失败）
echo 4 | sudo tee /sys/kernel/mm/hugepages/hugepages-1048576kB/nr_hugepages

# 2M 大页预留 4096 个
echo 4096 | sudo tee /sys/kernel/mm/hugepages/hugepages-2048kB/nr_hugepages

grep -i huge /proc/meminfo
```

```text
# 更稳的做法：内核参数在启动时预留 1G 页
default_hugepagesz=1G hugepagesz=1G hugepages=8
```

```xml
<memoryBacking>
  <hugepages>
    <page size='1' unit='GiB'/>
  </hugepages>
</memoryBacking>
```

> 注意：使用 hugetlbfs 显式大页后，该 VM 内存**不能被 balloon/KSM 回收**，
> 也不会被 swap。1G 页分配失败通常是内存碎片导致，重启是最可靠的办法。

### THP（透明大页）

```bash
cat /sys/kernel/mm/transparent_hugepage/enabled
# always / madvise / never
cat /sys/kernel/mm/transparent_hugepage/defrag
```

- `always`：自动用 2M 页，但 `khugepaged` 后台整理会带来抖动；
- `madvise`：仅对显式 `madvise(MADV_HUGEPAGE)` 的区域使用，KVM 匿名内存
  可配合使用，行为更可控；
- 延迟敏感场景建议 `madvise`，避免 `khugepaged` 抖动。

### KSM（页合并）

```bash
systemctl status ksmtuned
cat /sys/kernel/mm/ksm/run
cat /sys/kernel/mm/ksm/pages_shared
```

- 收益：大量同质 VM（同一镜像）可省大量内存；
- 代价：扫描消耗 CPU，且合并带来的共享缓存有**侧信道风险**（模块 10）；
- 对安全敏感或延迟敏感场景，用 `<memoryBacking><nosharepages/></memoryBacking>` 关闭。

### Ballooning（内存气球）

guest 内 `virtio_balloon` 驱动动态回收空闲页给宿主机：

```xml
<memballoon model='virtio'>
  <stats period='10'/>
</memballoon>
```

```bash
virsh setmem web01 2G --live     # 缩容（有下限）
virsh dommemstat web01
```

- 优点：提高内存利用率、支持动态调整；
- 缺点：回收时有抖动，guest 内可能触发直接回收甚至 OOM；
- **与 HugePages 互斥**（大页不可 balloon）。

### NUMA 绑定与 prefault

```xml
<memoryBacking>
  <allocation mode='immediate'/>   <!-- 启动即分配，避免运行期缺页 -->
  <locked/>                        <!-- mlock，禁止换出（需配 memtune） -->
</memoryBacking>
```

```bash
# 查看 guest 内存实际落在哪些 NUMA 节点
numastat -p $(pgrep -f 'guest=web01')
# 手工绑核 + 绑内存启动
numactl --cpunodebind=0 --membind=0 qemu-system-x86_64 ...
```

- `prefault`/`allocation immediate` 消除运行期缺页抖动，代价是启动慢；
- `locked` 防止内存被换出，但需设置 `<memtune><hard_limit>`，否则可能 OOM。

### 宿主机内存旋钮

```bash
sysctl vm.overcommit_memory     # 1=总是允许，0=启发式
sysctl vm.swappiness            # 降低，减少换出
sysctl vm.zone_reclaim_mode     # 0=优先跨节点分配而非回收本节点
```

---

## 8.4 存储调优

### cache 模式：安全与性能的权衡

| 模式 | 宿主页缓存 | 写安全（掉电） | 性能 | 说明 |
| --- | --- | --- | --- | --- |
| `none` | 绕过（O_DIRECT） | 依赖 guest 刷盘 | 高、延迟低 | 生产常用 |
| `writeback` | 使用 | 可能丢数据 | 高（缓存命中时） | 双重缓存，慎用 |
| `writethrough` | 使用 | 较安全 | 写慢 | 读可缓存 |
| `directsync` | 绕过 | 安全 | 较低 | 每次写同步 |
| `unsafe` | 使用且忽略 flush | 会丢数据 | 最高 | 仅测试 |

> 关键理解：`writeback` 会让同一份数据在**宿主页缓存和 guest 页缓存各一份**，
> 内存翻倍且一致性差。块设备直通/`none` 通常更优。

### io 模式与 io_uring

| `io` 值 | 机制 | 适用 |
| --- | --- | --- |
| `threads` | 阻塞线程池 | 兼容性最好 |
| `native` | Linux AIO | 中高 IOPS，需 `cache=none/directsync` |
| `io_uring` | 现代异步 I/O | 高 IOPS、低延迟首选 |

```xml
<disk type='file' device='disk'>
  <driver name='qemu' type='qcow2' cache='none' io='io_uring'
          discard='unmap' queues='4' iothread='1'/>
  <source file='/var/lib/libvirt/images/db.qcow2'/>
  <target dev='vda' bus='virtio'/>
</disk>
```

### iothread

把 I/O 处理放到独立线程，避免与 vCPU 抢核，并可与 vCPU 分核绑定：

```xml
<iothreads>1</iothreads>
<cputune>
  <iothreadpin iothread='1' cpuset='6'/>
</cputune>
```

### 多队列

virtio-blk/scsi 支持多队列，让多个 vCPU 并行提交 I/O：

```xml
<driver name='qemu' type='qcow2' queues='4'/>
```

```bash
# guest 内确认队列数
ls /sys/block/vda/mq/
cat /sys/block/vda/queue/nr_requests
```

> 队列数一般 ≤ vCPU 数；过多队列反而增加调度开销。

### preallocation 与镜像格式

```bash
# qcow2 预分配元数据（快，空间仍稀疏）
qemu-img create -f qcow2 -o preallocation=metadata disk.qcow2 50G

# qcow2 完全预分配（慢，空间立即占用，避免运行期分配抖动）
qemu-img create -f qcow2 -o preallocation=falloc disk.qcow2 50G

# raw + fallocate（性能最好，无元数据开销）
fallocate -l 50G disk.raw
```

| 格式 | 性能 | 快照/链 | 空间 | 适用 |
| --- | --- | --- | --- | --- |
| raw | 最好 | 无原生快照 | 稀疏/预分配 | 高性能、无快照需求 |
| qcow2 | 略低 | 支持、可压缩 | 稀疏 | 通用、需快照/克隆 |
| LVM | 好 | 快照 | 块设备 | 本地块存储 |
| RBD/Ceph | 中 | 快照/克隆 | 分布式 | 云、可迁移 |

### 后端选型与 I/O 限速

- 物理层是上限：本地 NVMe > 本地 SATA SSD > 本地 HDD > SAN/NFS；
- NFS 上跑数据库通常不行（延迟与一致性），优先块存储；
- 用 `<iotune>` 限制单盘 IOPS/带宽，防止一个 VM 打爆后端：

```xml
<iotune>
  <total_iops_sec>20000</total_iops_sec>
  <total_bytes_sec>524288000</total_bytes_sec>
  <read_iops_sec>10000</read_iops_sec>
</iotune>
```

- `discard='unmap'` 让 guest 的 TRIM 透传到后端（SSD 寿命/空间回收）；
- 避免在宿主用文件系统层再叠一层缓存。

---

## 8.5 网络调优

### 数据路径与手段

| 手段 | 说明 | 收益 |
| --- | --- | --- |
| virtio-net | 必选，替代 e1000 | 基础 |
| vhost-net | 数据面进内核 | 显著降低 CPU |
| 多队列 | `queues=N`，多 vCPU 并行收发 | 高 PPS/吞吐 |
| 巨帧 MTU 9000 | 减少分片与协议开销 | 大包吞吐 |
| offload | TSO/GSO/GRO、校验和卸载 | 降低 CPU |
| SR-IOV VF | 接近原生 | 极限性能 |
| XDP/eBPF | 宿主侧旁路加速 | 特定场景 |

```xml
<interface type='network'>
  <source network='default'/>
  <model type='virtio'/>
  <driver name='vhost' queues='4'/>
  <mtu size='9000'/>
</interface>
```

```bash
# 宿主/guest 两侧都要放行巨帧，端到端一致才有效
ip link set enp3s0 mtu 9000
ip link set br0 mtu 9000

ethtool -l enp3s0          # 队列数
ethtool -g enp3s0          # ring buffer
ethtool -k enp3s0 | grep -i offload
ethtool -C enp3s0          # 中断合并
ethtool -L enp3s0 combined 8
ethtool -G enp3s0 rx 4096 tx 4096
```

### 中断与 CPU 亲和

```bash
# 看网卡中断落在哪些 CPU，避免全挤在 CPU0
grep enp3s0 /proc/interrupts
# 用 irqbalance 或手工设置 smp_affinity
```

### 忙等与批处理

```bash
sysctl net.core.busy_poll net.core.busy_read   # 低延迟轮询
sysctl net.core.netdev_max_backlog
sysctl net.core.rmem_max net.core.wmem_max
```

### 网络后端对比

| 后端 | 性能 | 隔离 | 迁移 | 场景 |
| --- | --- | --- | --- | --- |
| NAT | 中 | 好 | 好 | 开发 |
| 桥接 + vhost | 高 | 好 | 好 | 生产通用 |
| macvtap | 高 | 中 | 好 | 宿主-guest 不需互通 |
| OVS | 高 | 好 | 好 | SDN/多租户 |
| SR-IOV VF | 接近原生 | 最好 | 差 | 高性能、可牺牲迁移 |

---

## 8.6 压测与指标解读

### 工具与关注点

| 维度 | 工具 | 关注指标 |
| --- | --- | --- |
| CPU | `stress-ng`、`sysbench cpu` | `%steal`、上下文切换、run queue |
| 内存 | `sysbench memory`、`stream` | 带宽（GB/s）、延迟（ns） |
| 磁盘 | `fio` | IOPS、吞吐、`lat (p99)`、`await` |
| 网络 | `iperf3`、`netperf` | 吞吐、PPS、重传、CPU |
| 整体 | `perf kvm stat` | VM-Exit 原因分布 |

### fio 参数怎么读

```bash
# guest 内：4K 随机读，深度 32，4 个 job
fio --name=t --ioengine=libaio --direct=1 --rw=randread --bs=4k \
    --iodepth=32 --numjobs=4 --size=1G --runtime=30 --ramp_time=5 \
    --group_reporting
```

- `iodepth`：单 job 的并发请求数，与延迟强相关（Little's Law）；
- `numjobs`：进程/线程数，常用来匹配 vCPU；
- `direct=1`：绕过 guest 页缓存，测真实设备；
- `ramp_time`：跳过预热，取稳态；
- 看 **IOPS、吞吐、`clat` 的 p99/p99.9**，不要只看平均。

### 网络压测

```bash
iperf3 -s                      # 服务端
iperf3 -c <ip> -P 8 -t 30      # 8 条并行流
iperf3 -c <ip> -u -b 10G       # UDP 测丢包/抖动
iperf3 -c <ip> -M 1400         # 指定 MSS
```

### 宿主机与 guest 侧观测

```bash
mpstat -P ALL 1                # 每核利用率，看 %steal
vmstat 1                       # run queue(r)、swap(si/so)、steal(st)
iostat -x 1                    # %util、await、aqu-sz
pidstat -p <qemu-pid> 1        # QEMU 进程 CPU
numastat -p <qemu-pid>         # NUMA 内存分布
sar -n DEV 1                   # 网络吞吐/丢包
turbostat                      # 频率、C-state、IPC
cat /proc/pressure/{cpu,io,memory}   # PSI 压力指标
```

### 关键指标解读

| 指标 | 含义 | 高说明什么 |
| --- | --- | --- |
| `%steal`（guest 内） | vCPU 想要 CPU 但被宿主拿走 | 宿主超分/争抢严重 |
| `%iowait` | CPU 等 I/O | 不一定是瓶颈，要看 `await` |
| `await`（iostat） | 单次 I/O 平均等待（含排队） | 后端饱和或队列太深 |
| `%util`（iostat） | 设备忙比例 | 接近 100% 且 await 高 = 饱和 |
| p99/p99.9 延迟 | 尾延迟 | 抖动/争抢的真相 |
| VM-Exit/s | 陷出频率 | I/O 模拟过重，考虑 vhost/直通 |

> 经验：**看 `%util` 高不高，不如看 `await` 和 p99 涨没涨**。
> 有些设备 `%util` 100% 但延迟仍低（如 NVMe 并行度高）。

---

## 8.7 常见性能量级参考

以下为现代 x86 服务器上的**数量级**参考，随硬件/负载差异巨大，仅用于
判断"是否离谱"：

| 场景 | 量级 | 备注 |
| --- | --- | --- |
| vCPU 计算（sysbench） | 原生的 90–99% | 有 EPT/APICv 时接近原生 |
| VM-Exit 单次开销 | 约 1–5 微秒 | 取决于退出原因与硬件 |
| virtio-blk + io_uring（本地 NVMe，4K 随机读） | 数十万 IOPS | 多队列 + 深度足够 |
| 全模拟 IDE 4K 随机读 | 几千 IOPS | 差距可达百倍 |
| virtio-net + vhost（单流） | 10–40 Gbps | 受单队列/单核限制 |
| 多队列 / SR-IOV | 40–100 Gbps | 需多核并行 |
| 内存带宽（THP + 同 NUMA） | 原生的 70–95% | 跨 NUMA 可跌到 ~60% |
| 跨 NUMA 访存延迟 | 约 1.5–2 倍本地 | 取决于平台 |
| 热迁移内存吞吐 | 受网络带宽限制 | 10GbE ≈ 1.1 GB/s 理论上限 |
| 热迁移停机时间 | 毫秒~几百毫秒 | 收敛良好时 |

> 用法：先测基线，再对照。若 4K 随机读只有几千 IOPS，先怀疑用了模拟设备；
> 若网络单流只有 2 Gbps，先查是否单队列/中断挤在 CPU0。

---

## 8.8 分场景调优清单

**延迟敏感型（数据库 / 交易）**

- vCPU 绑核 + `emulatorpin`/`iothreadpin` 分离，禁用 CFS quota；
- HugePages 1G + prefault + `locked`；
- 磁盘 `cache=none, io=io_uring`，专用 NVMe/LVM；
- 关 KSM、考虑关 THP `always`；
- 关 SMT 同核争抢或做 core scheduling。

**吞吐型（Web / 缓存 / 批量）**

- 多队列 virtio + vhost，队列数≈vCPU；
- 巨帧、offload 全开；
- 适度超分，用 cgroup 限速防单 VM 打爆；
- KSM 省内存（安全允许时）。

**HPC / AI**

- 整卡 GPU 直通或 SR-IOV；
- NUMA 严格绑定 + 本地内存；
- 巨页 + 大页对齐；
- 网络 RDMA（若支持）。

---

## 动手实验

**实验 8-1：建立性能基线**

分别用 `fio` / `iperf3` 记录默认配置下的数据，存成文件，作为后续对照。

```bash
fio --name=base --ioengine=libaio --direct=1 --rw=randread --bs=4k \
    --iodepth=32 --numjobs=4 --size=1G --runtime=30 --group_reporting \
    | tee /tmp/baseline-fio.txt
```

**实验 8-2：vCPU 绑核对比**

同一负载，`placement='auto'` vs `static + vcpupin`，对比 fio 的 p99 延迟。

**实验 8-3：HugePages 收益**

内存密集负载（如 `sysbench memory`）开启巨页前后对比带宽与延迟。

**实验 8-4：perf kvm 定位热点**

```bash
sudo perf kvm stat live
# 观察 EXTERNAL_INTERRUPT / MSR_WRITE / IO_INSTRUCTION 等占比
```

**实验 8-5：存储模式对比**

同一后端分别用 `cache=writeback/io=threads` 与 `cache=none/io=io_uring`，
用 `fio` 对比 IOPS 与 p99，观察宿主页缓存的影响。

**实验 8-6：网络多队列**

单队列 vs 4 队列，`iperf3 -P 8` 对比吞吐，同时用 `mpstat` 看 CPU 分布。

---

## 排错指南

| 现象 | 根因 | 定位命令 | 处理 |
| --- | --- | --- | --- |
| 延迟抖动大 | vCPU 被调度走/超分 | `mpstat -P ALL 1`、`virsh schedinfo` | pinning + 隔离核，quota=0 |
| `%steal` 高 | 宿主超分严重 | guest `mpstat`、宿主 `vmstat` | 减少超分或绑核 |
| 吞吐上不去 | 单队列 / 后端瓶颈 | `ethtool -l`、`iostat -x`、`ls /sys/block/*/mq` | 多队列 + 换后端 |
| 磁盘 p99 高 | 队列太深 / 后端饱和 | `iostat -x 1`（await/aqu-sz） | 降 iodepth、换 NVMe、开 io_uring |
| 巨页分配失败 | 内存碎片/预留不足 | `grep -i huge /proc/meminfo` | 重启预留，或改用 2M |
| 内存带宽低 | 跨 NUMA | `numastat -p <qemu>` | `numatune` 严格绑定 |
| 网络重传/丢包 | offload 或 MTU 不一致 | `ethtool -S`、`ip link show` | 统一 MTU，检查 offload |
| `%util` 100% 但延迟低 | NVMe 高并行，正常 | `iostat -x` | 无需处理，看 p99 |
| 首次压测好、复测差 | 页缓存假象 | `free`、`/proc/meminfo` | `drop_caches`、`ramp_time` |
| 迁移后性能下降 | 目标机配置不一致 | 对比两侧 XML/拓扑 | 统一 pinning/NUMA/大页 |

### 案例复盘 A：数据库 p99 周期性尖刺

**现象**：数据库 VM 平均延迟正常，但每隔几秒出现一次 p99 尖刺，
业务侧偶发超时。

**定位**：guest 内 `mpstat` 看到周期性 `%steal`；宿主 `virsh schedinfo`
发现设置了 `vcpu_quota`；`vmstat` 显示 run queue 偶尔很高。

**根因**：CFS quota 用完时 vCPU 被强制停跑，形成周期性停顿。

**处理**：`vcpu_quota=0` 取消节流，配合绑核与 `isolcpus`。
尖刺消失。**教训：延迟敏感负载永远不要设 CPU quota**。

### 案例复盘 B：磁盘吞吐只有裸机一半

**现象**：同样的 NVMe，VM 内 `fio` 吞吐只有裸机的约一半。

**定位**：`virsh dumpxml` 发现 `cache='writeback' io='threads' queues='1'`；
宿主 `iostat -x` 显示设备本身很闲；guest `fio` 的 CPU 占用很高。

**根因**：`writeback` 造成双重页缓存与额外拷贝，`threads` 模式上下文切换多，
单队列又限制了并行度。

**处理**：改 `cache='none' io='io_uring' queues='4'`，绑定 iothread。
吞吐翻倍。**结论：存储调优先看模式，再看后端**。

### 案例复盘 C：网络只有 2 Gbps

**现象**：25GbE 环境，VM 间 `iperf3` 单流只有 2 Gbps。

**定位**：`ethtool -l` 显示 1 队列；`grep virtio /proc/interrupts` 发现
中断全在 CPU0；`mpstat` 显示 CPU0 100%、其他核空闲。

**根因**：单队列 + 单中断，所有收包处理挤在一个核上，单核成为瓶颈。

**处理**：`queues='8'` + `ethtool -L` 多队列 + 中断亲和分散。
吞吐提升到 20 Gbps 以上。**结论：高带宽场景先看队列与中断分布**。

### 案例复盘 D：内存带宽不达标

**现象**：HPC VM 的内存带宽只有预期的一半多。

**定位**：`numastat -p <qemu>` 显示内存跨了两个 NUMA 节点，
而 vCPU 只绑在节点 0。

**根因**：vCPU 在节点 0，一半内存却分配在节点 1，跨节点访存。

**处理**：`<numatune mode='strict' nodeset='0'>` + vCPU 同节点绑定，
带宽恢复到本地水平。

---

## 练习

1. 为一个 8 vCPU 的数据库 VM 设计完整的 CPU/内存/存储调优方案，
   写明每项的**收益与代价**。
2. 解释 `emulatorpin` 为什么必要，`iothreadpin` 又解决什么问题。
3. 用数据说明 `cache=none` 相比 `writeback` 的延迟优势与吞吐代价，
   设计一个能复现该结论的 `fio` 实验。
4. 给定一台 `%steal` 持续 10% 的宿主，列出你的排查与处理步骤。
5. 说明何时该用 HugePages、何时用 THP，两者能否同时用？
6. 为什么"平均延迟"会骗人？用 p99 举一个业务受影响的反例。

## 延伸阅读

- `Documentation/admin-guide/mm/`（HugePages、KSM、THP）
- `Documentation/virt/kvm/`（KVM 内部机制、halt polling）
- `Documentation/scheduler/sched-design-CFS.rst`（CFS、core scheduling）
- Red Hat 虚拟化调优指南
- `man numactl`、`man taskset`、`man fio`、`man iostat`
- Brendan Gregg《Systems Performance》（USE 方法、尾延迟）
