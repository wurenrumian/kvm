# 模块 11 · 监控与排障

## 学习目标

- 建立 KVM 分层排障方法论（宿主机 → QEMU → guest）；
- 熟练使用 `kvm_stat`、`perf kvm`、`virsh domstats`、`trace-cmd`；
- 能根据症状快速定位是 CPU、内存、存储还是网络问题。

---

## 11.1 分层排障方法论

```
症状
 ├─ 宿主机层：CPU steal、内存压力、磁盘 IO、网络丢包
 ├─ QEMU 层：vCPU 线程状态、VM-Exit 频率、设备模拟
 └─ guest 层：guest 内核、驱动、应用
```

原则：**自下而上**，先排除宿主机资源问题，再看虚拟化层，最后进 guest。

## 11.2 常用工具速览

| 工具 | 层次 | 用途 |
| --- | --- | --- |
| `top` / `htop` | 宿主 | CPU/内存总览、steal time |
| `kvm_stat` | KVM | VM-Exit 原因统计 |
| `perf kvm` | KVM | 精确的 VM-Exit/guest 剖析 |
| `virsh domstats` | libvirt | VM 级指标 |
| `virsh domblkstat/domifstat` | libvirt | 块/网络 I/O 统计 |
| `trace-cmd` / ftrace | 内核 | 跟踪 kvm 事件 |
| `pidstat -t` | 宿主 | 按线程看 vCPU |
| `iostat` / `iotop` | 宿主 | 磁盘压力 |
| `sar` / `vmstat` | 宿主 | 历史趋势 |

## 11.3 kvm_stat

```bash
sudo dnf install -y kvm_stat
sudo kvm_stat                 # 实时
sudo kvm_stat --once          # 一次性
```

关注字段：

- `exit_*`：各类 VM-Exit 次数（IO、MSR、外部中断…）；
- 某类 exit 异常高 → 对应路径是瓶颈（如 IO 频繁 → 换 virtio/vhost）。

## 11.4 perf kvm

```bash
sudo perf kvm stat live
sudo perf kvm stat record -a sleep 10
sudo perf kvm stat report

# 剖析 guest 内热点（需要 guest 符号）
sudo perf kvm --host --guest record -a sleep 5
sudo perf kvm --host --guest report
```

## 11.5 virsh domstats

```bash
virsh domstats web01                    # 全量
virsh domstats web01 --state --cpu-total --balloon --block --interface
virsh domstats --list-running
```

输出含 vCPU 时间、内存、块 I/O、网络收发等。

## 11.6 块与网络统计

```bash
virsh domblklist web01
virsh domblkstat web01 vda
virsh domiflist web01
virsh domifstat web01 vnet0

# guest 内看磁盘/网卡
virsh qemu-monitor-command web01 --hmp 'info block'
virsh qemu-monitor-command web01 --hmp 'info network'
```

## 11.7 内核跟踪

```bash
sudo dnf install -y trace-cmd
sudo trace-cmd record -e kvm:* sleep 5
sudo trace-cmd report | head

# 或用 ftrace 直接看 kvm 事件
sudo ls /sys/kernel/debug/tracing/events/kvm/
```

## 11.8 典型症状 → 定位路径

### 症状：VM 卡顿、延迟高

```bash
top                          # 看 steal time
virsh domstats web01 --cpu-total
sudo perf kvm stat live      # 看 VM-Exit
# 若 steal 高 → 宿主机超分/绑核问题（模块 08）
```

### 症状：磁盘 I/O 慢

```bash
virsh domblkstat web01 vda
iostat -x 1                  # 宿主磁盘 util / await
# 检查 cache 模式、是否 virtio、后端是否瓶颈（模块 05/08）
```

### 症状：网络慢/丢包

```bash
virsh domifstat web01 vnet0
ethtool -S enp3s0 | grep -i drop
iperf3 -c <ip> -P 8
# 检查多队列、vhost、MTU（模块 06）
```

### 症状：内存不足 / OOM

```bash
virsh dommemstat web01
grep -E 'MemAvailable|Swap' /proc/meminfo
# 检查 balloon、KSM、超分比例
```

### 症状：VM 起不来

```bash
virsh start web01 --debug
sudo tail -f /var/log/libvirt/qemu/web01.log
sudo ausearch -m avc -ts recent      # SELinux
journalctl -u virtqemud -n 50
```

## 11.9 日志位置

| 日志 | 路径 |
| --- | --- |
| libvirt QEMU 日志 | `/var/log/libvirt/qemu/<name>.log` |
| libvirt daemon | `journalctl -u virtqemud` |
| 内核 | `dmesg`、`journalctl -k` |
| SELinux | `/var/log/audit/audit.log` |

---

## 动手实验

**实验 11-1：建立监控基线**

```bash
virsh domstats web01 --cpu-total --balloon --block --interface
```

**实验 11-2：制造 IO 压力并定位**

```bash
# guest 内跑 fio，宿主用 kvm_stat 和 iostat 观察
```

**实验 11-3：perf kvm 剖析**

```bash
sudo perf kvm stat record -a sleep 5 && sudo perf kvm stat report
```

**实验 11-4：故意制造启动失败**

把镜像路径改错，观察 libvirt 日志和 SELinux 审计日志。

---

## 排错指南（速查）

| 症状 | 先查 |
| --- | --- |
| 卡顿 | `top` steal、`perf kvm` |
| IO 慢 | `domblkstat`、`iostat`、cache 模式 |
| 网络差 | `domifstat`、多队列、vhost |
| 内存紧 | `dommemstat`、超分、KSM |
| 启动失败 | qemu 日志、SELinux avc |
| 迁移失败 | CPU 兼容、存储、网络 |

---

## 练习

1. 给出一套"VM 变慢"的完整排查流程图。
2. 说明 `kvm_stat` 与 `perf kvm` 的区别和各自适用场景。
3. 如何判断瓶颈在 guest 内部而非虚拟化层？

## 延伸阅读

- `man kvm_stat`、`perf kvm --help`
- libvirt 监控 API：<https://libvirt.org/html/libvirt-libvirt-domain.html>
- 内核 tracepoint：`/sys/kernel/debug/tracing/events/kvm/`
