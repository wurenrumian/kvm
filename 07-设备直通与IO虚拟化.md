# 模块 07 · 设备直通与 I/O 虚拟化

## 学习目标

- 理解 virtio / vhost / VFIO 各自的定位与数据路径；
- 掌握 IOMMU 与设备直通（PCI passthrough）的配置；
- 了解 GPU 直通与 vGPU 的差异和适用场景。

---

## 7.1 I/O 虚拟化的演进

| 层次 | 机制 | 数据是否经过 QEMU | 性能 |
| --- | --- | --- | --- |
| 全模拟 | e1000 / IDE | 是（每字节模拟） | 低 |
| virtio | 共享环 + 半虚拟化驱动 | 是 | 高 |
| vhost | 数据面下沉内核 | 否（内核处理） | 更高 |
| vhost-user | 数据面到独立进程（DPDK/OVS） | 否 | 极高 |
| VFIO 直通 | 设备整块给 guest | 否（硬件直接访问） | 接近原生 |

## 7.2 virtio 深入

virtio 的核心是 **virtqueue**：guest 驱动和 hypervisor 通过共享内存里的
环（ring）交换描述符，避免为每次 I/O 做完整硬件模拟。

常见 virtio 设备：

| 类型 | 用途 | XML |
| --- | --- | --- |
| virtio-blk | 块设备，简单高效 | `<disk ... bus='virtio'/>` |
| virtio-scsi | SCSI 语义，支持更多特性（热插拔、多队列、TRIM） | `<controller type='scsi' model='virtio-scsi'/>` |
| virtio-net | 网卡 | `<model type='virtio'/>` |
| virtio-gpu / virtiofs | 图形 / 文件共享 | — |

**virtio-blk vs virtio-scsi**：

- 单盘、追求极致 → virtio-blk；
- 多盘、需热插拔/多队列/TRIM → virtio-scsi。

## 7.3 vhost 与 vhost-user

```bash
lsmod | grep -E 'vhost'
# vhost_net        # 网卡数据面内核加速
# vhost           # 通用框架
```

```xml
<interface type='network'>
  <source network='default'/>
  <model type='virtio'/>
  <driver name='vhost' queues='4'/>   <!-- 内核 vhost -->
</interface>
```

vhost-user 用于 OVS-DPDK 等用户态数据面，需在 `<driver name='vhostuser'>` 中
指定 chardev socket。

## 7.4 VFIO 设备直通

### 前置条件

1. CPU 支持 **VT-d / AMD-Vi**（IOMMU）；
2. 内核开启 IOMMU：`intel_iommu=on iommu=pt`（加到内核 cmdline）；
3. 设备能被 IOMMU 分组隔离；
4. 设备绑定到 **vfio-pci** 驱动。

### 步骤

```bash
# 1. 确认 IOMMU 开启
dmesg | grep -i -e DMAR -e IOMMU
# 应为 "DMAR: IOMMU enabled"

# 2. 找到目标设备
lspci -nn | grep -i nvidia        # 例：01:00.0 VGA ... [10de:xxxx]

# 3. 查看 IOMMU 分组
for d in /sys/kernel/iommu_groups/*/devices/*; do
  echo "Group $(basename $(dirname $(dirname $d))): $(basename $d)"
done

# 4. 绑定到 vfio-pci
# 方式 A：内核参数
#   vfio-pci.ids=10de:xxxx,10de:yyyy
# 方式 B：运行时（驱动解绑）
echo 0000:01:00.0 | sudo tee /sys/bus/pci/devices/0000:01:00.0/driver/unbind
echo vfio-pci | sudo tee /sys/bus/pci/devices/0000:01:00.0/driver_override
echo 0000:01:00.0 | sudo tee /sys/bus/pci/drivers_probe
```

XML：

```xml
<hostdev mode='subsystem' type='pci' managed='yes'>
  <source>
    <address domain='0x0000' bus='0x01' slot='0x00' function='0x0'/>
  </source>
</hostdev>
```

> `managed='yes'` 让 libvirt 自动完成驱动绑定/解绑。
> 同一 IOMMU 组内的所有设备需一起直通，否则隔离不成立。

### 检查隔离质量

```bash
# 若设备与其他设备同组，需 ACS 覆盖或换 PCIe 插槽
lspci -vvv -s 01:00.0 | grep -i acs
```

## 7.5 GPU 直通 vs vGPU

| 方式 | 原理 | 一卡多机 | 适用 |
| --- | --- | --- | --- |
| **整卡直通（VFIO）** | 整块 GPU 给一台 guest | 否 | 单机高性能（AI 训练） |
| **vGPU（NVIDIA vGPU / Intel GVT-g / SR-IOV）** | 硬件切分显存给多台 guest | 是 | VDI、多租户图形 |
| **GPU 半虚拟化（virtio-gpu / venus）** | 共享渲染 | 是 | 轻量图形 |

GPU 直通常见坑：

- 消费级卡可能有 vendor reset bug（重启 guest 后 GPU 不恢复）；
- 需要 vBIOS 文件（某些卡）；
- 音频设备（function 0x1）通常要同组一起直通；
- 宿主机不能再占用该 GPU（无显示输出）。

```xml
<!-- 显卡 + 音频一起直通 -->
<hostdev mode='subsystem' type='pci' managed='yes'>
  <source><address domain='0x0000' bus='0x01' slot='0x00' function='0x0'/></source>
</hostdev>
<hostdev mode='subsystem' type='pci' managed='yes'>
  <source><address domain='0x0000' bus='0x01' slot='0x00' function='0x1'/></source>
</hostdev>
```

## 7.6 其他直通：USB / SCSI / 串口

```xml
<!-- USB 设备直通 -->
<hostdev mode='subsystem' type='usb' managed='yes'>
  <source>
    <vendor id='0x1234'/><product id='0x5678'/>
  </source>
</hostdev>

<!-- USB 控制器直通（整组 USB 口） -->
<controller type='usb' model='qemu-xhci'/>
```

---

## 动手实验

**实验 7-1：对比 virtio-blk 与 IDE 性能**

同一 guest 分别用 `bus='ide'` 和 `bus='virtio'`，`fio` 对比 IOPS。

**实验 7-2：查看 IOMMU 分组**

```bash
ls /sys/kernel/iommu_groups/ | wc -l
for d in /sys/kernel/iommu_groups/*/devices/*; do
  echo "Group $(basename $(dirname $(dirname $d))): $(basename $d)"
done | head
```

**实验 7-3（进阶）：直通一个 USB 设备**

用 `lsusb` 找设备，按 7.6 的 XML 直通，进 guest 用 `lsusb` 验证。

**实验 7-4：确认 vhost 生效**

```bash
lsmod | grep vhost_net
# guest 跑 iperf3 时，用 top 观察 vhost 线程 CPU 占用
```

---

## 排错指南

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| 直通后 guest 黑屏 | 未关宿主机对该 GPU 的占用 | 换集成显卡输出 |
| `vfio-pci` 绑定失败 | 设备被其他驱动占用 | 先 unbind，或内核参数屏蔽 |
| IOMMU 分组过大 | 缺 ACS | 换插槽 / 开 ACS override |
| guest 重启后 GPU 失效 | vendor reset bug | 打内核补丁或换卡 |
| virtio 设备 guest 不识别 | 缺驱动 | 装 `virtio-win`/内核模块 |

---

## 练习

1. 画图对比 virtio-net、vhost-net、SR-IOV VF 三种网络的数据路径。
2. 为什么 GPU 直通后宿主机通常不能再用这块卡？
3. 说明 IOMMU 分组对直通安全性的意义。

## 延伸阅读

- 内核文档：`Documentation/driver-api/vfio.rst`
- VFIO 与 IOMMU：<https://www.kernel.org/doc/html/latest/driver-api/vfio.html>
- NVIDIA vGPU 文档：<https://docs.nvidia.com/grid/>
- QEMU virtio 规范：<https://docs.oasis-open.org/virtio/virtio/>
